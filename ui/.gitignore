# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# dist is deliberately NOT ignored. advisor_api serves the built client, and
# runctl starts it on a machine that may have no node toolchain -- the dashboard it
# replaced was a single .py that always ran, and that property is worth keeping.
