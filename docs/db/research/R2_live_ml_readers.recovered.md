# R2 -- every reader on the live advisor loop and the ML/training paths

Method: every file in the lane list was read in full at HEAD; DB facts were measured on `tw_stack` through `decisions.pg.connect(readonly=True)` with `statement_timeout` set, on 2026-09-04 21:15 PDT. Numbers marked *measured* are from those runs; *logged* means taken from `D:\twdata\logs\advisor\session_1000x20_20260902_222645.log` or model `meta.json`. Report was not written to the scratchpad (plan mode forbids writes outside the plan file).

Corpus state at measurement: `decisions` 206,907 rows (max `decision_id` 206945, newest ts 2026-09-03 20:43 PDT); `taken` 205,533; `entities` 1.13M; `offers` 24.8M (2.2 GB); `offer_scores` 22.4M (2.6 GB); `offer_model_scores` 22.1M (2.3 GB); `actions` 5.1M (1.6 GB); `blobs` 949k rows, 2.4 GB on disk, 8,079 MB of JSON text (`SUM(n)`); `interrupts` 43,392; `campaigns` 4,827 (4,749 with `first_decision_id`). Recorder idle (newest `rpc_requests` ts 2026-09-03 20:44).

---

## A. EVERY DB READ

All Postgres access goes through `decisions/pg.py:49-64` (`psycopg.connect`, optional `SET search_path`, `SET default_transaction_read_only = on` when `readonly=True`). Table names below are the physical tables of `decisions/pg_schema.py:28-161` and the views of `pg_schema.py:168-276` (`decision_points`, `turn_open`, `turn_close`, `turn_bounds`, `action_taken`, `action_offers`, `interrupt_decisions`, `start_counts`, `campaign_gains`).

### A1. Inside the per-decision round trip (`advisor/loop.py:_run_turn`, timed by the `timing` dict at `loop.py:546-558`)

| # | file:line | SQL / helper | tables, columns | blob roles decoded | volume | caching | latency context |
|---|---|---|---|---|---|---|---|
| A1.1 | `advisor/loop.py:505` -> `decisions/journal.py:173-187` `request_snapshot` -> `journal.py:77-82` `_ask` | `INSERT INTO rpc_requests(req_id,kind,ts,payload)` + `SELECT pg_notify('rpc_requests',..)` | `rpc_requests` (write) | none | 1 row | thread-local connection `journal.py:26-41` (LISTENs `rpc_requests`,`rpc_responses`; never closed -- no caller of `journal.close`) | start of `roundtrip_ms` |
| A1.2 | `journal.py:141-166` `_await` | `SELECT decision_id,payload,error FROM rpc_responses WHERE req_id=%s` polled, `con.notifies(timeout<=1s)` between polls | `rpc_responses` | none | 1 row per poll | none | the wait is `roundtrip_ms` (measured last 5k decisions: p50 719 ms, p90 1276 ms; of which recorder `collect_ms` p50 520 / p90 1082, `store_ms` p50 103 / p90 219, `pickup_lag_ms` p50 0) |
| A1.3 | `journal.py:169-170` -> `decisions/store.py:574-587` `read_decision` | `SELECT d.turn,c.campaign_key,bc.z,bw.z FROM decisions d JOIN campaigns c .. LEFT JOIN blobs bc ON bc.blob_id=d.campaign_blob LEFT JOIN blobs bw ON bw.blob_id=d.world_blob WHERE d.decision_id=%s` | `decisions`(turn,campaign_id,campaign_blob,world_blob), `campaigns`(campaign_key), `blobs`(z) | **campaign_blob** (`json.loads`, ~0.9 KB avg, 2.3 KB max), **world_blob** (~12 KB avg, 41 KB max) | 1 decision | thread-local `DecisionStore(readonly)` `journal.py:44-49`; no result cache | measured 1.7-8.6 ms for the newest decisions; included in `roundtrip_ms` (after `_t_received`? no: `_t_received` is stamped at `journal.py:183` after the read, so the read is inside `roundtrip_ms`) |
| A1.4 | `store.py:584` -> `store.py:549-572` `_entities_and_offers(" WHERE e.decision_id=%s")` | (a) `SELECT e.decision_id,e.entity_seq,e.context_kind,e.context_id,b.z FROM entities e JOIN blobs b ON b.blob_id=e.features_blob WHERE e.decision_id=%s ORDER BY e.decision_id,e.entity_seq`; (b) `SELECT o.decision_id,o.offer_seq,o.entity_seq,a.action_type,a.action_key,a.params FROM offers o JOIN actions a .. WHERE o.decision_id=%s ORDER BY ..` | `entities`, `blobs`, `offers`, `actions` | **entity features_blob** per entity (avg: campaign 15.9 KB, lord 12.8 KB, hero 7.0 KB, province 3.8 KB; 5.8 entities/decision avg, 14 max) | 1 decision | none | inside `roundtrip_ms`. Query (b) always returns 0 rows on the live path: options are attached later by `journal.log_options` (`loop.py:508`), so `e["offers"]` from the DB is empty and is filled in memory by `O.attach` (`loop.py:509`, `options.py:878-887`) -- a wasted query per decision |
| A1.5 | `loop.py:507` `pol.gate.apply(record)` -> `options.py:863-875` -> `generate` `options.py:686-709` | no decision-DB SQL; **reference** lookups via `features_db` (see C) | `reference.skill_actions`, `reference.agent_permitted_subtypes`, `reference.agent_actions`, `reference.agent_abilities` | none | per new key only | process-lifetime dict caches (`features_db.py:312-324 _unlock_cache`, `options.py:261-274 _subtype_types`, `options.py:258,277-296 _hero_matrix`) | `generate_ms` measured p50 1 ms / p90 2 ms |
| A1.6 | `loop.py:508` `journal.log_options` -> `_ask("options")` | `INSERT INTO rpc_requests` + notify | `rpc_requests` (write) | none | 1 row, payload = all surviving options (offers p50 93, p90 172, max 458 per decision) | -- | `generate_ms` bucket |
| A1.7 | `loop.py:536` `pol.choose(record)` -> `policy.py:122-170` -> `model.py:186-210` `Ranker.score` -> `features.py:1082-1087 decision_rows` | no decision-DB SQL; reference lookups `features.py:526-545 _db_features`, `:194-197 lord_stack_block`, `:379 _queue_turns_to_clear`, `:1050,1069 ancillary_features` | `reference.buildings, building_chains, tech, units, skills, rituals, ancillaries, ancillary_effects` | none | per never-seen key | `features_db.py:22-34 _lookup_cache`, `:88-120 _res_cache` (no TTL, misses cached as `{}`) | `score_ms` measured p50 39 ms / p90 56 ms / max 367 ms (includes CatBoost predict on ~100 rows x 311 columns and, when `greedy_gnn` is in the mix, `build_graph` + torch inference `policy.py:128-130,172-180`) |
| A1.8 | `policy.py:128` `_graph` -> `mapgraph/build.py:138-509` -> `schema.py:298-303 _dense` -> `catalogue.py:82-98 dense_ids`, `catalogue.py:119-143 attrs`, `catalogue.py:27-71 _load` | 3 loads, once per process | `reference.buildings, building_chains, units, tech, skills, rituals, agent_actions, ancillaries, agent_permitted_subtypes, effects_meta, skill_actions, tech_links, skill_links, ancillary_effects` | none | whole tables (measured `_load` 0.19 s, `dense_ids` 0.09 s, `attrs` 0.06 s) | module dicts `_CACHE`, `_DENSE`, `_ATTRS`, `schema._DENSE_CACHE` (process lifetime) | first decision of the process only |
| A1.9 | `loop.py:571` `journal.log_pick` | `INSERT INTO rpc_requests` (pick + `P.scores_for_store(ranked)` for **every** ranked offer, `policy.py:207-221`, `limit=None`) | `rpc_requests` (write); recorder then writes `offer_scores`/`offer_model_scores` rows = number of offers | none | 1 row (~100 score dicts) | -- | `housekeep_parts.pick_log` (measured 0-1 ms) |
| A1.10 | `loop.py:580,597,605` `journal.log_verification`; `loop.py:764` | `INSERT INTO rpc_requests` | `rpc_requests` (write) | none | 1 row | -- | `verify_log` (0 ms) |
| A1.11 | `loop.py:664` `_drain_interrupts` -> `journal.log_interrupt` `journal.py:204-207` | `INSERT INTO rpc_requests` per interrupt record | `rpc_requests` (write) | none | 0-n rows | -- | `drain` (0 ms) |
| A1.12 | `loop.py:235` Watchdog -> `journal.request_hash` `journal.py:197-201` (every 15 s, `watchdog.py:12`) | RPC insert + `_await` poll | `rpc_requests`, `rpc_responses` | none | 1 | separate thread-local connection per Watchdog thread (one thread per campaign, `loop.py:235-237`); never closed | background thread, not in the decision timing |

### A2. Per turn
| # | file:line | SQL | notes |
|---|---|---|---|
| A2.1 | `loop.py:397` `journal.request_turn` `journal.py:190-194` | RPC insert + `rpc_responses` poll | returns `turn`, `campaign_uuid` |
| A2.2 | `loop.py:701` `DS.checkpoint` -> `launcher/diplo_stream.py:61 journal.log_diplomacy` | RPC insert | write only |

### A3. Per campaign
| # | file:line | SQL | tables | volume | timing |
|---|---|---|---|---|---|
| A3.1 | `advisor/session.py:581` -> `loop.py:775-785 stream_watermark` | `SELECT COALESCE(MAX(decision_id),0) FROM decision_points` (new `DecisionStore(readonly)` = 1 connect + `store.py:83-94 _assert_compatible` `SELECT v FROM meta WHERE k='schema_version'`) | `decisions`, `meta` | 1 | ms |
| A3.2 | `session.py:682-694` `_verify_streams_bg` -> `loop.py:788-822 verify_streams` | `SELECT COUNT(*) FROM turn_open WHERE campaign_id=?`, `.. FROM decision_points WHERE decision_id>?`, `action_offers`, `action_taken` (11 queries) | -- | **never executes**: `?` placeholders raise `psycopg.ProgrammingError: the query has 0 placeholders but 1 parameters were passed` (measured); `session.py:687-688` stores `{"error": ...}` in every campaign entry | -- |
| A3.3 | `loop.py:199 _verify_action_catalogues` -> `features_db.py:189-205 verify_hero_action_mappings` -> `:143-159 agent_action_keys` -> `:123-125 label` | `SELECT key FROM loc WHERE key LIKE %s` (leading-wildcard `'agent_actions_localised_action_name_%<suffix>'`), `SELECT text FROM loc WHERE key=%s` | `reference.loc` (242k rows) | 5 actions | measured 271 ms |
| A3.4 | `loop.py:200-201` `IM.InterruptRanker()` | disk only (`models/interrupt/model.cbm`, `meta.json`) | -- | -- | -- |
| A3.5 | `session.py:560-566 _preload_models` -> `M.Ranker()` `model.py:168-184`; `P.Policy()` -> `policy.py:92-96` -> `greedy_rank.Ranker()` -> `greedy_net.load` `greedy_net.py:65-81` (`torch.load` 15.3 MB) | disk only | -- | per campaign, in a thread; `session.py:593` logs the join wait (logged: `model preload: 0.0s still to wait`) | -- |
| A3.6 | `session.py:461 _width_counts` (only `--width`) | `SELECT campaign_map, faction, n FROM start_counts` (`pg_schema.py:253-257`, correlated `COUNT(*)` over `decisions` per campaign) | `campaigns`, `decisions` | 129 rows | measured 48 ms |
| A3.7 | `session.py:473 _start_gain_stats` (only `--ucb`) -> `ucb_stats.py:44-51 window_rewards` | `SELECT campaign_map, faction, settlements_gained + levels_gained FROM (SELECT .. FROM campaign_gains ORDER BY first_ts DESC LIMIT %s) w` with `window = common.LOOKBACK_CAMPAIGNS = 1000` (`common.py:144`, `ucb_stats.py:9`) | `campaign_gains` view = `GROUP BY campaign_id` over all 206k `decisions` (`pg_schema.py:259-275`) | 1000 rows | measured 68 ms |
| A3.8 | `session.py:146` `UCB.load_adjustments()` `ucb_stats.py:16-27` | file read `D:\twdata\rules\ucb_adjust.json` (falls back to repo `rules/ucb_adjust.json`) | -- | -- | -- |
| A3.9 | `session.py:179 journal.log_ucb_pick` | RPC insert (write) | -- | -- | -- |
| A3.10 | `session.py:612` and `:702 _checkpoint_trial` -> `_trial_row` `:906-951` -> `_measure` `:831-862` | per campaign in `stretch`: `_resolve_uuid` `:768-779` (`SELECT campaign_id, MIN(ts) FROM decision_points GROUP BY campaign_id HAVING MIN(ts) BETWEEN %s AND %s` -- full `decisions` group-by, only when `campaign_uuid` missing; measured 58 ms); `_campaign_growth` `:785-802` (`campaign_growth.py:8-34 TRAJECTORY_SQL_ONE`, measured 16 ms); `_campaign_timing` `:805-828` (2 queries on `decision_points`.timings jsonb and `action_taken`.timing jsonb joined to `decision_points`; measured 2 ms + 5 ms) | `decisions`, `campaigns`, `taken`, `actions`, `blobs` (views) | `stretch` = every campaign since the last retrain (up to `--retrain-every`, or the whole session if 0) -> 3 queries x len(stretch), twice per campaign (O(n^2) per stretch). Connection via `session.py:733 _db` opened per `_measure` and closed `:857-861` | also re-parses `loop_report.jsonl` in full each time the mtime changed (`session.py:740-765 _loop_turns`) |
| A3.11 | `session.py:958,966 _write_trial` -> `metrics_db.py:26-39 write_trial` | `INSERT .. ON CONFLICT` into `metrics.trials` (write, own connection with `search_path=metrics, public`, DDL executed on every non-readonly connect `metrics_db.py:21-25`) | `metrics.trials` | 1 | -- |
| A3.12 | `session.py:677 _postmortem` -> `_ending_evidence` `:202-227` | `SELECT campaign_id FROM decision_points ORDER BY decision_id DESC LIMIT 1` (only when the entry has no uuid); `SELECT turn, settlements, income, power_rank FROM turn_open WHERE campaign_id=%s ORDER BY turn DESC LIMIT 6`; `SELECT turn, kind, chosen, confirmed FROM interrupt_decisions WHERE campaign_id=%s AND kind IN ('pre_battle','battle_results','occupation') ORDER BY interrupt_id DESC LIMIT 6` | `decisions`, `campaigns`, `interrupts` (via views; `interrupt_decisions` view joins 3 blobs but they are not selected) | 6 + 6 rows | ms; bare `except Exception` -> `evidence_error` |
| A3.13 | `session.py:317 journal.log_postmortem` | RPC insert (write) | -- | -- | -- |

### A4. Session start
| # | file:line | SQL | notes |
|---|---|---|---|
| A4.1 | `session.py:585 _reconcile_ledger` `:991-1008` | `SELECT campaign_key FROM campaigns` (4,827 rows, 5 ms) then `metrics_db.prune_unmatched` `metrics_db.py:42-70` (`SELECT trial,ts,first_ts,snapshots,payload FROM metrics.trials`; **writes** `INSERT INTO trials_archive`, `DELETE FROM trials`) | once per session (first campaign) |
| A4.2 | `session.py:438 _require_models` `:351-378` | disk only: `M.Ranker()`, `IM.InterruptRanker()`, `GGR.Ranker()` | all three models loaded a second time per campaign (A3.4, A3.5) |
| A4.3 | `session.py:931 _feature_version` `:1071-1085` | file: `models/global/meta.json` (`num`+`cat` sha1) | per trial row |
| A4.4 | `session.py:971 backfill_trials` -> `metrics_db.trial_ids` | `SELECT trial FROM metrics.trials` | `--backfill-trials` only |

### A5. Training time (child processes spawned by `session.py:326-348 _train_child`, or CLI)

**model.py (CatBoost main ranker)** -- `model.py:45-108 gather`:
| # | file:line | SQL / helper | tables, columns | blobs decoded | volume | caching |
|---|---|---|---|---|---|---|
| A5.1 | `model.py:46` `common.run_dbs` `common.py:198-206` | `SELECT to_regclass('public.decisions')` | -- | -- | 1 | none; returns exactly one pseudo "run db" path when the table exists |
| A5.2 | `model.py:54` `DecisionStore(readonly)` -> `store.py:71-72` | `SELECT v FROM meta WHERE k='schema_version'` | `meta` | -- | 1 | -- |
| A5.3 | `model.py:60` `s.snapshot_read()` `store.py:20-41` | `BEGIN ISOLATION LEVEL REPEATABLE READ` .. `COMMIT` | -- | -- | -- | required: `taken_rows` uses a **server-side named cursor** (`store.py:652-653`), which errors outside a transaction (`DECLARE CURSOR can only be used in transaction blocks`, measured) |
| A5.4 | `model.py:61` `s.target_series()` `store.py:767-777` | `SELECT campaign_id,turn,income,settlements,allies,vassals,power_rank,lord_level FROM turn_open` | `turn_open` view (`turn_bounds` = `GROUP BY campaign_id, turn` over all `decisions`, joined back to `decisions`, `campaigns`) | none (scalar columns of `decisions`) | whole corpus: 27,772 rows / 4,749 campaigns, measured 0.10-0.17 s | none |
| A5.5 | `model.py:62` `MEM.prebattle_attributions(s.con)` `memory.py:227-273` | `_PB_ATTRIB_SQL`: `interrupts i LEFT JOIN blobs bp (panel) JOIN LATERAL (latest taken t2 with t2.campaign_id=i.campaign_id AND t2.ts<=i.ts, refusal filter) JOIN actions a JOIN decisions d2 LEFT JOIN blobs bw (world) WHERE i.kind='pre_battle' AND i.counted=1 AND a.action_type IN ('attack_army','attack_settlement') AND i.ts - t.ts <= 120` plus two jsonb subselects on `bw.z` (`regions[].region/province`, `hostiles[].cqi/province`) and `a.params::jsonb->>'target_cqi'` | `interrupts`(kind,counted,ts,campaign_id,chosen,panel_blob), `taken`(campaign_id,ts,refusal,action_id,decision_id), `actions`(action_type,action_key,params), `decisions`(world_blob), `blobs` | **panel_blob** (json.loads in Python, `memory.py:260-263`), **world_blob** decoded **in SQL** (`::jsonb`) per attribution, `actions.params` (json.loads `:264-267`) | whole corpus, not windowed: 14,272 attributions, measured 2.54 s (plan: seq scan `interrupts` 19.6k est rows, index `ix_taken_agg` lateral) | dict in memory for the gather |
| A5.6 | `model.py:63` `s.window_floor(window)` `store.py:532-540` | `SELECT MIN(first_decision_id) FROM (SELECT first_decision_id FROM campaigns WHERE first_decision_id IS NOT NULL ORDER BY first_decision_id DESC LIMIT %s) w` | `campaigns` | -- | 1 (measured floor = 144299 for window 1000) | -- |
| A5.7 | `model.py:65` `s.taken_rows(min_decision=floor)` `store.py:651-703` | streaming cursor (`itersize=50`): `taken t JOIN decisions d JOIN campaigns c LEFT JOIN actions a LEFT JOIN blobs bc (campaign) LEFT JOIN blobs bw (world) LEFT JOIN entities e ON (t.decision_id, t.entity_seq) LEFT JOIN blobs be (taken entity) LEFT JOIN LATERAL (array_agg of every province entity's features blob for the decision) WHERE refusal filter AND t.decision_id >= floor ORDER BY t.decision_id` | `taken`(decision_id,counted,entity_seq,action_id,refusal), `decisions`(turn,campaign_id,campaign_blob,world_blob), `campaigns`(campaign_key), `actions`(context_kind,context_id,action_type,action_key,params), `entities`(context_kind='province' rows + the taken entity), `blobs` | **campaign_blob**, **world_blob**, **taken entity features_blob**, **every province features_blob** (`json.loads` `store.py:681-696`); the taken action's `params` | window: 62,267 eligible taken rows (of 205,385 corpus-wide); measured 0.1 ms/row DB side -> ~9 s and ~2.1 GB of JSON text for the window | none (rows are consumed and dropped; `full` keeps only the 371-column feature dict) |
| A5.8 | per row `model.py:66-101`: `F.stamp_prev_actions/stamp_action_counts` (`features.py:48-61`), `MEM.CampaignMemory` replay (`memory.py:54-175`), `decision_deltas`/`target` (`base_model.py:208-226`), `F.decision_rows(rec)` (`features.py:1082-1087`) | reference lookups as in A1.7 | -- | -- | measured 2.9 ms/decision end-to-end in `gather(window=25)` (1,111 decisions, 3.2 s) -> ~3 min extrapolated for the 1000-campaign window; logged: catboost retrain wall 116 s of which `fit_seconds` 53.6 (2026-09-03 16:01:05 -> 16:03:01) | -- |

**interrupt_model.py** -- `interrupt_model.py:134-183 gather`:
| # | file:line | SQL / helper | tables | blobs decoded | volume |
|---|---|---|---|---|---|
| A5.9 | `:137` `run_dbs`, `:139` `DecisionStore` | as A5.1/A5.2 | | | |
| A5.10 | `:144` `target_series()` | as A5.4 | | | whole corpus |
| A5.11 | `:145` `window_floor`, `:146` `window_keys` `store.py:542-547` (`SELECT campaign_key FROM campaigns WHERE first_decision_id IS NOT NULL ORDER BY first_decision_id DESC LIMIT %s`) | `campaigns` | | 1000 keys |
| A5.12 | `:148` `s.action_sequence(min_decision=floor)` `store.py:726-737` | `SELECT c.campaign_key,t.ts,a.action_type FROM taken t JOIN decisions d JOIN campaigns c LEFT JOIN actions a WHERE a.action_type != 'noop' AND refusal filter AND t.decision_id >= %s ORDER BY t.decision_id` | `taken`,`decisions`,`campaigns`,`actions` | none | ~60k rows (window) |
| A5.13 | `:150` `s.campaign_snapshots(min_decision=floor)` `store.py:705-724` | `SELECT c.campaign_key,d.ts,bc.z,bw.z FROM decisions d JOIN campaigns c LEFT JOIN blobs bc LEFT JOIN blobs bw WHERE d.decision_id >= %s ORDER BY d.decision_id` (non-streaming `execute` + loop, all rows materialised) | `decisions`,`campaigns`,`blobs` | **campaign_blob** and **world_blob** of every decision in the window (62,647 decisions x ~13 KB = ~820 MB JSON) | measured 0.22 ms/row -> ~14 s |
| A5.14 | `:152` `s.interrupt_rows()` `store.py:739-765` | `SELECT i.interrupt_id,i.ts,c.campaign_key,i.turn,i.kind,i.options_json,i.chosen,i.executed,i.confirmed,i.counted,i.refusal,bc.z,bw.z,bp.z FROM interrupts i LEFT JOIN campaigns c LEFT JOIN blobs bc LEFT JOIN blobs bw LEFT JOIN blobs bp ORDER BY i.interrupt_id` -- **no window in SQL**; filtered in Python by `window_keys` (`:155`) | `interrupts` (all 43,392 rows), `blobs` | interrupt **campaign_blob** (43,392), **world_blob** (43,386), **panel_blob** (35,533), `options_json` -- all json.loads'd; `r["campaign"]` and `r["world"]` are then **never read** by `gather` (it uses `base`/`base_world` from A5.13) | measured 5.7 s |
| A5.15 | labels `:160` `decision_deltas(base, series[camp], r.turn)`; rows `:180 _row` | reference: none | | | logged interrupt retrain wall 33 s, `fit_seconds` 13.x, rows 12,212 (2026-09-03 16:03:01 -> 16:03:35); current model meta: rows 11,477, 971 campaigns |

**mapgraph greedy GNN** -- `advisor/mapgraph/greedy_train.py:196-222 train` -> `train.py:106-235 walk`:
| # | file:line | SQL / helper | tables | blobs | volume |
|---|---|---|---|---|---|
| A5.16 | `train.py:115` `run_dbs`; `:123` `DecisionStore`; `:129` `target_series()`; `:131` `max_decision_id()` (`store.py:528-530` `SELECT MAX(decision_id) FROM decisions`), `taken_map()` (`store.py:595-605` `SELECT t.decision_id,a.context_kind,a.context_id,a.action_type,a.action_key,t.counted,t.refusal FROM taken t LEFT JOIN actions a` -- **whole corpus**, 205,385 rows, measured 0.99 s), `window_floor(window)` | `taken`,`actions`,`decisions`,`campaigns` | none | whole corpus |
| A5.17 | `train.py:139-140` `corpus.load(run_key)` `corpus.py:81-99` | disk: `D:\twdata\models\mapgraph_corpus\<fingerprint>\run\manifest.json` + `torch.load(shard, mmap=True)` | -- | -- | 32 shards, 61,977 graphs, 8.8 GB live (+5.4 GB stale, see D) |
| A5.18 | `corpus.py:44-66 fingerprint` | `pg.connect(search_path="reference")`: `SELECT v FROM meta WHERE k='built'`, `SELECT (COUNT buildings)+(COUNT units)+(COUNT agent_actions)` | `reference.meta, buildings, units, agent_actions` | -- | measured 0.07 s; result = sha1 of schema + source files + these counts; `except Exception` -> `"reference|absent"` |
| A5.19 | `train.py:147-151` cache diff (by `taken_hash` of the taken tuple) -> `_shard` `train.py:33-103` per shard job: `DecisionStore` (new connection per worker), `st.labelled_decisions(after=lo-1, before=hi)` `store.py:607-649` (3 queries: `taken JOIN actions` range; `_entities_and_offers` range = **all entities + all offers of the range with their `actions.params`**; `decisions JOIN campaigns JOIN blobs bc, bw` range), `M2.replay_stamps(st, dids)` `memory.py:276-332` (`SELECT DISTINCT campaign_id FROM decisions WHERE decision_id = ANY(%s)`, `prebattle_attributions(con, camps)`, named cursor over `taken JOIN decisions LEFT JOIN actions LEFT JOIN entities LEFT JOIN blobs be` with jsonb extraction of `pending_recruit_keys, x, y, pending_queue` for **every taken row of those campaigns**, `ORDER BY t.decision_id`) | `taken, actions, entities, blobs, decisions, campaigns, interrupts` | **campaign_blob, world_blob, every entity features_blob** (json.loads in `_entities_and_offers`), offers' `params`; `be.z::jsonb` fields in SQL | only decisions missing from the cache (logged 2026-09-03: `rebuilding 5380 graphs over 4 shard(s), 4 worker(s)`, `walk 82.0s -- 5380 graphs built, 57467 reused from cache`; `walk+tensorize 86.8s`; `norm stats 12.0s`; fit stopped by 513 s budget; whole GNN retrain 16:03:35 -> 16:14:16 = 641 s) |
| A5.20 | labels `train.py:217-231` `decision_deltas(campaign, series[camp_id], turn)` using the `campaign` dict cached in the shard (`train.py:71`) | -- | -- | tally in current meta: `taken_missing` 286, `no_actions` 2, `no_label` 0 |

**Optuna tuners (on demand)**: `advisor/optimize_catboost.py:111-130` re-runs `AM.gather()` / `IM.gather()` (A5.1-A5.15); `:48-56 _storage` connects to db `postgres` as admin and `CREATE DATABASE optuna` if absent (write, other DB); `advisor/mapgraph/optimize_greedy.py:95` `T.walk` (A5.16-A5.19), `:46-54` same optuna storage; `optuna_table.py:40-42` reads the `optuna` DB via optuna.

### A6. On-demand tools
| file:line | SQL | notes |
|---|---|---|
| `advisor/replay_options.py:23-35` | `DecisionStore.max_decision_id()`, `read_decision(did)` for the last `--n` (default 200) decisions | replays `O.generate` and diffs against stored offers |
| `advisor/mapgraph/wl.py:137-145` | same shape (last 40) | WL colour check |
| `advisor/mapgraph/eval.py:21-47 _load` | `labelled_decisions()` **unwindowed** (whole corpus: all taken + all entities/offers/blobs) then `target_series()` | reads `models/mapgraph/model.pt` (`eval.py:53`, `S.MODEL_DIR = common.MODEL_MAPGRAPH`) = the retired marwil artefact (meta `schema_version` 7 vs code 11), no schema-hash check |
| `advisor/mapgraph/greedy_rank.py:91-128 _smoke` | `labelled_decisions()` unwindowed inside `snapshot_read` | smoke only |
| `advisor/mapgraph/invariants.py:90` | `C.dense_ids()` | reference only |
| `advisor/mapgraph/catalogue.py:174-181 __main__` | `_load()` | prints counts |
| `advisor/reference/features_db.py:286-291 __main__` | 5 lookups | -- |
| `decisions/gameref.py` | `ensure` (DDL, `:57-63`), `write` (`INSERT/DELETE reference.ref_*`, `:80-156`), `have_subtypes` (`SELECT DISTINCT subtype FROM reference.ref_skill WHERE subtype = ANY(%s)` `:159-169`), `have_faction_tech` (`:172-181`), `region_count` (`:184-193`) | **no caller in the advisor lane files**; the `ref_region/ref_tech*/ref_skill*` tables exist (814 / 5,191 / 10,467 rows) -- called from the recorder side (`decisions_stream.py:125 _sync_reference`, not read here) |
| `bake.py`, `presaves.py`, `arms.py`, `campaigns/splitter.py` | **no DB access** (`bake.py` uses the bus + `presaves` filesystem; `splitter.py` parses `script_log_*` and `events.jsonl`; no module in the tree imports `campaigns`) | |

### A7. rules/*.json consumption
- `rules/ucb_adjust.json` (repo) / `D:\twdata\rules\ucb_adjust.json` (override wins, `ucb_stats.py:16-21`): read by `ucb_stats.py:24-27 load_adjustments` from `session.py:146` on every UCB pick; `adjustments{faction: delta}` added to the score (`ucb_stats.py:76-81`). Both files exist on disk.
- `rules/v1.json`, `rules/probe_gaps.json`: **no consumer in the working tree** (grep over `*.py,*.md,*.json,*.cmd,*.lua`); the `ruleset` arm is `RETIRED` (`arms.py:8`).
- `D:\twdata\rules\reward_weights.json`: read/written only by `advisor_api/queries.py:1071-1114` (not this lane).

---

## B. CONSUMED-KEY MATRIX

Record shape after hydration (`store.py:574-587`, measured on decisions 206945..206645): `{decision_id, turn, campaign_id(=campaigns.campaign_key), campaign{}, world{}, entities[{snapshot_id, context_kind, context_id, state{}, offers[{offer_id, action_type, key, params{}}]}]}`. Live-loop stamps added in memory after hydration: `campaign.act_index, move_index` (`loop.py:511-512`), `prev_action_1..5` (`features.py:48-51`), `action_counts` (`:54-56`), `mem_acts, prebattle_mem, recruit_counts_turn, queue_ages, queue_stall, last_move_turn` (`memory.py:76-84`), `_t_request, _t_received, _collect_ms, _store_ms, _pickup_lag_ms` (`journal.py:182-186`). These stamps are **not** in the DB blobs (blobs are written by the recorder before the advisor stamps them).

Legend for the ORDER column: **O** = list order changes the output; **D** = duplicates change the output; **F** = first match wins; **L** = last wins (dict build); **-** = order/duplicates irrelevant.

### B1. `record.campaign` (campaign_blob) and in-memory stamps
| key path | consumer file:line | becomes | order |
|---|---|---|---|
| `campaign.faction` | `features.py:130-131` `camp_faction`, `camp_race` (`race_of` = 3rd `_` token `:41-45`); `loop.py:420`; `build.py:141` player faction node id, `S.race_index`; `interrupt_model.py:66,88-93`; `options.py:556` (`_hero_subtype_types(campaign.get("faction"))`) | feature / graph node `f:<faction>` / filter | - |
| `campaign.turn` | `features.py:133` `camp_turn`; `:404` (turns since moved); `loop.py:424` `campaign_turn`; `build.py:184,496,607`; `base_model.py:209` (label turn comes from `rec["turn"]`, not this) | feature / g_ctx[0] | - |
| `campaign.income, settlements, lord_level, allies, vassals, power_rank, treasury, armies` | `features.py:136-142`; `:1005,1040,1045` (treasury, income for ratios); `loop.py:420-424`; `build.py:182-184,495-504` g_ctx; `base_model.py:213` label baseline (`GAIN_PARTS` settlements, lord_level, allies, vassals) | features `camp_*`, `opt_cost_vs_treasury`, `opt_upkeep_after`, `lord_upkeep_vs_income`; g_ctx; **training label baseline** | - |
| `campaign.is_researching` | `features.py:143` `camp_is_researching` | feature | - |
| `campaign.campaign_map` | `features.py:144` `camp_map`; `build.py:185-189` faction node `campaign_map` (+1/-1 only for `wh3_main_combi`/`wh3_main_chaos`) | feature / node scalar | - |
| `campaign.game_version` | `features.py:132` `camp_game_version` (not in MODEL_COLUMNS) | -- | - |
| `campaign.resources{}` | `features.py:74-79` `res_<k>`, `camp_n_resources` (none in MODEL_COLUMNS) | -- | - |
| `campaign.ll_wounded` | `loop.py:423,520-531` (ends the turn; growth verdict `:116-118`) | control flow | - |
| `campaign.campaign_uuid` | `loop.py:716-717`; `session.py:725-730` | campaign_uuid in reports | - |
| `campaign.hero_type_counts{agent_type:int}` | `options.py:557` `type_fielded` param | offer param -> `rh_type_fielded` (`features.py:734`) | - |
| `campaign.anc_pool[]{key,name,index}` (from campaign **entity state**, merged at `options.py:689-692`) | `options.py:128,187,311-324` item offers (`key or name`, `index` -> `pool_index`); `build.py:232-235` `holds_free` | offers `items`; graph edge | D (Counter of free copies `:303-309`) / O (offer order) |
| `campaign.equipped_all[]{key,name}` | `options.py:130,303-309`; `build.py:236-239` | free-copy count | D |
| `campaign.lord_pools{subtype:{n, traits[], agents[], can[], bg_skills[], cqis[], units[], ranks[], subtypes[]}}` | `options.py:524,552-586` parallel arrays indexed by `i` | `recruit_lord`/`recruit_hero` offers `%s@%d`, params `candidate_index, n_candidates, traits, trait(=traits[0]), n_traits, bg_skill, cqi, unit_key, cand_rank, cand_subtype` | **O** (index i identifies the candidate; `traits[0]` = `opt_trait`) |
| `campaign.tech[]{key, researched, can_research, cost}`, `campaign.current_research`, `campaign.research_points` | `options.py:593-604` research offers; `build.py:218-229` tech edges (`researched`/`available`/`locked`, `researching`; `current_research` may be str or `{key}`) | offers, params `cost, points_available` -> `opt_research_turns` (`features.py:963-965`); graph edges to `tech:<key>` | O (offer order) |
| `campaign.rites[]{key, can_perform, invalid_reason, index}` | `options.py:605-612` | `rites` offers (key or `rite_index_<i>`), `opt_rite_index` | O |
| `campaign.missions[]`, `effect_bundles[]`, `read_failures{}`, `defeated`, `difficulty`, `faction_cqi`, `leader`, `presave_radius`, `selector` | no reader in this lane (recorder writes `difficulty, presave_radius, selector, leader` into `campaigns` columns `store.py:217-221`) | -- | -- |
| stamp `campaign.act_index` | `features.py:134` `camp_act_index` (MODEL_COLUMNS) | feature | -- (see F: train/serve skew) |
| stamp `campaign.move_index` | `features.py:135` (not in MODEL_COLUMNS) | -- | -- |
| stamp `campaign.prev_action_N` | `features.py:126-127` `camp_prev_action_1..5` (MODEL_COLUMNS) | feature | -- |
| stamp `campaign.action_counts{}` | `features.py:114-119` `camp_taken_total`, `camp_taken_<type>` | feature | -- |
| stamp `campaign.mem_acts`, `prebattle_mem[]{x,y,zone,choice,result,casualties,cqi,act,turn}` | `memory.py:191-224 prebattle_option_feats` (iterates `reversed(mem)` -> most recent first); `build.py:675-695` pb_* action scalars | `opt_last_prebattle_*`, `opt_actions_since_prebattle_*`, `opt_last_prebattle_same_lord` | **O** (recency) |
| stamp `campaign.recruit_counts_turn{cqi:n}`, `queue_ages{cqi:[[key,age]]}`, `queue_stall{cqi:[flags]}`, `last_move_turn{cqi:turn}` | `features.py:388-425 lord_memory_block`; `:886-891` (`queue_stall[cqi][queue_index]`); `build.py:589-613 _lord_memory_row`, `:700-711` | `lord_recruits_this_turn`, `lord_queue_queued_this_turn`, `lord_queue_turns_to_clear`, `lord_queue_stalled_units`, `lord_turns_since_moved`, `prov_pending_recruits_others`, `prov_recruits_this_turn_others`, `opt_cancel_stalled`; lord node `recruits_this_turn, queue_age_max, queue_new_this_turn, queue_stalled, turns_since_moved`; action `cancel_stalled` | **O** (`queue_stall` flags are positional = `pending_queue` index) |

### B2. `record.world` (world_blob)
| key path | consumer file:line | becomes | order |
|---|---|---|---|
| `world.armies[]` (`cqi, has_army, units, hp, x, y, agent_type, stance, subtype, rank, ap_pct, acted, is_leader, in_own_territory, region, province, skill_points?, pending_recruits?`) | `features.py:85-95` hero roster counts (`agent_type`, `has_army`); `:98-111` own agents; `:145-151` `camp_armies, camp_army_units, camp_characters, camp_lords, camp_heroes`; `:215-220` `lord_in_own_territory` (**F** by `cqi`); `:287,313-317` positioning; `:410-424` province co-lords (`cqi, has_army, province`); `:456` friend channel (`has_army, x, y, units, stance, hp`); `:571-582` own garrison (`cqi, region, units, hp` joined to `citizenry`); `:720-725` hero target (**F** by cqi); `:762-765,824-836` reinforcement; `options.py:383-388,420-422,513-519` (own_armies targets, embedded check, `_own_lords`); `build.py:146-151,266-267,367-408` own char nodes (row = army row overlaid with entity state non-None values `:372-373`), `:512-555 _wire_char`; `interrupt_model.py:60-62,67-76` lord by `ally_cqi`/`enemy_cqi` (**F**); `memory.py:` none | features, graph nodes `c:<cqi>`, offers | **F** on cqi matches; **O** for tie-breaking in `near_block` (stable sort `features.py:483-485`; self-removal only drops the first zero-distance pair `:486-487`); **L** in `build.py:146` `_armies_by_cqi`; D counts double |
| `world.hostiles[]` (`cqi, kind {army, neutral_army, settlement, hero, neutral_hero}, faction, units, hp, x, y, dist, province, region, stance, is_armed_citizenry, visible, agent_type, subtype, rank?`) | `features.py:147,152-154` counts; `:288-289,314-319` positioning; `:457-459` enemy/neutral/enemysett channels; `:555-568 _enemy_garrison` (**F** settlement by `region`, then citizenry at same `x,y`); `:598-622,742-753` target units/hp/is_garrison (**F** by cqi); `:633-645 _target_army_dist` (min `dist`); `:807-822,838-851` reinforcement (excludes `is_armed_citizenry`, `visible False`); `memory.py:44-51 target_zone` (**F** cqi -> `province`); `options.py:117-119,337-341,370-381` targets (`_is_citizenry` raises `CollectError` if `is_armed_citizenry` missing `:357-362`); `build.py:170-171` factions, `:410-431` hostile char nodes (`MOBILE_KINDS` only, skips `visible False`, skips cqi already in citizenry/own), `:433-451` enemy settlement nodes (`region, x, y, faction`), `:459-471` garrisons edge by exact `(x,y)` match; `interrupt_model.py:61` enemy by cqi | features `near_enemy_*`, `near_neutral_*`, `near_enemysett_*`, `opt_target_*`, `opt_tgt_*`, `agg_enemy_*`, `prov_enemy_known`, `prov_neutral_known`, `camp_enemy_*`; graph nodes/edges; `attack_army`/`attack_settlement`/hero-action offers | **F** cqi/region matches; **O** stable-sort ties |
| `world.settlements[]` (`region, x, y, units, capital`) | `features.py:460` ownsett channel; `:587` own regions; `:983-988 _locus` (**F** by region; for the campaign context: first `capital`, else `settlements[0]` -> **O**); `options.py:111,120,179-185,337-338` garrison offers; `build.py:317-336` `s:<region>` own nodes (`garrison_units` from citizenry army) | features `near_ownsett_*`, locus of province/campaign rows; offers `garrison`; graph | **O** for campaign locus |
| `world.regions[]` (`region, province, owner, x, y, capital, abandoned, adjacent[]`) | `memory.py:39-43` (**F** region -> province); `options.py:110` known regions; `build.py:166-167,246-283` region nodes (**L** by key), `adj` edges (only `j>i` `:281-283`), `prov_of_region`; `interrupt_model.py:64-65,87-93` (**F** by region; province settlement count by `owner==faction`) | zone for prebattle memory; graph `r:<region>`, `p:<province>`, `owns_region`, `adj`, `in_prov` | **L**/**F** |
| `world.relations[]` (`faction, standing, excluded, at_war, allied, trade, their_vassal, our_master, def_ally, nap, mil_ally, mil_access`) | `options.py:613,632-683` diplomacy offers (sorted by `-abs(standing)`, stable -> **O** on ties; `excluded` dropped; `None`/empty handled `:633-640`); `build.py:152-153` (**L** by faction), `:168,173-207` faction nodes + `dip_*` edges | offers `diplomacy` with params `faction, terms[], gift, standing, at_war, allied, trade, their_vassal`; graph | O/L |
| `world.war_graph[]{faction, at_war_with[]}` | `build.py:209-216` `dip_war` edges | graph | - |
| `world.ruins[]{region, x, y}` | `options.py:121,170-175,342-343,368-369` colonize / hero targets; `build.py:` none | offers `colonize`, hero `ruins` targets | O |
| `world.citizenry[]` (cqi list) | `features.py:575-576`; `options.py:383,517`; `build.py:145-151,369,415` | garrison detection; excluded from lord counts/nodes | - |
| `world.stationed{region: cqi}` | `options.py:127,177-183` (`settlement_occupied` gate) | offer availability | - |
| `world.enemy_agents[]{cqi, faction, at_war, x, y, agent_type?}` | `features.py:101-110` `camp_enemy_agents_*`; `options.py:389-393` hero `enemy_agents` targets | features (none in MODEL_COLUMNS); offers | O |
| `world.diplo_hostile_rows`, `diplo_schema`, `diplo_unseen` | no reader in this lane | -- | -- |

### B3. `entities[].state` (entity features_blob) by `context_kind`
Common: `entities[].context_kind`, `context_id` -> `features.py:993-994` (province map by `context_id`), `:1084`; `loop.py:721-741`; `options.py:693-706` (unknown kind raises); `build.py:156-164,482-484`; `store.py:681-696`; `replay_options.py:38-47`; `wl.py:45-50`. **Entity order = offer order = row order = action-node order** (`features.py:1082-1087`, `build.py:482-488`, `wl.py:59-61` asserts equality).

| kind.key | consumer file:line | becomes | order |
|---|---|---|---|
| lord/hero `x, y` | `features.py:180,980`; `memory.py:90-94` (move detection); `build.py:384-386`; `options.py:79-83,397-401,420` | `lord_x, lord_y`, locus, `last_move_turn`, node scalars | - |
| lord/hero `cqi` | `features.py:216,390,1062`; `memory.py:` (uses context_id); `build.py:` (uses `world.armies` cqi) | joins to `world.armies` | - |
| lord/hero `rank, skill_points, units, pending_recruits, ap_pct, garrisoned, besieging, acted, stance, subtype, is_leader, hp` | `features.py:173-183 lord_block`; `:1041-1042,1063`; `build.py:377-391,401` (`S.subtype_index` -> reference dense ids), `_wire_char :524-527`; `options.py:151,154-156,176,467-476` | `lord_*` features, `opt_self_units/hp`, node scalars/embeddings, gates (`no_attack`, `marching`, `recruiting`, `garrisoned`) | - |
| lord/hero `move_tiles[]{x, y, sample_index, reach_rays, reach_max}` | `features.py:165-167,183` `lord_reach_max` (max); `options.py:71-89` move offers (`xy:x,y`) and `leave_garrison` (nearest tile **O** on ties) | offers `move`, `lord_reach_max` | O |
| lord `unit_cards[]{key, strength_pct, category, xp}` | `features.py:189-206` (`key.partition("@")[0]` -> `reference.units.num_men/upkeep_cost`); `build.py:531-535` `in_army` edges (dedup on key) | `lord_effective_men`, `lord_stack_upkeep`, `lord_upkeep_vs_income`, `opt_upkeep_after`; graph | D (sum counts every card) |
| lord/hero `pending_recruit_keys[]` | `features.py:350-353` `lrec_<k>`, `lrec_inprogress`; `memory.py:95-104` queue ages (merged by key); `build.py:528-529` `queued` edges, `:602-605`; `memory.py:295-322` (replay via SQL jsonb) | features/graph | **O** (`queue_ages` list order), D |
| lord `pending_queue[]{key, turns_left}` | `options.py:147-150` `cancel_recruit` offers `key@<index>` (params `queue_index, turns_left`); `features.py:371-373` `lord_queue_turns_to_clear` (max `turns_left`); `memory.py:105-123` stall flags by position; `build.py:` via memory | offers, `opt_cancel_turns_left`, `lord_queue_stalled_units`, `opt_cancel_stalled` | **O** (queue index is the executor's argument and the stall-flag index) |
| lord `recruitable[]{key, state, cost, disabled}` | `options.py:141-146` (`state=="active"` gate); `features.py:354-357` `lord_queue_on_hold`; `build.py:603-605` | offers `recruit_unit` (params `unit, cost, recruitment_disabled`) | O |
| lord `stances[]{key, active, can_activate, can_afford}` | `options.py:134-140` | offers `stance` (param `active` -> `opt_is_active`) | O |
| lord/hero `skills[]{key, status, level, total_levels, tier}` | `options.py:54-68` (`active` -> `has_pts` gate; params `level,total_levels,tier,at_max`); `:479` hero active skills -> `actions_for_skills`; `build.py:539-551` `skill_*` edges + `skill_child_of` via `reference.skill_links` | offers `skills`, `opt_skill_level/total_levels/tier`; graph | O |
| hero `hidden_skills[]` | `options.py:479`; `build.py:536-538` `innate` edges | granted actions | - |
| hero `is_agent, can_embed, agent_type` | `options.py:467-469,441-447` | hero_action gates, `agent_type` param -> `ha_agent_type` | - |
| lord/hero `reach_chars{cqi:bool}`, `reach_setts{region:bool}` | `options.py:131-132,158,165,171,181,438`; `build.py:403-408,453-457` `can_reach` edges | availability gates; graph | - |
| lord/hero `equipped[]{key, name, index}` | `options.py:129,325-327` `item_unequip` offers; `build.py:552-555` `wears` edges | offers/graph | O |
| lord `horde_slots[]{available, empty, key, slot_id, slot_index}` | `options.py:92-105` (dedup empty slots by key **F**) | offers `horde_building` `slot_id@key` (key semantics after `@`, `features.py:862-863`) | O/F |
| lord `merc_pools{atype:[{key, avail, cost, can?}]}` | `options.py:33-46` (sorted atype) | offers `raise_dead/recruit_ror/...` (params `unit, cost, pool_avail`) | O within pool |
| lord/hero `region` | `features.py:408-409,998,1002`; `options.py:35` (`at_sea`); `build.py:` via armies row | province lookup; `own_garrison_units_here` | - |
| lord `ap_remaining` (via `o["_state"]`) | `options.py:808-832` `no_action_points` gate for `COSTS_MOVEMENT` | drop reason | - |
| lord/hero others (`armory, authority, background_skill, effect_bundles, force_effect_bundles, hidden_skill_states, loyalty, resurrection_turns, subterfuge, trait_progress, traits, upkeep, wounded, xp, xp_next_level, zeal, ap_per_turn`) | no reader in this lane | -- | -- |
| province `settlement_present, province, complete_owner, max_slots, free_slots, can_set_edict, selected_edict, active_edict, public_order, buildings, is_capital, settlement_level, gross_income, income, growth_per_turn, locked_slots[], corruption{wh3_main_corruption_*}` | `features.py:232-261,270-276 province_block`; `:428-439 carried_province_block`; `:1021` capital lookup (**F**); `build.py:157-160` (**L** by `state.region`), `:254-258,268-276,285-315` region/province node scalars, `has_edict`; `options.py:541-551` edict offers | `prov_*`, `corr_*`, `own_*`; graph | L/F |
| province `region` | `features.py:993` (map key is `context_id`), `:409`; `build.py:159`; `options.py:` uses `context_id` as region | map keys | - |
| province `built{slot: key}`, `building_now{slot: key or {key}}` | `features.py:326-346` `pbld_<key>`, `pbldnow_<key>`, `pbld_count`, `pbld_inprogress`; `:433-434`; `build.py:347-363` slot nodes | features (none in MODEL_COLUMNS except via counts) / graph | - |
| province `buildable[]{slot_index, key, active, empty, can_upgrade, cost, upkeep, level, can_afford_resources}` | `options.py:528-540` (dedup `(slot_index,key)` **F**) | offers `building` (params `slot_index, is_upgrade, ...`) -> `opt_slot_index`, `opt_cost` (from reference, not params) | O/F |
| province `edicts[]` | `options.py:526,543-551` | offers `edict` | O |
| province `slot_states[]{index, key, queued_key, health, max_health, ruined, upgrading, dismantling, can_repair, damaged, repairing, repair_cost, can_dismantle, empty, refund}` | `options.py:488-510` | offers `building_repair/dismantle` `region@index` | O |
| province `development_points, has_port, has_walls, plague_bundles, effect_bundles` | no reader | -- | -- |
| campaign entity state | merged over `record.campaign` (`options.py:689-692`, state wins) -> B1 rows `anc_pool, equipped_all, lord_pools, tech, rites, current_research, research_points, hero_type_counts`; `build.py:163-164 camp_state` (`tech, current_research, anc_pool, equipped_all`) | -- | -- |

### B4. `entities[].offers[]` and `params` (live: generated in memory; training: `actions.action_type/action_key/params`)
| key path | consumer file:line | becomes | order |
|---|---|---|---|
| `offers[].action_type`, `key` | `features.py:867-878,895,949-975` (`_option_key` `:859-864`: key before `@`, `horde_building` after `@`, pointer-keyed types none; `recruit_unit` queue = after `@`, `"global"` doubles `create_time`); `model.py:188-208`; `policy.py:163-167`; `strategies.py:27-30,46-47,51-53`; `build.py:657-737` (`_CAT_OF_TYPE`, `S.atype_index`, `S.term_index` from diplomacy key suffix, stance key -> `S.stance_index`), `:795-813 _target_of`; `greedy_rank.py:19-21,61,66`; `train.py:81-84`; `loop.py:573-575,577,590,608,615,631`; `options.py:820-861` gate | `opt_type`, `optk_<type>`, `opt_recruit_queue`, `opt_recruit_turns_effective`, `opt_db_*` (reference by key); graph action node `a:<ck>:<cid>:<key>:<n>` and `act_on`; `taken` identity `(ck,cid,at,key)` | **O** (row order; `max()` in `strategies.py:47` and `greedy_rank.py:86` return the **first** maximal -> ties broken by offer order); **D** (`train.py:81-84`: two offers with the same identity -> `sum(mask)!=1` -> `taken_missing`; `greedy_rank.py:61` `dict(zip)` last wins) |
| `params.x, y` | `features.py:897-914,799-803`; `build.py:665-667,752-761`; `memory.py:139-140,200-204`; `options.py:79-83` | `opt_target_dist/dir_*`, `opt_delta_*`, `opt_tgt_*`, action node x/y, `near_target` edges, prebattle memory loc | - |
| `params.target_cqi` | `features.py:601,613,719,747,805`; `memory.py:44-51`; `build.py:801-802` | garrison/target lookups, `act_target` | - |
| `params.target_faction`, `params.faction` | `features.py:653-654,667,703-704,933-934`; `build.py:763-765` `act_subject` | `dip_target*`, `ha_target_faction/race`, `opt_target_faction/race` | - |
| `params.terms[]`, `gift`, `standing`, `at_war`, `allied`, `trade`, `their_vassal` | `features.py:648-677` | `dip_*` (`terms[0]` when single -> **O**) | O |
| `params.ability, agent_type, target_kind, target_own, action, action_key, attribute, chance, ability_category, skill_unlocked, innate, target_on_settlement, target_is_agent` | `features.py:685-726`; `build.py:741-747` (`agent_action:<action_key>`, ability relation) | `ha_*` | - |
| `params.agent_type, type_fielded, n_candidates, cand_rank, trait, n_traits, candidate_index` | `features.py:729-739,935-939` | `rh_*`, `opt_trait`, `opt_n_traits`, `opt_cand_rank`, `opt_candidate_index` | - |
| `params.slot_index, slot_id, region, building_key, item_key` | `features.py:938`; `build.py:729-737,767-781,798-800,810-812` | `opt_slot_index`; `act_on` catalogue node; `act_slot`; target settlement/region | - |
| `params.rite_index, active, level, total_levels, tier` | `features.py:940-948` | `opt_rite_index`, `opt_is_active`, `opt_skill_*` | - |
| `params.turns_left, queue_index, queue` | `features.py:884-895`; `build.py:703-708` | `opt_cancel_turns_left`, `opt_cancel_stalled`, `opt_recruit_queue` | - |
| `params.cost, repair_cost, pool_avail, points_available` | `build.py:668-673`; `features.py:964` (`points_available`) | action node `cost`, `pool_avail`; `opt_research_turns` | - |
| `params.unit, cost, recruitment_disabled, in_progress, current_research, ritual_key, invalid_reason, province, can_set, is_selected, sample_index, reach_rays, reach_max, pool_index, item_name, equipped_index, damaged, repairing, refund, queued_key, health, max_health, ruined, upgrading, dismantling, bg_skill, cqi, unit_key, is_agent, cand_subtype, traits, target_agent, slot_empty` | generated (`options.py`) but **no feature/graph reader** in this lane (executor lane may read them) | -- | -- |
| `offers[].offer_id`, `entities[].snapshot_id` | no reader in this lane | -- | -- |

### B5. Interrupt rows (training: `interrupts` columns + panel_blob; live: launcher panel dict)
| key path | consumer file:line | becomes | order |
|---|---|---|---|
| row `screen(kind), chosen, ts, turn, campaign_id, options{opt:{...}}, panel` | `interrupt_model.py:152-167`; `memory.py:153-175` (`kind=="pre_battle"`, `counted`, `ts`, `chosen`, `panel.result.state`, `panel.casualties.text`) | rows/labels; prebattle memory | - |
| `options[opt].dilemma_id, option_id, text, payload[]` (as `meta`) | `interrupt_model.py:101-106` | `isc_dilemma_id`, `isc_option_id`, `isc_option_label`, `isc_payload` (sorted join), `isc_n_payload` | - |
| `options[opt].answer, context, exploit, gnn, score, subtree` | no reader here | -- | -- |
| `panel.ally_cqi, enemy_cqi, region, n_ally_armies, n_enemy_armies` | `interrupt_model.py:57-94` | `isc_lord_*`, `isc_enemy_*`, `isc_siege`, `isc_ally_armies`, `isc_enemy_armies`, `isc_region_ours`, `isc_setts_in_province` | - |
| `panel.result{state}`, `casualties{state, text}` | `interrupt_model.py:108-109`; `memory.py:174-175`, `:271-272` | `isc_fc_result`, `isc_fc_casualties`; prebattle memory result/casualties | - |
| `panel.attitude, attitude_label, race, reliability[], strength_ranks[], settlements, demands[], offers[], treaties[], amount_demanded, amount_offered` | `interrupt_model.py:110-130` | `isc_dip_*` (`reliability[0]`, `strength_ranks[0]`=them, `[1]`=us -> **O**) | O |
| `panel.amounts, armies, dismiss_visible, faction_names, name, outcome, resources, result_flag, rewards, rows, sections, settlement_captured, terms` (present in blobs, measured union) | no reader | -- | -- |
| `record.world` at choose time | `interrupt_model.py:307` | world for `_local_block`, `campaign_block` | - |

### B6. Order/duplicate summary
Order-sensitive: entity/offer order (row order, tie-breaking of `max`, action-node index, `taken` mask), `lord_pools` parallel arrays, `pending_queue` index, `queue_stall` flags, `prebattle_mem` recency, `terms[0]`, `reliability[0]`, `strength_ranks[0..1]`, `settlements[0]` fallback locus, stable-sort ties in `near_block`/`_reinf_feats`/`_diplo_options`. Duplicate-sensitive: `unit_cards` (sums), `pending_recruit_keys` (counts), `anc_pool`/`equipped_all` (Counter), offers with identical `(ck,cid,at,key)` (GNN training drops the decision; `greedy_rank` last-wins), `world.armies` cqi collisions (first-wins in features, last-wins in build).

---

## C. REFERENCE DATA USE

All through `advisor/reference/features_db.py` (module-level singleton connection `_connect :14-19`, `search_path=reference`, reconnects only if `closed`) or `advisor/mapgraph/catalogue.py` (opens/closes a connection per load, `_open :9-24`, refuses to run if `reference.buildings` or `reference.ancillary_effects` is missing).

| file:line | table(s) | lookup key from the record | cadence / cache |
|---|---|---|---|
| `features.py:526-545 _db_features` -> `features_db.py:37-62` | `buildings` (+ `building_chains` by `building_chain`), `tech`, `units`, `skills`, `rituals`, `ancillaries` (+ `ancillary_effects` `:107-120`) | offer `key` (after `_option_key`: `building` key, `research` key, unit key before `@`, `skills` key, `rites` key, `items`/`item_unequip` key); every non-`key`/`id` column becomes `opt_db_<col>` (`:949-955`), first of `create_cost, recruitment_cost, cost_per_round, influence_cost` -> `opt_cost` (`:548,956-958`); `ancillary_effects` -> `opt_db_res_<resource>` for `ITEM_RESOURCES` (`features_db.py:65-80,91-104`) | per offer per decision; `_lookup_cache` per `(table,col,key)` forever, misses cached as `{}`; measured cold 147 ms (first connect) / warm 0.002 ms |
| `features.py:194-197,379-380` | `units` (`num_men`, `upkeep_cost`, `create_time`) | `unit_cards[].key`, `queue_ages` keys | per lord per decision, cached |
| `features.py:1050,1069` | `ancillaries.category` | item offer keys | per decision, cached |
| `options.py:233-255 _build_hero_actions` -> `features_db.py:224-250 agent_action_catalogue` | `agent_abilities`(key,category), `agent_actions`(key,agent,ability,attribute,chance_of_success) | none (catalogue) | at import; `HERO_ACTIONS` = 5 assist_army actions (measured) |
| `options.py:264-274 _hero_subtype_types` -> `features_db.py:259-262 permitted_agent_subtypes` | `agent_permitted_subtypes`(agent,subtype) WHERE faction | `campaign.faction` | per faction, cached; measured 17 ms |
| `options.py:277-296 _hero_action_matrix` -> `features_db.py:208-218 agent_action_rows`, `:253-256 agent_ability_category` | `agent_actions` WHERE key LIKE `%<suffix>`; `agent_abilities` | none | per action, cached |
| `options.py:404-411 _granted_actions` -> `features_db.py:315-324 actions_for_skills` | `skill_actions`(agent_action) WHERE skill | hero `skills[].key` (active) + `hidden_skills[]` | per unseen skill key; `_unlock_cache`; raises `CollectError` on failure (no fallback) |
| `loop.py:165-176` -> `features_db.py:189-205,143-159,123-140` | `loc` (LIKE prefix+`%`+suffix; exact key) | none | per campaign; 271 ms measured |
| `catalogue.py:27-71 _load` | `buildings`(key,building_chain,level), `building_chains`(key,superchain,chain_category), `agent_actions`(key,agent,ability), `units`(key,caste,category,tier), `skill_actions`, `tech_links`(child,parent), `skill_links`(child,parent), `ancillary_effects`(ancillary,effect,value) | graph catalogue keys: building keys (`chain_of`, `next_level_of`), tech keys (`tech_parents_of`), skill keys (`skill_parents_of`), item keys (`item_effects_of`), agent_action key (`ability_of`) | once per process (0.19 s) |
| `catalogue.py:82-98 dense_ids` | `key` of `buildings, building_chains, units, tech, skills, rituals, agent_actions, ancillaries`; `agent_permitted_subtypes.subtype`; `effects_meta.effect` | every catalogue key and character `subtype` in the record -> `S.cat_index/subtype_index` (`schema.py:273-323`; unknown keys hash above the dense range) | once (0.09 s); counts measured: building 5259, chain 1943, unit 2609, tech 2056, skill 5944, ritual 1326, agent_action 176, item 2671, agent_subtype 565, effect 15064 |
| `catalogue.py:101-143 attrs` | `buildings`(create_cost,level,create_time), `units`(recruitment_cost,upkeep_cost,num_men,tier,create_time), `skills`(unlocked_at_rank,is_background_skill), `tech`(tier,research_points_required), `ancillaries`(uniqueness_score,legendary,transferrable), `effects_meta`(positive_good,priority) | catalogue node scalars `build.py:122-131` | once (0.06 s) |
| `corpus.py:53-65` | `meta`(built), counts of `buildings, units, agent_actions` | -- | per training walk (fingerprint) |
| `decisions/gameref.py` | `ref_region, ref_tech, ref_tech_effect, ref_tech_parent, ref_skill, ref_skill_level, ref_skill_effect` | -- | **not read by any advisor-lane file**; writer/reader is the recorder (`decisions_stream.py:125`) |

`reference.meta`: `built` 2026-08-04 (1786170352), `extra_built` 2026-08-29 (1788319259).

---

## D. MODEL ARTIFACTS (`common.py:225-230`, root `D:\twdata\models`)

| dir / file | writer | reader | duplicates DB content? |
|---|---|---|---|
| `global/model.cbm` (645 KB), `global/meta.json` (60 KB: `num`[246], `cat`[65], `pred_lo/hi`, `mae_in_sample`, `fit`, `rows` 61,975, `campaigns` [999 campaign_keys], `short_horizon`, `short_weight`, `target`) | `model.py:145-152` (staging dir + `os.replace`; removes stale `e1.cbm`, `e2.cbm` `:153-156`) | `model.py:168-184 Ranker` (per campaign preload `session.py:562` + `_require_models :356`); `session.py:1071-1085 _feature_version`; `advisor_api/queries.py:3967` (other lane) | `campaigns` list = `campaigns.campaign_key` subset; `rows` = training row count |
| `interrupt/model.cbm` (598 KB), `interrupt/meta.json` (53 KB: `num`[66], `cat`[19], `rows` 11,477, `screens`, `screen_rows`, `campaigns`[971]) | `interrupt_model.py:224-236` | `interrupt_model.py:245-270` (per campaign `loop.py:200`, session gate `session.py:358`, `bake.py:302` with the non-existent cold dir) | `campaigns` list |
| `mapgraph_greedy/model.pt` (15.3 MB: `encoder`, `head`, `meta`), `mapgraph_greedy/meta.json` (55 KB, same `meta`: `backend, schema_version 11, schema_hash, cfg, rows 61,689, campaigns[999], fit{curve}, y_mean, y_sd, n_scalars, node_types, relations, tally`) | `greedy_train.py:166-177 _save` | `greedy_net.py:65-81 load` reads **model.pt only** (schema-hash gate `:71-76`); `meta.json` has no reader in this lane (API only) | `campaigns` list; meta stored twice (inside `.pt` and as `.json`) |
| `mapgraph_corpus/<fingerprint>/run/manifest.json` + `shard_NNNNNNNN.pt` (2000 decisions per shard, `corpus.py:13`) | `corpus.py:102-108 write_shard`, `:116-127 write_manifest` (prunes other fingerprint dirs only) | `corpus.py:81-99 load`, `:110-113 read_shard` (`train.py:139,203`) | **yes**: each record = `(decision_id, campaign_key, campaign dict, turn, drop, tensor graph, counts, taken_hash)` (`train.py:71-86`) -- a tensorised copy of campaign_blob + world_blob + entity blobs + offers for every labelled decision in the window. Measured: manifest 32 shards / 61,977 graphs / watermark 211,678 / 8,776 MB live; **20 stale shard files** (`shard_00000054..73.pt`, 5,438 MB, dated Sep 1) not in the manifest and never pruned; manifest watermark 211,678 > live `MAX(decision_id)` 206,945 (the cache was written 2026-09-04 12:26 against a DB that had more decisions than the one measured now) |
| `mapgraph/model.pt` (14.5 MB, meta `schema_version 7`, backend `mapgraph`, 2,201 campaigns, Aug 28), `mapgraph/meta.json` | no writer in the tree (`mapgraph/train.py` has no `_save`/`train`) | only `mapgraph/eval.py:53` (on demand, no hash check); `corpus.py:12` uses the dir name only | orphan of the retired `marwil_gnn` arm (`arms.py:8`) |
| `mapgraph_interrupt/model.pt` (50.8 MB, Aug 21), `local/{e1.cbm,e2.cbm,meta.json}` (Aug 26) | none | none (grep: no reference in the working tree) | orphans |
| `__cold_start__` | none; `common.MODEL_COLD_START` | `loop.py:162,200` (cold), `bake.py:302`; dir does not exist -> `InterruptRanker.ready=False` -> random | -- |
| `D:\twdata\tmp\catboost\` (`catboost_training.json`, `learn_error.tsv`, ... Aug 30) | CatBoost `train_dir` (`base_model.py:57,122`) although `allow_writing_files=False` `:123`; last write Aug 30 predates `allow_writing_files=False`? UNKNOWN which code wrote them | none | -- |
| `D:\twdata\logs\services\optuna_catboost\`, `optuna_gnn_greedy\` (`trials_*.jsonl`, `best_*.json`), `optuna_gnn\` (`optuna_gnn.db` sqlite + jsonl) | `optimize_catboost.py:149,179,210`, `optimize_greedy.py:22,172,194` | `optuna_table.py` reads the **Postgres** `optuna` DB, not these files | `optuna_gnn/optuna_gnn.db` has no writer in the tree |
| `D:\twdata\metrics` (`common.py:238`) | `metrics_db.DB_PATH` is a **Postgres DSN string** with `search_path=metrics` (`metrics_db.py:9`), not a path; `METRICS_DIR` is imported by `session.py:719` and unused | -- | trials live in `metrics.trials` (152 rows) / `trials_archive` (149) |
| `D:\twdata\runs\human\session_*.json` | `session.py:1124-1129 _write` (whole report rewritten after every turn `:614-619` and campaign) | `session.py:1045-1054 _session_reports` (backfill) | `campaigns[].campaign_uuid, run_dir, outcome, growth, timing` duplicate `campaigns`/`postmortems` rows |
| `<run_dir>/loop_report.jsonl`, `turn_trail.jsonl`, `locomotion.jsonl`, `post_attack_trace.jsonl` | `loop.py:266,296,357,74,102` | `session.py:740-765` | turn rows duplicate `turn_open`/`turn_close` scalars |

---

## E. TRAINING WINDOW

- Window constant: `common.LOOKBACK_CAMPAIGNS = 1000` (`common.py:144`) -> `base_model.TRAIN_WINDOW_CAMPAIGNS` (`base_model.py:17`) -> default for `model.gather` (`model.py:45`), `interrupt_model.gather` (`:134`), `train.walk` (`train.py:113-114`), and the UCB window (`ucb_stats.py:9`).
- Selection: `window_floor(n)` = `MIN(first_decision_id)` of the `n` newest campaigns ordered by `first_decision_id` (`store.py:532-540`); everything with `decision_id >= floor` is in (measured floor 144,299: 1,000 campaigns, 62,647 decisions, 62,267 eligible taken rows, 51,266 counted). Campaign filter for interrupts = `window_keys(n)` (`store.py:542-547`).
- Row filter (all trainers): `taken.refusal IS NULL OR NOT IN ('awaiting_execution','campaign_died')` (`store.py:674-675`, `:600`, `:623`, `:733-734`; `memory.py:242-243,306-307`) -- unconfirmed/failed picks (`execute_failed` 20,737, `command_silently_refused` 13,845, `pre_check_refused` 2,872, `executed_unconfirmed` 550 corpus-wide) **stay in** the training set; `counted` is carried but only feeds `CampaignMemory.note_pick` (`model.py:37,89`) and the `n_confirmed` report.
- One row per taken decision: `taken_rows` attaches only the taken offer (`store.py:695-696`) so `F.decision_rows` yields one triple and `model.py:95` takes `triples[0][2]`; columns restricted to `F.MODEL_COLUMNS` (371 names, `features.py:1090-1165`; 311 present in the current meta).
- Label (`base_model.py:191-226`): for each of `settlements, lord_level, allies, vassals`: `max(turn_open[part] for turns >= decision turn)` (whole future of the campaign, `horizon=None`) minus the decision's campaign-blob value; `target = sum(weight * delta)` with `vassals` x3; `None` (skipped) only if every part is `None`. `turn_open` = first decision of each `(campaign, turn)` (`pg_schema.py:215-227`). `SHORT_HORIZON`/`SHORT_WEIGHT` (`base_model.py:21-22`) are written to meta but unused in the label.
- Minimums: `model.MIN_ROWS = 40` (`model.py:25`), `interrupt_model.MIN_ROWS = 5` (`:25`, also the per-screen exploit gate `:295-301`), `schema.MIN_ROWS = 40` for the GNN (`schema.py:236`).
- Split: `grouped_split` by campaign, seed 0, 15 % held out (`base_model.py:59-80`) for CatBoost; `stable_split` sha1-salted (`"gnn3"`) for the GNN (`:82-104`, `greedy_train.py:32`).
- Retrain cadence: `session.py:508` every `--retrain-every` campaigns (logged: before run 301), sequential child processes (`session.py:516-537`); `trained=false` from any trainer kills the session (`:538-544`).
- Volumes / durations (logged 2026-09-03 16:01-16:14): catboost `rows 62,845`, 116 s wall (`fit_seconds` 53.6, 288 trees); interrupt `rows 12,212`, 33 s wall (`fit_seconds` ~13); GNN `rows 62,567`, walk 82.0 s (5,380 built / 57,467 cached), `walk+tensorize` 86.8 s, norm 12.0 s, fit 513 s budget, 641 s wall; **~13 min per retrain**. Current artefacts (2026-09-04 12:24-12:36): catboost rows 61,975 / 999 campaigns / fit 17.3 s (30 trees); interrupt rows 11,477; GNN rows 61,689 / fit 548.2 s / 6 epochs.
- Timing log sites: `base_model.py:135,149` (`fit_seconds`), `model.py:157` (report), `session.py:517-519,531-534` (`seconds`), `train.py:212-213` (walk), `greedy_train.py:42,140-142,191-192,222` (norm, epochs, budget, walk_seconds), `optimize_catboost.py:138-141,182-187`, `optimize_greedy.py:122-123,175-178`. `model.gather` itself logs nothing (measured 2.9 ms/decision -> ~3 min for the window; `taken_rows` DB side ~9 s / 2.1 GB JSON).

---

## F. FALLBACKS, SILENT EXCEPTS, DEAD CODE, SKEWS (facts only)

Broken / dead on these paths
1. `loop.py:788-822 verify_streams` uses `?` placeholders on psycopg; every call raises (measured), so `session.py:687` records `{"error": ...}` under `streams` for every campaign; `verify_streams_bg` thread + `stream_watermark` (`session.py:581`, `loop.py:775-785`) exist only to feed it.
2. `store.py:560-571` offers query inside `read_decision` returns 0 rows on the live path (options are attached after the read).
3. `interrupt_model.py:152` decodes `campaign_blob` and `world_blob` of all 43k interrupts (`store.py:756-764`) and `gather` never reads `r["campaign"]`/`r["world"]`.
4. `catalogue.py:47-57` loads `chain_super`, `chain_category`, `unit_caste`, `skill_actions` with no reader except `__main__`; `catalogue.ready()` (`:170-171`) has no caller.
5. `base_model.py:21-22 SHORT_HORIZON/SHORT_WEIGHT`, `:229-234 turns_left`, `:191-195 future_best`, `:154-168 _pct/_sd` -- no caller in the lane. `session.py:719 METRICS_DIR`, `:1066-1068 IN_FLIGHT_S, LIVE_LOG_S, CURRENT_SESSION_LOG` unused in the file.
6. `rules/v1.json`, `rules/probe_gaps.json`, `campaigns/splitter.py`, `decisions/gameref.py` (from this lane), `models/local`, `models/mapgraph`, `models/mapgraph_interrupt`: no consumer in the working tree (advisor side).
7. `mapgraph_corpus`: 20 stale shards (5.4 GB) never pruned (`corpus.py:121-125` prunes sibling fingerprints only); manifest watermark ahead of the DB.
8. `session.py:560-566` reloads `model.cbm`, the interrupt model (`loop.py:200`) and `model.pt` (`policy.py:96`) for every campaign after `_require_models` already loaded them.
9. `journal.py:26-41` connections (one per thread: main, each campaign's Watchdog thread) are never closed (`journal.close` has no caller).
10. `session.py:831-862 _measure` re-queries every campaign of the stretch (3 SQL + full `loop_report.jsonl` parse) twice per campaign.

Silent excepts / fallbacks
11. `session.py:99-108 _width_counts`, `:119-130 _start_gain_stats` swallow `OperationalError`/`UndefinedTable` -> `{}` (width filter passes everything; UCB scores every start `inf`).
12. `session.py:202-227 _ending_evidence`: bare `except Exception`; falls back to the newest campaign in the corpus when the entry has no uuid (`:206-212`). `session.py:768-779 _resolve_uuid`: time-window fallback, `None` unless exactly one hit.
13. `session.py:1071-1085 _feature_version`: `except Exception: continue` -> `"unfitted"`.
14. `loop.py:165-176 _verify_action_catalogues`, `:775-785 stream_watermark` (-> 0), `:366-386 _drain_interrupts`, `:335-359 _turn_trail

`_turn_trail`, `:270-274` on_turn hook: `except Exception` -> log line only.
15. `store.py:20-41 _SnapshotRead`: failure to `BEGIN` is logged as "reads may be torn" and reads proceed without a snapshot (and `taken_rows`' named cursor would then fail, measured).
16. `store.py:715-722, 751-758` (`campaign_snapshots`, `interrupt_rows`): malformed blob JSON -> `{}` silently; `memory.py:260-267`: bad panel/params JSON -> `{}`.
17. `corpus.py:53-65 fingerprint`: any exception -> `"reference|absent"` mixed into the fingerprint (a transient reference-DB failure changes the fingerprint and forces a full 14 GB cache rebuild); `corpus.py:96-99 load`: unreadable cache -> `rmtree` + rebuild.
18. `features_db.py:14-19`: singleton connection with no timeout/retry; `:25-34` misses cached as `{}` for the process lifetime (a key missing at first sight stays "missing" even after a reference rebuild).
19. `options.py:233-241 _build_hero_actions`: catalogue failure -> `HERO_ACTIONS = {}` (no hero actions offered, stderr only); `:264-274 _hero_subtype_types`: failure -> `{}` (every `recruit_hero` gated `agent_type_unknown`); `:277-296 _hero_action_matrix`: failure -> empty matrix.
20. `interrupt_model.py:295-301, 320-324`: `greedy_catboost_random_fallback` whenever the screen has < 5 training rows or `screen_rows` is absent from meta; `:326-330` second fallback branch reachable only when `self.ready` is False (but `usable` already requires meta, so `:330` is dead).
21. `policy.py:151-158`: `<arm>_random_fallback` mode only for non-trainable arms; the only non-trainable arm is `random` itself and `Random.pick` (`strategies.py:25-34`) never returns `None`, so the branch is dead.
22. `strategies.py:47 GreedyCatboost.pick`: `r.get("exploit") or 0.0` treats a missing score and a 0.0 score alike; `max` returns the first maximal offer (offer order decides ties).
23. `greedy_rank.py:66-78`: offers without an action node are scored at `min(reward) - 1` (floor) with a one-time warning (`_warned`).
24. `build.py:795-813 _target_of`: chained fallbacks (`s:` then `r:`; `target_cqi` before key-based targets; `params.region` last).
25. `memory.py:20 choice_name`, `:23-35 choice_index/result_index` map unknown values to `"none"`/`0.0`.
26. `model.py:186-193 Ranker.score` with `ready=False` returns unscored rows (`exploit=None`), and `policy.py:147-150` raises `ModelUnavailable` only if a trainable arm is drawn -- a random-only mix runs fine with no model.
27. `session.py:326-348 _train_child` parses the last `{`-line of the child's stdout as the report JSON; any stdout line starting with `{` after the real report would be picked instead.
28. `optimize_catboost.py:48-56`, `optimize_greedy.py:46-54`: connect as `postgres`-db admin and `CREATE DATABASE optuna` on demand.

Train/serve skews (both sides cited)
29. `camp_act_index`: serve = executed non-noop picks + 1 (`loop.py:511`, noop `continue`s at `:577-588` before `actions += 1` at `:607`); train = every taken row including `noop` (`model.py:67-70`). In `MODEL_COLUMNS` (`features.py:1117`).
30. `opt_item_pool_free`, `opt_item_pool_same_type` (`features.py:1047-1073`, in `MODEL_COLUMNS :1158`): serve sees the entity's whole item pool; train sees only the taken offer (`store.py:695-696`) -> pool size 0/1.
31. Interrupt model: serve `campaign` = last snapshot's campaign dict carrying `act_index`, `move_index`, `action_counts`, memory stamps (`loop.py:207-210, 511-516`); train `campaign` = campaign blob + `stamp_prev_actions` only (`interrupt_model.py:164`) -> `camp_act_index` `None` and every `camp_taken_*` = 0 at train (`features.py:114-119, 134`; `camp_act_index`, `camp_taken_total`, `camp_taken_diplomacy`, `camp_taken_recruit_unit`, `camp_taken_attack_army`, `camp_taken_attack_settlement`, `camp_taken_cancel_recruit` are in `MODEL_COLUMNS`).
32. Interrupt `world`: serve = last snapshot world (`interrupt_model.py:307`); train = latest decision world at or before the interrupt ts (`interrupt_model.py:158-159`) -- same source, but the interrupt's own `world_blob` (collected at interrupt time, `decisions_stream.py:154`) is never used by either side.
33. Memory stamps at train are rebuilt by replaying `taken` rows in `decision_id` order (`model.py:64-89`; `memory.py:281-332` for the GNN) whereas serve replays executed picks including `_force_end_turn` picks that have no `pick` RPC (`loop.py:526-530, 564-568`) -- `note_pick("faction","*","end_turn",None,True)` at serve vs the recorder's `taken` row keyed to the real decision at train.
34. GNN training drops any decision whose taken identity matches 0 or 2+ offers (`train.py:81-84`; current tally `taken_missing` 286), while serve scores such offers normally.

Simplicity notes (facts)
35. Three trainers each re-read `target_series()` (whole `turn_open`), `run_dbs`, `window_floor`; `taken_map()` (whole corpus, 1 s) is read before the window is applied (`train.py:131-143`).
36. `interrupt_rows()` has no `WHERE`; the 1000-campaign window is applied in Python after decoding three blobs per row (`interrupt_model.py:152-156`).
37. `prebattle_attributions` is run unwindowed for the CatBoost gather (`model.py:62`, 14,272 rows, 2.5 s) and once per shard job with a campaign filter for the GNN (`memory.py:288`).
38. `scores_for_store(ranked)` with `limit=None` (`loop.py:571`, `policy.py:207`) sends every ranked offer's scores through the RPC and into `offer_scores` + `offer_model_scores` (22M rows each, 2.6 + 2.3 GB) -- the largest tables in the DB are written by the live loop's pick log.
39. `actions` (5.1M rows, 1.6 GB) is keyed on `(context_kind, context_id, action_type, action_key, params)` (`pg_schema.py:39-43`), so every distinct `params` dict (move x/y, standing, chance, cost) is a new row; the readers here only use `action_type`, `action_key`, `params`.
40. `features_db._lookup` runs `SELECT *` and `action_block` turns every column into an `opt_db_<col>` feature (`features.py:949-955`), of which `MODEL_COLUMNS` keeps 9 (`opt_db_chain_chain_category, opt_db_upkeep_cost, opt_db_category, opt_db_legendary, opt_db_transferrable, opt_db_uniqueness_score, opt_db_tier, opt_db_required_parents, opt_db_level, opt_db_building_chain` + `opt_db_res_*`).
41. `session.py` writes the full session JSON after every turn (`:614-619`) and again per campaign; `_loop_turns` re-parses the whole `loop_report.jsonl` on each checkpoint because its mtime changes every turn (`:740-765`).

UNKNOWN
- Which code produced `D:\twdata\tmp\catboost\*.tsv` (dated Aug 30; current code sets `allow_writing_files=False`).
- Why the live DB's `MAX(decision_id)` (206,945) is below the corpus-cache watermark (211,678) written at 12:26 today -- outside this lane's files.
- Wall time of `model.gather` at the full window (no log site; extrapolated ~3 min from the 25-campaign measurement).