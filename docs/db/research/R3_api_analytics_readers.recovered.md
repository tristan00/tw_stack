# Lane R3 — every reader in the dashboard API, the analytics layer and the utility scripts

Working tree at HEAD (`6d18a7f`) is the only source. All measurements below were taken 2026-09-04 ~21:12-21:20 local on the live `tw_stack` DB with `readonly=True`, `statement_timeout` ≤ 180 s, sampling by `MOD(id, k)` or `TABLESAMPLE`. The corpus was at rest during measurement (max `decisions.ts` = 09-03 20:43:58; `MAX(decision_id)` = 206,945; three other sessions from sibling lanes were running heavy selects, `pg_stat_activity`). The report file was not written to the scratchpad because the session is in plan mode (only read-only actions permitted); this message is the complete report.

---

## 0. Plumbing shared by every API reader

| Fact | Where |
|---|---|
| API corpus connection: one `psycopg` connection per thread (`threading.local`), `autocommit=True`, `readonly=True`, `row_factory=pg.row_factory`, `search_path = "public, analytics, app, reference"` | advisor_api/db.py:18, 57, 60-66 |
| `db.write()` opens a non-readonly connection with the same search path — **no caller anywhere in the tree** | advisor_api/db.py:69-74 |
| `db.stamp()` = 3 queries: `SELECT MAX(decision_id) FROM decisions`, `… FROM taken`, `SELECT MAX(interrupt_id) FROM interrupts`; errors are stringified into the tuple | advisor_api/db.py:77-93 (measured 0.01 s / 0.00 s / 0.00 s) |
| `db.columns()` and `db.db_path()` have no caller | advisor_api/db.py:53-54, 96-100 |
| Analytics connection: second per-thread connection via `analytics.store.connect(readonly=True)` → `search_path = "analytics, public"` (env `TW_ANALYTICS_SCHEMA`, `TW_PG_SEARCH_PATH` override). `adb.rows/one` swallow every `psycopg.Error` and return `[]`/`None` | advisor_api/analytics_db.py:21-25, 48-60; analytics/store.py:37-47 |
| Reference-data connection: one process-wide connection guarded by a lock, `search_path="reference"`; every failure resets it to `None` and returns `None`/`[]` (fallback) | advisor_api/labels.py:12-14, 23-38, 119-126 |
| Timing middleware: every `/api/*` except `/api/events`, `/api/health` gets per-function timings via `@db.timed` collected in a contextvar, printed and sent as `Server-Timing` | advisor_api/app.py:86-109; db.py:22-46 |
| Warm thread: on startup (unless `TW_API_NO_WARM`) runs `q.warm_caches()` every `WARM_EVERY_S = 120` s and on `_warm_kick` (POST reward-weights) | app.py:48-79, 469; queries.py:1061-1063, 1161-1176 |
| `warm_caches` force-rebuilds: `_camp_meta`, `campaign_rows_cached`, `_positions_data` (whole-corpus campaign-blob decode), `_hist_map` for the 5 `POSITION_HISTORY` families | queries.py:1161-1176, 2804 |
| While the warm thread is alive, `_stamped_slow` **never expires** a cached value (TTL 300 s is bypassed by `or _WARM_ALIVE.is_set()`); values change only when the warm loop rebuilds them (every 120 s) or on `set_reward_weights` → `_STAMPED.clear()` | queries.py:1151-1158, 1113 |
| `_stamped(name)` keys on `(db.stamp(), minute)` — it runs `db.stamp()` (3 queries) on every call | queries.py:1048-1058 |
| **Cache-key collision**: `_stamped("campaign_rows")` (used by `campaign_row` 1180 and `start_campaigns_slice` 1185) and `_stamped_slow("campaign_rows")` (used by `campaign_rows_cached` 574 and the warm loop 1165) share the dict key `_STAMPED["campaign_rows"]` with incompatible key formats (`(stamp, minute)` vs `("slow", ts)`); each path treats the other's entry as a miss and rebuilds `campaign_rows()` (≈9 queries incl. `campaign_growth`, `campaign_endings`, `campaign_gains`, `ucb_picks`) then overwrites the entry. Alternating `/api/campaigns` and `/api/campaigns/{key}` therefore rebuilds on every request, and the warm loop's copy is never seen by `campaign_row` | queries.py:1048-1058, 1151-1158, 573-575, 1179-1186 |
| Process-lifetime caches never invalidated: `labels._cache` (loc strings, unbounded), `_HAVE`, `_effect_good`, `_item_res_cache`, `_bname_rows`, `_tech_key_cache`, `_skill_line_cache`, `_tech_groups_cache`; `ident.culture_names/campaign_map_names` (`lru_cache(1)`); `_SCHEMA_HASH`, `_TARGET_WEIGHTS` | labels.py:14, 129, 161, 221, 253, 426, 469, 506; ident.py:35, 51; queries.py:3956, 4752 |
| `_totals_memo` 60 s TTL; `_weights_cache` keyed on file mtime of `<TWDATA>/rules/reward_weights.json` | queries.py:290-312, 1071-1094 |

UI fetch mechanics (ui/src/lib/api.ts): one `EventSource('/api/events')` per tab (209-215); the server loop calls `db.stamp` in a thread every 2.0 s per connected client and emits a `corpus` event on change, keepalive every 10 loops (app.py:880-901). `useApi(url, deps, {live})` refetches when `url`/`deps` change and, for `live !== false` (the default), whenever the stamp changes, throttled client-side to one emit per `STAMP_THROTTLE_MS = 10000` (api.ts:134-175, 180-207). "Poll cadence" below therefore means: **live** = refetch ≤ every 10 s while the corpus is changing; **once** = `{live:false}`, refetch only on URL/deps change or manual reload. `/api/log` is the only `setInterval` (3000 ms while "follow", Log.tsx:71). `Layout.tsx:97` mounts `StatusLine` → `/api/run` on **every** route.

---

## A. Every API query

Format: **route** (handler app.py:line) → query functions (queries.py:line) → SQL → tables → blob/JSON decoded in Python → scope → cache → UI caller / cadence. "corpus" = `public` schema; "adb" = analytics schema via `analytics_db`.

### A.1 Run / health / events / log

**GET /api/run** (120-130) → `proc.services()` (proc.py:82-106, PowerShell `Get-CimInstance` sample refreshed every 4 s by a thread, proc.py:14-54) + 5 query functions:
- `current` (198-225): `SELECT d.turn,d.ts,d.campaign_id,c.campaign_key,c.leader,c.faction,c.campaign_map,b.z FROM decisions d JOIN campaigns c … LEFT JOIN blobs b ON b.blob_id=d.campaign_blob ORDER BY d.decision_id DESC LIMIT 1`; then `SELECT SUM(n) FROM start_counts` (view; measured 0.02 s, plan = correlated subquery per campaign, pg_schema.py:253-257); `SELECT COUNT(*) n, MIN(ts) t0 FROM decisions WHERE campaign_id=%s`; `SELECT pick_id FROM ucb_picks WHERE campaign_map=%s AND faction=%s ORDER BY pick_id DESC LIMIT 1`. **Decodes the latest campaign blob** (`_jload(row["z"])`, 207) → keys `settlements`, `power_rank`, `lord_level` (218-220). Bounded (LIMIT 1). No cache.
- `throughput` (316-345): `SELECT ts,campaign_id,turn FROM decisions WHERE ts >= now-3600 ORDER BY decision_id DESC`; `SELECT COUNT(*) a, SUM(counted=1) c FROM taken WHERE refusal NOT IN ('awaiting_execution','campaign_died') AND ts >= %s`; `_confirm_spark` (370-386): `SELECT counted, refusal FROM action_taken ORDER BY decision_id DESC LIMIT 2000` (view `action_taken` = taken ⋈ actions, pg_schema.py:202-213). Bounded by time / LIMIT. No cache.
- `totals` (294-312): `SELECT COUNT(*) FROM (SELECT campaign_id FROM decisions GROUP BY campaign_id HAVING COUNT(*)>=2)`; `SELECT COUNT(*) FROM decisions`; `SELECT COUNT(*) FROM offers` (**whole-corpus count of the 24.8 M-row table, measured 0.33 s**); `SELECT COUNT(*) FROM taken WHERE counted=1`. Memo 60 s (`_totals_memo`).
- `collect_timing` (390-414): `SELECT timings FROM decision_points ORDER BY decision_id DESC LIMIT 400` (view joins two blobs it does not use, pg_schema.py:169-177); **decodes `decisions.timings` JSON** keys `collect_ms, roundtrip_ms, score_ms, store_ms, trace_ms, housekeep_ms, pickup_lag_ms` (396-397). Bounded. No cache.
- `cycle_timing` (418-439): `SELECT timing FROM action_taken ORDER BY decision_id DESC LIMIT 400`; **decodes `taken.timing`** keys `snapshot_ms, execute_ms, confirm_ms, gates_ms, total_ms` (424). Bounded. No cache.
- UI: `Layout.tsx:97` (every page, live), `Home.tsx:6` (live), `Run.tsx:33` (live).

**GET /api/log** (133-144) → `read_session_log` (4989-5051): filesystem only — `log_files()` lists `<LOGS_ADVISOR>/session_*.log` (4952-4960), `session_log_path()` reads `common.CURRENT_SESSION_LOG` pointer (4939-4944), reverse chunked read with bisect on timestamps (4970-5047), caps `LOG_CHUNK=256 KiB`, `LOG_SCAN_CAP=8 MiB`, `LOG_MAX_LINES=2000` (4947-4949). No DB. UI: `Log.tsx:71` `setInterval(3000)` while follow.

**GET /api/events** (880-901): SSE; per client `db.stamp()` every 2 s. UI: api.ts:211 (one per tab).
**GET /api/health** (904-907): `db.stamp()` once. UI: `Layout.tsx:52` once.

### A.2 Campaigns list / detail

**GET /api/campaigns** (539-573) → `outcome_join` (175-190): `SELECT campaign_key FROM campaigns` (corpus) + adb `SELECT campaign_key, ts, faction, outcome, when_text, error, verdict, suspicious, because FROM campaign_endings` (whole table, 4,392 rows, 0.01 s) — Python builds a fake postmortem dict per row with `"growth": {}` (188). Then `campaign_rows_cached` (573) → `campaign_rows` (487-570): `_decs_all` (464-469) `SELECT c.campaign_key ckey, COUNT(*) n, MIN(d.ts) t0, MAX(d.ts) t1, MAX(d.turn) last_turn FROM decisions d JOIN campaigns c … GROUP BY c.campaign_key` (**whole-corpus GROUP BY over decisions**, 0.04 s); `_acts_all` (472-483) `SELECT campaign_id, COUNT(*) rows_, SUM(awaiting/died→0 else 1) attempted, SUM(counted=1) confirmed FROM taken GROUP BY campaign_id` (whole taken, 0.03 s) + `_campaign_keys` (442-444) `SELECT campaign_id, campaign_key FROM campaigns`; adb `SELECT * FROM campaign_growth` (491, 4,827 rows, `CG.enrich` per row); `SELECT campaign_id, campaign_key, faction, turns, campaign_map, presave_radius, leader FROM campaigns` (492-494); `outcome_join` again (497); `pick_campaigns` (671-692): `SELECT picked_ts, campaign_map, faction, campaign_key FROM campaigns WHERE picked_ts IS NOT NULL ORDER BY picked_ts` + `SELECT pick_id, ts, campaign_map, faction FROM ucb_picks ORDER BY pick_id`, joined in Python by a ≤120 s time window (`PICK_JOIN_S`, 646, 683-691); `gains_all` (649-657) `SELECT * FROM campaign_gains` (view = whole-decisions GROUP BY per campaign, pg_schema.py:259-275; 4,719 rows, 0.07 s) with Python `_weighted_reward`. No blobs. Whole corpus. Cache: `_stamped_slow("campaign_rows")` (see collision in §0). `campaigns_slice` (611-629) filters/sorts/pages in Python (`_slice_rows` 592-607, page_size ≤ 200). UI: `Campaigns.tsx:154` (live), `QuickJump.tsx:47` (once, on Ctrl-K open).

**GET /api/campaigns/{key}** (576-588) → `campaign_row` (1179-1181; `_stamped("campaign_rows")` → rebuild, see §0); `reward_series` (4865-4878): `SELECT turn, income, settlements, allies, vassals, power_rank FROM turn_open WHERE campaign_id=%s ORDER BY turn` (view over `turn_bounds`, pg_schema.py:215-227; note `turn_open.campaign_id` is the campaign **key**); `diplomacy_tail` (4882-4911): `SELECT ts,campaign_key,turn,kind,payload FROM diplomacy_events WHERE campaign_key=%s ORDER BY event_id DESC LIMIT 600`, **decodes `payload` JSON** keys `kind, turn, deal_score, success_chance, outcome, result, channel, target, faction, standing, terms, speech` (4894-4910) — measured payload keys (1/10 sample of 74,145 rows) are `channel, faction, gift, ok, pair, panel, req_id, rpc_id, terms, treaty_before, ended_by, tracked, turns_played, …`; `deal_score/success_chance/outcome/result/standing/target/speech(66 rows)` are absent or nearly so, so `DiploEvent.outcome/deal_score/standing` are None for the sampled rows; `campaign_verdict` (3347-3379) parses `row.ended_because` text with regex/`ast.literal_eval`; `campaign_turn_rollup` (3383-3395): `SELECT d.turn, COUNT(*) n, SUM(counted=1) conf, SUM(refused) FROM taken t JOIN decisions d … JOIN campaigns c … WHERE c.campaign_key=%s AND d.turn IS NOT NULL GROUP BY d.turn ORDER BY d.turn DESC`. Bounded by campaign (except the campaign_rows rebuild). UI: `CampaignDetail.tsx:647` (live).

**GET /api/campaigns/{key}/state** (869-877) → `campaign_state` (233-287): `_campaign_id_of` (3190-3194) `SELECT campaign_id, faction, campaign_map, leader FROM campaigns WHERE campaign_key=%s`; adb `SELECT family, COUNT(DISTINCT key) n FROM acquisitions WHERE campaign_id=%s AND acquired_decision IS NOT NULL AND acquired_decision > first_seen_decision AND family IN ('research','building','skills') GROUP BY 1`; **entity blob**: `SELECT b.z FROM entities e JOIN decisions d … JOIN blobs b ON b.blob_id=e.features_blob WHERE d.campaign_id=%s AND e.context_kind='campaign' ORDER BY e.decision_id DESC LIMIT 1` → keys `current_research`, `equipped_all[].key`, `anc_pool[].key` (256-267); **lord blobs**: `SELECT DISTINCT ON (e.context_id) b.z … WHERE d.campaign_id=%s AND e.context_kind='lord' ORDER BY e.context_id, e.decision_id DESC` → keys `is_leader, region, hp, rank, wounded, skill_points` (274-285; measured 0.00 s, 1 row for the newest campaign). Bounded by campaign. No cache. UI: `CampaignDetail.tsx:130` (live).

**GET /api/campaigns/{key}/decisions** (591-598) → `decisions_page(con, 0, 500, campaign=key)` (3493-3577; see A.6). **No UI caller** (CampaignDetail TABS are overview/buildings/research/skills/items, CampaignDetail.tsx:32-38; `Run.tsx:76` links to `?tab=decisions`, which is not a tab).

**GET /api/campaigns/{key}/buildings** (601-610) → `campaign_buildings` (2094-2149): `SELECT d.turn, a.action_type kind, a.action_key, a.context_id ctx, a.params FROM taken t JOIN actions a … JOIN decisions d … WHERE t.campaign_id=%s AND t.counted=1 AND a.action_type = ANY(['building','horde_building','building_repair','building_dismantle']) ORDER BY t.decision_id`; **decodes `actions.params`** keys `building_key, is_upgrade, cost, repair_cost, region, refund` (2107-2137); `labels.building_info` (reference `buildings ⋈ building_chains`, labels.py:291-299); `labels.name_for("garrison", …)` → `loc`. Measured params keys: `building` rows carry no `building_key`/`region` (falls back to `action_key`/`ctx`), `horde_building` rows carry no `cost` → horde cost is always None (2128-2129). Bounded. UI: `CampaignDetail.tsx:341` (once).

**GET /api/campaigns/{key}/research** (613-622) → `campaign_research` (3198-3242): taken⋈actions⋈decisions `action_type='research'` for the campaign; `WITH f AS (SELECT DISTINCT ON (turn) turn, decision_id FROM decisions WHERE campaign_id=%s AND turn IS NOT NULL ORDER BY turn, decision_id) SELECT f.turn, a.action_key FROM f JOIN offers o … JOIN actions a … WHERE a.action_type='research'` (offer sets per turn); reference `tech`, `tech_links`, `tech_groups`, `loc`. No blobs. Bounded. UI: `CampaignDetail.tsx:400` (once).

**GET /api/campaigns/{key}/skills** (625-633) → `campaign_skills` (3280-3301): `_campaign_chars` (3245-3276) **decodes every lord/hero's latest entity blob** (`SELECT DISTINCT ON (e.context_id) e.context_kind, e.context_id, b.z FROM entities e JOIN decisions d … JOIN blobs b … WHERE d.campaign_id=%s AND e.context_kind IN ('lord','hero') ORDER BY e.context_id, e.decision_id DESC`) → keys `subtype, rank, skill_points, equipped[].key, equipped[].name` (3261-3270); then taken⋈actions⋈decisions `action_type='skills'` with **`params` decoded** → `level`, `total_levels` (3294-3300). Bounded. UI: `CampaignDetail.tsx:491` (once).

**GET /api/campaigns/{key}/items** (636-645) → `campaign_items` (3305-3339): `_campaign_chars` again (blobs, as above); taken⋈actions⋈decisions `action_type = ANY(['items','item_unequip'])`; `SELECT MAX(o.decision_id) FROM offers o JOIN actions a … action_type='items' JOIN decisions d … WHERE d.campaign_id=%s` then `SELECT DISTINCT a.action_key FROM offers o JOIN actions a … WHERE o.decision_id=%s`. Bounded. UI: `CampaignDetail.tsx:541` (once).

### A.3 Starts (UCB view of campaigns)

**GET /api/campaigns/starts** (147-159) → `gains_all` (649) → `ucb_context` (696-740): Python UCB over the newest `UCB.WINDOW = common.LOOKBACK_CAMPAIGNS = 1000` gains (ucb_stats.py:9; common.py:144), `UCB.load_adjustments()` reads `rules/ucb_adjust.json` (ucb_stats.py:16-31), `_pool()` imports `presaves.list_presaves` (660-667, filesystem, bare `except Exception` fallback to `{}`), `SELECT campaign_map, faction FROM ucb_picks ORDER BY pick_id` (726-727). `starts_page_extras` (886-934): Python only. `starts_rows(con, None, cx, gains)` (752-866): **`rows is None` → `campaign_rows(con)` uncached** (753-754, whole campaign_rows rebuild incl. a second `gains_all`, 501); `SELECT campaign_map, faction, n FROM start_counts` (769-770); `_start_leaders` (5069-5072) `SELECT campaign_map m, faction f, MAX(leader) l FROM campaigns WHERE leader IS NOT NULL GROUP BY …`; `SELECT campaign_id ckey, SUM(allies>0) a, SUM(vassals>0) v FROM turn_open GROUP BY campaign_id` (799-802, 0.07 s). No blobs. Whole corpus, no cache except the nested rebuild's overwrite of `_STAMPED["campaign_rows"]`. UI: `Campaigns.tsx:99` (live), `QuickJump.tsx:46` (once).

**GET /api/campaigns/starts/{map}/{faction}** (162-174) → `start_head` (953-1002): `gains_all`, `ucb_context` (both whole corpus, recomputed), `SELECT campaign_id FROM campaigns WHERE campaign_map=%s AND faction=%s`, `SELECT MAX(turn), MIN(ts), MAX(ts) FROM decisions WHERE campaign_id = ANY(%s) GROUP BY campaign_id`, `SELECT SUM(att), SUM(conf) FROM taken WHERE campaign_id = ANY(%s)`, `SELECT n FROM start_counts WHERE …`, `_start_leaders`. `start_last_played` (1006-1018) → `_start_gains` (1213-1226): `SELECT c.campaign_id, c.campaign_key, c.outcome, g.* FROM campaigns c JOIN campaign_gains g … WHERE c.campaign_map=%s AND c.faction=%s ORDER BY g.first_ts`. No blobs. No cache. UI: `StartDetail.tsx:668` (live).

**…/performance** (177-186) → `start_performance` (1243-1294): `gains_all` (whole), `_start_gains`. UI: `StartDetail.tsx:240` (once).

**…/openings** (189-201) → `start_openings` (1298-1433): `_start_gains`; `start_firsts` (1022-1036) `SELECT DISTINCT ON (t.campaign_id, a.action_type) … FROM taken t JOIN actions a JOIN campaigns c WHERE c.campaign_map=%s AND c.faction=%s AND t.counted=1 AND a.action_type = ANY(['building','skills','research','items']) ORDER BY t.campaign_id, a.action_type, t.decision_id`; offered-set CTE joining `offers` (1310-1325); conquest steps `ROW_NUMBER() OVER (PARTITION BY t.campaign_id ORDER BY t.decision_id)` for `attack_settlement` (1396-1404); `labels.name_for`. No blobs. Bounded by start. UI: `StartDetail.tsx:166` (once, band param).

**…/campaigns** (204-210) → `start_campaigns_slice` (1184-1210): `_stamped("campaign_rows")` (rebuild, §0), `SELECT campaign_id, campaign_key FROM campaigns WHERE campaign_map=%s AND faction=%s`, `start_firsts`, `gains_all` (whole). UI: `StartDetail.tsx:337` (once).

**…/actions** (213-221) → `start_actions` (1437-1461): `SELECT a.action_type, SUM(tried), SUM(ok), COUNT(*), SUM(COALESCE(t.latency_ms,0)) FROM taken t JOIN actions a JOIN campaigns c WHERE c.faction=%s GROUP BY 1` (faction only — `campaign_map` is ignored). **No UI caller.**

**…/research** (224-231) → `start_research` (1846-1894): adb `WITH camps AS (…campaigns ⋈ campaign_gains with reward computed in SQL from reward_weights…)` (`_FACT_CAMPS` 1126-1134, `_fact_params` 1137-1142) `SELECT campaign_id, reward FROM camps WHERE campaign_map=%s AND faction=%s`; `SELECT a.key, COUNT(*) FILTER (acquired) tn, AVG(acquired_turn), AVG(m.reward) FILTER (acquired) FROM acquisitions a JOIN camps m USING (campaign_id) WHERE … AND a.family='research' GROUP BY a.key`; reference `tech` (`tech_universe` node_set expansion, labels.py:414-423), `tech_links`, `tech_groups`, `loc`. No blobs. UI: `StartDetail.tsx:401` (once).

**…/skills** (234-243) → `start_skills` (1917-2013): `_start_snapshots` (1897-1914) **decodes the latest lord+hero blob per character per campaign of the start** (`SELECT DISTINCT ON (c.campaign_id, e.context_id) … FROM entities e JOIN decisions d JOIN campaigns c JOIN blobs b … WHERE c.campaign_map=%s AND c.faction=%s AND e.context_kind IN ('lord','hero') ORDER BY c.campaign_id, e.context_id, e.decision_id DESC`; measured for the busiest start (283 campaigns): 0.25 s, 835 rows, 8.15 MB of JSON) → keys `subtype, rank, skill_points, skills[].key/.level/.tier/.total_levels` (1924-1938, 1968-1972); `_start_gains`; adb `SELECT a.key, AVG(a.acquired_turn) FROM acquisitions a JOIN campaigns c … WHERE COALESCE(c.campaign_map,'')=%s AND c.faction=%s AND a.family='skills' AND a.sub=%s AND a.acquired_turn IS NOT NULL GROUP BY 1` (1977-1983); reference `skill_links ⋈ skill_node_sets`, `skill_categories`, `skill_indents`, `loc`. Bounded by start. No cache. UI: `StartDetail.tsx:457` (once, subtype param).

**…/items** (246-254) → `start_items` (1595-1633): adb `camps` CTE; `SELECT a.campaign_id, COUNT(*) held, COUNT(*) FILTER (acquired_turn IS NOT NULL) eq FROM acquisitions a WHERE a.family='items' AND a.campaign_id = ANY(%s) GROUP BY 1`; `_fact_item_counts` (1566-1582) `SELECT e.campaign_id, e.key, COUNT(*) FILTER (kind='on'), COUNT(*) FILTER (kind='off') FROM item_events e JOIN campaigns c … WHERE … GROUP BY 1,2`; `_fact_item_rows` (1530-1563) `WITH camps, per AS (SELECT a.campaign_id, a.key, BOOL_OR(acquired_turn IS NOT NULL) worn FROM acquisitions a WHERE a.family='items' GROUP BY 1,2) SELECT p.key, COUNT(*) held, COUNT(*) FILTER (worn) eq, AVG(m.reward) FILTER (worn), AVG(m.reward) FILTER (NOT worn) FROM per p JOIN camps m USING (campaign_id) [WHERE m.campaign_map=… AND m.faction=…] GROUP BY p.key` (**the `per` CTE aggregates the whole acquisitions items family before the start filter**); reference `ancillaries`, `ancillary_effects` (`item_resources` cached whole table, labels.py:240-250), `loc`. UI: `StartDetail.tsx:618` (once).

**…/buildings** (257-264) → `start_buildings` (2042-2086): adb camps CTE + `per` over `acquisitions … family='building'` joined to camps filtered by start; reference `buildings`, `loc`. UI: `StartDetail.tsx:580` (once).

### A.4 Catalog (items, buildings, research, skills, traits, choices, overtime)

**GET /api/items** (267-274) → `items_page` (1744-1748) → `_fact_item_rows()` unfiltered (whole `acquisitions` items family ⋈ all campaigns, adb) + `labels.item_resources()` (whole `ancillary_effects`, cached) + `item_category` per key (`SELECT category FROM ancillaries WHERE key=%s`, one query per item, labels.py:140-149). No blobs. Whole corpus. No cache. UI: `Items.tsx:204` (once).

**GET /api/items/swaps** (277-284) → `_fact_item_swaps` (1641-1741): adb `WITH camps … SELECT campaign_id, campaign_map, faction, reward, turns_reached FROM camps` (all campaigns) + `SELECT campaign_id, ctx, key, kind, decision_id, turn FROM item_events ORDER BY campaign_id, ctx, decision_id, event_id` (**whole item_events table, 19,250 rows**); O(offs×ons) pairing in Python (1663-1715) with `labels.item_category` per key (one reference query each). Whole corpus. No cache. UI: `Items.tsx:181` (once).

**GET /api/items/{key}** (287-293) → `item_page` (1751-1842): 5 adb queries: per-start CTE (1753-1766), totals CTE (1772-1781), `SELECT COUNT(*) FILTER (kind='on' AND char_kind='lord'), COUNT(*) FILTER (kind='on') FROM item_events WHERE key=%s`, `SELECT campaign_id, ons, offs FROM item_events WHERE key=%s GROUP BY 1`, recent CTE `ORDER BY m.first_ts DESC NULLS LAST LIMIT 8`; reference `ancillary_effects`, `effects_meta`, `loc` (`ancillaries_colour_text_`, `ancillaries_explanation_text_`). Bounded by key. UI: `ItemDetail.tsx:44` (once).

**GET /api/buildings | /research | /skills | /traits** (324-363 → `_catalog_index` 308-312) → `catalog_index` (2183-2242): adb `WITH camps, per AS (SELECT a.campaign_id, a.key, BOOL_OR(acquired) taken, MIN(acquired_turn) turn, MAX(a.ranks) ranks FROM acquisitions a WHERE a.family=%s GROUP BY 1,2) SELECT p.key, COUNT(*) FILTER (taken) took_n, COUNT(*) of_n, COUNT(DISTINCT m.campaign_map||'|'||m.faction) FILTER (taken) starts, AVG(p.turn) FILTER (taken), AVG(m.reward) FILTER (taken) rt, AVG(m.reward) FILTER (NOT taken) rp, AVG(p.ranks) FILTER (taken) FROM per p JOIN camps m USING (campaign_id) GROUP BY p.key` (**whole family**); `_fact_campaign_count` (1145-1148) `SELECT COUNT(*) FROM campaigns c JOIN campaign_gains g …`; `_fact_key_cultures` (2245-2261, research/traits only) `SELECT a.key, m.faction, COUNT(DISTINCT a.campaign_id) FROM acquisitions a JOIN camps m … WHERE a.family=%s GROUP BY 1,2`; `_catalog_ref` (2152-2179) → reference `buildings ⋈ building_chains` / `tech` / `skills` / `trait_levels`+`trait_meta`; `_fam_label` → `loc` per key (`tech_name` also does `SELECT technology_key FROM tech WHERE key=%s` per key, labels.py:429-444). No blobs. Whole corpus. No cache. UI: `Catalog.tsx:397` (once); `conditions.tsx:226-227` loads `/api/buildings` and `/api/research` when a has/has-not condition needs them (once).

**GET /api/{family}/{key}** (330-369 → `_catalog_key` 315-321) → `catalog_key_page` (2595-2795): adb per-start CTE (2597-2608), `SELECT campaign_map, faction, AVG(reward) FROM camps GROUP BY 1,2` (all campaigns), recent CTE LIMIT 8, `_fact_key_stats` (2264-2295, `a.key = ANY(%s)`), per-character `SELECT sub, MAX(kind), COUNT(DISTINCT campaign_id) FILTER (acquired) tn, COUNT(DISTINCT campaign_id) onn, AVG(ranks) FILTER (acquired), AVG(acquired_turn) FROM acquisitions WHERE family='traits'|'skills' AND key=%s AND sub IS NOT NULL GROUP BY sub ORDER BY tn DESC` (2745-2755, 2774-2784), `SELECT DISTINCT sub FROM acquisitions WHERE family='skills' AND key=%s AND sub IS NOT NULL` (2787-2790); reference `buildings`, `building_chains`, `tech`, `tech_links`, `skill_links ⋈ skill_node_sets`, `skills`, `trait_levels`, `trait_effects`, `trait_antitraits`, `trait_meta`, `effects_meta`, `loc`. No blobs. Bounded by key (plus one all-campaign means query). UI: `CatalogDetail.tsx:170` (once).

**GET /api/choices/{family}** (389-400) → `choices_page` (2398-2533): adb `WITH camps … SELECT campaign_id, reward, turns_reached, faction, campaign_map, leader FROM camps` (all); `_fact_turn_states` (2298-2316): `SELECT DISTINCT ON (campaign_id, turn) campaign_id, turn, settlements, lord_level, allies, vassals FROM decision_features WHERE turn IS NOT NULL ORDER BY campaign_id, turn, decision_id` (**whole decision_features, 206,905 rows**) + `SELECT campaign_id, MAX(settlements), MAX(lord_level), MAX(allies), MAX(vassals) FROM decision_features GROUP BY 1` — this is the **only consumer of `decision_features`**; building: `SELECT campaign_id, ctx, key, MIN(first_seen_turn), MIN(first_seen_decision), MIN(acquired_turn), MIN(acquired_decision) FROM acquisitions WHERE family='building' AND ctx <> '' GROUP BY 1,2,3` (whole family); research/skills: `… WHERE family=%s AND key = ANY(%s) GROUP BY 1,2,3,4`; reference `tech_links` (`_fork_specs`), `skill_links ⋈ skill_node_sets` (`skill_forks`, labels.py:555-566), `buildings`, `loc`. No blobs. Whole corpus. No cache. UI: `Catalog.tsx:175` (once).

**GET /api/overtime/{family}** (403-415) → `catalog_overtime` (2537-2591): adb camps (all); corpus `SELECT DISTINCT ON (t.campaign_id) t.campaign_id, a.action_key FROM taken t JOIN actions a … WHERE t.counted=1 AND a.action_type=%s ORDER BY t.campaign_id, t.decision_id` (**parallel seq scan of taken**, plan above). Whole corpus. No cache. UI: `Catalog.tsx:334` (once, race/lord/start params).

### A.5 Positions / lookup / reward weights

**GET /api/positions** (372-386) → `positions_page` (3024-3116) → `_positions_data` (2807-2917), built once and held by `_stamped_slow("positions_data")` / warm loop: `SELECT campaign_id, faction, campaign_map, leader, outcome FROM campaigns`; `_camp_meta` (1496-1512; `_stamped_slow("camp_meta")`) `SELECT c.campaign_id, c.campaign_key, c.faction, c.campaign_map, c.leader, g.* FROM campaigns c JOIN campaign_gains g …`; `SELECT t.decision_id, a.action_type, a.action_key FROM taken t JOIN actions a … WHERE t.counted=1` (167,374 rows, 0.58 s); **`SELECT d.decision_id, d.campaign_id, d.turn, d.settlements, d.income, d.power_rank, d.lord_level, d.allies, d.vassals, b.z FROM decisions d LEFT JOIN blobs b ON b.blob_id=d.campaign_blob` — whole-corpus decode of every campaign blob (206,907 rows; 1/50 sample: fetch 0.08 s + json 0.02 s ⇒ ≈4 s + 1 s extrapolated; plan = merge join over the `blobs_pkey` index scan of all 949,090 blobs)** → keys `hero_type_counts` (dict; summed and split into `hero:<k>`), `is_researching`, `ll_wounded`, `treasury`, `armies`, `resources` (dict → `res:<k>`) (2850-2877); `SELECT t.campaign_id, a.action_key, MIN(d.turn) FROM taken t JOIN actions a JOIN decisions d … WHERE t.counted=1 AND a.action_type = ANY(['attack_settlement','colonize']) GROUP BY 1,2` (2890-2896). Conditions with `has:/not:` use `_hist_map` (2920-2930): adb `SELECT campaign_id, key, MIN(acquired_turn) FROM acquisitions WHERE family=%s AND acquired_turn IS NOT NULL GROUP BY 1,2` per family (settlement/building/research/skills/items; measured building 0.08 s, 23,059 rows), cached `_stamped_slow(("hist", fam))`. Filtering, future-reward and aggregation are pure Python over the in-memory `decs` tuples (2956-3009, 3035-3063). `labels.target_for` per key. Whole corpus, cached (warm every 120 s, rebuild ≈5 s + Python). UI: `Positions.tsx:52` (once per condition set).

**GET /api/lookup** (418-432) → `campaign_lookup` (3135-3187): `_positions_data`, `_camp_meta`, Python filter/sort/page. UI: `Lookup.tsx:54` (once per query string).

**GET /api/lookup/facets** (435-441) → `lookup_facets` (3019-3020) → `_positions_data` (cached). UI: `conditions.tsx:219` (once, mounted on Positions and Lookup).

**GET/POST /api/reward-weights** (456-470) → `reward_weights` / `set_reward_weights` (1075-1114): file `<TWDATA>/rules/reward_weights.json`; POST clears `_STAMPED` and kicks the warm loop. UI: `reward.tsx:7, 26`.

### A.6 Decisions

**GET /api/decisions** (713-732) → `decisions_page` (3493-3577): filters built on view aliases then string-replaced onto base tables (3528-3531); `SELECT COUNT(*) FROM taken t JOIN decisions d … LEFT JOIN campaigns c … LEFT JOIN actions a … [WHERE …]` (unfiltered path: `SELECT COUNT(*) FROM taken`, 0.03 s); page query `SELECT t.decision_id, t.ts, a.context_kind, a.context_id, a.action_type, a.action_key, t.executed, t.confirmed, t.counted, t.refusal, t.latency_ms, (t.decision_id*1048576 + t.offer_seq) offer_id, COALESCE(t.policy,d.policy) policy, c.campaign_key campaign_id, d.turn, d.n_offers FROM taken t JOIN decisions d … ORDER BY t.decision_id DESC LIMIT %s OFFSET %s`; then `SELECT offer_id, exploit, pct_global, rank, gnn_impact, gnn_rank, ggnn_score, ggnn_rank FROM action_offers WHERE decision_id IN (…) AND offer_id IN (…)` (view over offers ⋈ actions ⋈ offer_scores ⋈ offer_model_scores, pg_schema.py:185-200); `policy` filter does `SELECT DISTINCT policy FROM taken WHERE policy IS NOT NULL` (3500-3502); `search` uses `ILIKE` on `a.action_key`/`c.campaign_key` (3510-3512, seq scan). `decision_facets` (3581-3590): `SELECT DISTINCT action_type FROM actions WHERE … AND action_id IN (SELECT action_id FROM taken)` (0.37 s) + `SELECT DISTINCT policy FROM taken`. `labels.target_for` per row. No blobs. Page-bounded rows; count is whole-corpus. No cache. UI: `Decisions.tsx:174` (live, ≤200 per page).

**GET /api/decisions/{id}** (699-710) → `decision_detail` (3593-3642): `SELECT … FROM action_taken at JOIN decision_points dp ON dp.decision_id=at.decision_id WHERE at.decision_id=%s` (view `decision_points` **joins both campaign and world blobs** even though only `timings` is read); fallback query on `decision_points` alone (3602-3607); `SELECT offer_id, context_kind, context_id, action_type, action_key, exploit, pct_global, rank, gnn_impact, gnn_rank, ggnn_score, ggnn_rank FROM action_offers WHERE decision_id=%s ORDER BY COALESCE(rank,9999), offer_id`; **`SELECT context_kind, context_id, features FROM entity_snapshots WHERE decision_id=%s LIMIT 40` → every entity blob decoded whole and returned raw as `EntityState.features`** (3636-3641); `_phases` (3645-3658) decodes `timings` (`collect_ms, score_ms, roundtrip_ms`) and `timing` (`total_ms`). `decision_agreement` (4513-4544): adb `SELECT * FROM model_agreement WHERE decision_id=%s`. Bounded. No cache. UI: `DecisionDetail.tsx:123` (live).

**GET /api/decisions/actions** (648-655) → `actions_summary` (3662-3740): `SELECT action_id, SUM(tried), SUM(ok) FROM taken GROUP BY action_id` (68,276 rows, 0.08 s); `SELECT action_id, refusal, COUNT(*) FROM taken WHERE refusal IS NOT NULL AND … GROUP BY action_id, refusal`; `_action_types_for` (452-461) chunks of 900 `SELECT action_id, action_type FROM actions WHERE action_id IN (…)` (≈76 queries for 68k ids); `SELECT COALESCE(policy,'(unrecorded)') p, COUNT(*) FROM taken GROUP BY p`; `SELECT COUNT(*), SUM(attempted), SUM(confirmed) FROM taken`; `arms.arm_of/fell_back` grouping (`_by_arm` 143-153). Whole corpus. No cache. UI: `Decisions.tsx:265` (live).

**GET /api/decisions/menus** (676-683) → `menus` (3766-3809): `SELECT COUNT(*) FROM interrupts`; `SELECT kind, COUNT(*) FROM interrupts GROUP BY kind`; `SELECT COALESCE(policy,'(unrecorded)'), COUNT(*) FROM interrupts GROUP BY p`; `SELECT interrupt_id, ts, kind, root, campaign_id, turn, n_options, chosen, executed, confirmed, counted, refusal, latency_ms, policy, options_json FROM interrupt_decisions ORDER BY interrupt_id DESC LIMIT 60` (view **joins campaign/world/panel blobs it does not read**, pg_schema.py:238-251) with **`options_json` decoded** (`_options_of` 3432-3442 → option dict keys `exploit`, `gnn`, label from dict key or `label|key|option`); `_interrupt_coverage` (3746-3762): **`SELECT kind, options_json FROM interrupts ORDER BY interrupt_id` — every interrupt (43,392 rows, 0.08 s fetch) JSON-decoded per request**. Whole corpus. No cache. UI: `Decisions.tsx:362` (live).

**GET /api/decisions/timeline** (686-696) → `timeline` (3813-3853): `SELECT at.decision_id, at.ts, at.action_type, at.action_key, at.executed, at.confirmed, at.counted, at.refusal, at.timing, dp.campaign_id, dp.turn, dp.timings FROM action_taken at JOIN decision_points dp … ORDER BY at.decision_id DESC LIMIT 200` (view joins two unused blobs); decodes `timings`/`timing` via `_phases`. Bounded. No cache. UI: `Decisions.tsx:508` (live).

**GET /api/decisions/diplomacy** (658-673) → `model_versions` (4000-4034): adb `SELECT trial, generation, seg_from_ts, seg_to_ts, campaigns, corpus_decisions FROM model_generations ORDER BY seg_from_ts`; `diplomacy_mix` (4085-4115) → `_diplomacy_partials` (4053-4081): `SELECT action_id, action_key FROM actions WHERE action_type='diplomacy'` + `SELECT campaign_id, action_id, COALESCE(policy,'(unrecorded)') p, MIN(ts), SUM(attempted), SUM(confirmed) FROM taken WHERE action_id IN (SELECT action_id FROM actions WHERE action_type='diplomacy') GROUP BY campaign_id, action_id, p`; version windows applied in Python (`_version_windows` 4037-4046). Whole corpus. No cache. UI: `Decisions.tsx:606` (live).

### A.7 Models

**GET /api/models** (810-813) → `_model_cards` (3877-3949) and `_fit_config` (3976-3996): read `meta.json` under `common.MODEL_GLOBAL`, `MODEL_INTERRUPT`, `MODEL_MAPGRAPH_GREEDY` (3856-3869); `_graph_schema_hash` imports `advisor.mapgraph.schema` (3959-3964). No DB. UI: `Models.tsx:86` (live).

**GET /api/models/forcing** (735-748) → `model_versions` (adb) + `forcing` (4119-4152): `SELECT COALESCE(policy,'(unrecorded)') p, action_id, COUNT(*) n FROM taken [WHERE (ts >= %s AND ts < %s) …] GROUP BY p, action_id`, `_action_types_for` chunks, `arms.TRAINABLE`, Wilson CI. Whole corpus. No cache. UI: `Models.tsx:145` (live).

**GET /api/models/agreement** (751-753) → `agreement_page` (4294-4371): `_freshness("model_agreement")` (4165-4203; adb `analytics_state` + corpus `SELECT MAX(decision_id) FROM decisions`); `pair_options` (4464-4475) adb `SELECT pair, comparable FROM agreement_summary WHERE scope='all'`; adb `SELECT * FROM agreement_summary WHERE scope='all' AND pair=%s`; `SELECT * FROM agreement_breakdown WHERE dim='arm' AND pair=%s ORDER BY decisions DESC`; `SELECT * FROM agreement_hist WHERE pair=%s ORDER BY bucket`; `agreement_matrices` (4219-4262) adb `SELECT pair, rho_median, comparable FROM agreement_summary WHERE scope='all'` + `SELECT pair, seq, rho_median, decisions, gate, trial, generation, from_ts, retrained FROM agreement_series WHERE axis='generation' ORDER BY pair, seq`. Precomputed only. UI: `Models.tsx:237` (live).

**GET /api/models/agreement/series** (756-759) → `agreement_series` (4386-4457): adb `SELECT * FROM agreement_series WHERE axis=%s AND pair=%s ORDER BY seq`, `SELECT * FROM agreement_summary WHERE scope='all' AND pair=%s`, `SELECT * FROM agreement_series WHERE axis='generation' AND pair=%s ORDER BY seq`. UI: `Models.tsx:604` (live).

**GET /api/models/agreement/breakdown** (762-766) → `agreement_breakdown` (4461-4491): adb `SELECT * FROM agreement_breakdown WHERE dim=%s AND pair=%s ORDER BY decisions DESC`. **No UI caller.**

**GET /api/models/correlations** (779-791) → `correlations` (4564-4613): `gains_all` (whole), `_campaign_keys`, `SELECT COALESCE(policy,'(unrecorded)') arm, campaign_id, COUNT(*) FROM taken GROUP BY arm, campaign_id` and the same over `interrupts`; Pearson in Python. Whole corpus. No cache. UI: `Models.tsx:757` (live).

**GET /api/models/training** (794-807) → `_training_history` (4638-4711): reads the last 12 `<RUNS_ROOT>/session_*.json` files; `trials` (4805-4815) → `_trials` (4819-4862) → `metrics_db.trials()` (metrics_db.py:78-98: `SELECT payload,snapshots,ts FROM metrics.trials ORDER BY ts, trial`, JSON payload decoded), `metrics_db.live_trials`; `_campaign_arm_shares` (4722-4739) `SELECT arm, campaign_id, COUNT(*) FROM taken GROUP BY arm, campaign_id`; `_campaign_settlement_growth` (4743-4749) **runs `CG.trajectories(con)` = `campaign_growth.TRAJECTORY_SQL` (whole-decisions GROUP BY with two self-joins, campaign_growth.py:8-32) live per request instead of reading the `campaign_growth` analytics table**; `campaign_reward_series` (4765-4786): `SELECT campaign_id, campaign_key FROM campaigns` + `gains_all`, weights from `advisor/base_model.TARGET_WEIGHTS` (`_W`, 4755-4760) — a second reward formula distinct from `reward_weights()`. Whole corpus. No cache. UI: `Models.tsx:801` (live).

### A.8 Selector (UCB picks)

**GET /api/campaigns/picks** (473-489) → `gains_all`, `ucb_context`, `ucb_pick_series` (5101-5166): `_start_leaders`; `SELECT pick_id, rank, score FROM ucb_pick_rows WHERE rank <= 2` (7,706 rows, 0.13 s); `SELECT pick_id, ARRAY_AGG(n) FROM ucb_pick_rows GROUP BY pick_id` (0.11 s); `pick_campaigns`; `_camps_meta` (5078-5097) `SELECT c.campaign_key, c.turns, MAX(d.ts) FROM campaigns c JOIN decisions d … GROUP BY …` + `outcome_join`; `SELECT * FROM ucb_picks ORDER BY pick_id` (3,853 rows); per-pick Python (`PULSE=50` window). `ucb_window_edges` (5173-5200), `ucb_tiles` (5211-5240), `ucb_picks` slice (5203-5207; `limit ≤ 5000`, cursor by `pick_id`). No blobs. Whole corpus. No cache. UI: `Selector.tsx:88` and `:134` (both live — two components fetch the same URL).

**GET /api/campaigns/picks/{id}** (492-502) → `ucb_pick_rows` (5244-5270): **calls `ucb_pick_series(con)` (the whole series above) to find one head row** (5245), then `SELECT * FROM ucb_pick_rows WHERE pick_id=%s ORDER BY rank`. UI: `Selector.tsx:93` (live, when a pick is selected).

**GET /api/campaigns/matrix** (505-536) → `matrix` (3399-3429): `SELECT campaign_id, action_id atype, SUM(tried), SUM(ok), SUM(COALESCE(latency_ms,0)) FROM taken GROUP BY campaign_id, action_id` (159,274 rows, 0.16 s) or `… FROM interrupts GROUP BY campaign_id, kind`; `_faction_of` (447-449); `_action_types_for` chunks. **No UI caller** (`Campaigns.tsx:25` maps the legacy `matrix` view name to `starts`).

### A.9 Infra / analytics / control

**GET /api/infra** (833-851) → `session_log_tail(14)` (5054-5066, last 256 KiB of the session log), `activity` (4921-4936, mtimes of session log, `trace.jsonl`, `decisions_stream.jsonl`), `proc.services`, `_launch_defaults` (816-830, `run_config.RUN`). No DB. UI: `Infra.tsx:216` (live), `Status.tsx:49` (live).
**POST /api/infra/kill|launch|coldstart** (854-866) → `runctl` (proc.py:109-167; `launch` refuses on dirty git / unbumped VERSION, writes `app.experiments/segments` through `runctl._record_launch → open_segment`, runctl.py:252-256). UI: `Infra.tsx:66, 226`.
**GET /api/analytics** (769-771) → `analytics_status` (4495-4510): adb `SELECT * FROM analytics_state ORDER BY tenant` + `_freshness` per tenant (each does `SELECT * FROM analytics_state WHERE tenant=%s` again and, for `model_agreement`, `SELECT MAX(decision_id) FROM decisions`). **No UI caller.**
**POST /api/analytics/rebuild** (774-776) → `runctl.rebuild_analytics` (kills and respawns `python -m analytics.runner --rebuild`, runctl.py:139-145). **No UI caller.**

### A.10 Summary lists

**Queries that decode blobs / JSON columns in Python** (the de-blob-sensitive set): `current` (campaign blob, 207); `campaign_state` (entity campaign + lord blobs, 255-285); `_start_snapshots`→`start_skills` (lord/hero blobs, 1910); `_campaign_chars`→`campaign_skills`, `campaign_items` (lord/hero blobs, 3258); `_positions_data`→`positions_page`, `campaign_lookup`, `lookup_facets` (**every** campaign blob, 2850); `decision_detail` (≤40 entity blobs passed through raw, 3641); `campaign_buildings` and `campaign_skills` (`actions.params`, 2107, 3294); `collect_timing`, `_phases`, `timeline` (`decisions.timings`), `cycle_timing`, `_phases` (`taken.timing`); `menus`/`_interrupt_coverage` (`interrupts.options_json`, all rows); `diplomacy_tail` (`diplomacy_events.payload`); `_trials` (`metrics.trials.payload`); `campaign_verdict` (regex over `ended_because` text). The API never reads `decisions.world_blob`, `interrupts.{campaign,world,panel}_blob`, `taken.diagnostics`, `postmortems.payload` (postmortems reach it only via `campaign_endings`) or `rpc_*`.

**Whole-corpus scans per request (no cache)**: `/api/campaigns/starts` (campaign_rows rebuild + turn_open + start_counts), `/api/campaigns/starts/{m}/{f}` (gains_all + ucb_context twice via start_head), `…/performance`, `…/campaigns`, `/api/campaigns/picks` and `/picks/{id}` (ucb_pick_series), `/api/items`, `/api/items/swaps`, `/api/{family}` catalog index, `/api/choices/*` (decision_features twice), `/api/overtime/*` (taken seq scan), `/api/decisions` count + facets, `/api/decisions/actions`, `/api/decisions/menus` (43k options_json decodes), `/api/decisions/diplomacy`, `/api/models/forcing`, `/api/models/correlations`, `/api/models/training` (TRAJECTORY_SQL + taken GROUP BY), `/api/campaigns/matrix`. Cached whole-corpus builds: `campaign_rows` (both keys), `_camp_meta`, `_positions_data`, `_hist_map` ×5 — rebuilt by the warm loop every 120 s regardless of whether the corpus changed (`force=True`, 1164-1168).

---

## B. Consumed-key matrix (API side; analytics/utilities in C and D)

| Blob / JSON column (role) | Key path read | Consumer file:line | Becomes |
|---|---|---|---|
| `decisions.campaign_blob` (per-decision campaign scalars, 25 keys measured) | `settlements`, `power_rank`, `lord_level` | queries.py:218-220 | `Current.settlements/power_rank/lord_level` |
| same | `hero_type_counts{k:v}` (sum → heroes; each `k` → `hero:<k>` sparse) | queries.py:2851, 2859, 2873-2877 | `PositionFacetOption` hero_types; `POSITION_SCALARS["heroes"]`; condition `hero:<k>` |
| same | `is_researching`, `ll_wounded` (truthy → bit flags 1,2) | queries.py:2852-2853, 2801 | condition `flag:is_researching`, `flag:ll_wounded` |
| same | `treasury`, `armies` | queries.py:2858 | `POSITION_SCALARS["treasury"]`, `["armies"]` |
| same | `resources{k:v}` → `res:<k>` | queries.py:2867-2872 | `PositionFacetOption` resources (`labels.pooled_resource_name`), condition `res:<k>` |
| entity `campaign` features (32 keys measured) | `current_research` | queries.py:256-259 | `CampaignStatePage.research` |
| same | `equipped_all[].key`, `anc_pool[].key` | queries.py:260-267 | `CampaignStatePage.equipped/pool` |
| entity `lord` features | `is_leader`, `region`, `hp`, `rank`, `wounded`, `skill_points` | queries.py:274-285 | `LordState` |
| entity `lord`/`hero` features | `subtype`, `rank`, `skill_points`, `skills[].key/.level/.tier/.total_levels` | queries.py:1924-1938, 1965-1972 | `StartCharacterRow`, `SkillRow` (took/avg_ranks/tier/max_ranks), `StartSkills.avg_rank/avg_unspent` |
| same | `subtype`, `rank`, `skill_points`, `equipped[].key/.name` | queries.py:3261-3270 | `CampaignCharacter` (label, rank, points_unspent, slots, wearing) |
| any entity features (≤40 per decision) | whole dict | queries.py:3637-3641 | `EntityState.features` |
| `actions.params` (building family) | `building_key`, `is_upgrade`, `cost`, `repair_cost`, `region`, `refund` | queries.py:2107-2137 | `CampaignBuildingRow.kind/key/cost/region` |
| `actions.params` (skills) | `level`, `total_levels` | queries.py:3294-3300 | `CampaignSkillRow.rank/max_ranks` |
| `decisions.timings` | `collect_ms, roundtrip_ms, score_ms, store_ms, trace_ms, housekeep_ms, pickup_lag_ms` | queries.py:396-397 | `TimingRow` (collect_timing) |
| same | `collect_ms, score_ms, roundtrip_ms` | queries.py:3646-3650 | `PhaseSpan` collect/queue/score |
| `taken.timing` | `snapshot_ms, execute_ms, confirm_ms, gates_ms, total_ms` | queries.py:424 | `TimingRow` (cycle_timing) |
| same | `total_ms` | queries.py:3651 | `PhaseSpan` verify |
| `interrupts.options_json` | dict `{label: {…}}` or list `[{label|key|option, …}]`; option `exploit`, `gnn` | queries.py:3433-3442, 3754-3757, 3791-3793 | `InterruptOption`, `ArmCoverage.scored/compared/agree` |
| `diplomacy_events.payload` | `kind, turn, deal_score, success_chance, outcome, result, channel, target, faction, standing, terms, speech` | queries.py:4894-4910 | `DiploEvent` (measured: only `channel, faction, terms, speech` exist in payloads) |
| `campaign_endings` row (analytics, from `postmortems.payload`) | `outcome, when_text, error, verdict, because` (+ `growth` always `{}`) | queries.py:180-189, 552-562, 63-85, 5085-5092 | `CampaignRow.outcome/outcome_state/ended_when/ended_because/suspicious`, `CampaignsPage.unjoined` |
| `metrics.trials.payload` | `trial, ts, running, campaign_uuids, corpus_at_train.rows, settlements.{mean,total,campaigns_that_gained,campaigns_that_lost,campaigns_measured}, lord_level.mean, timing.{s_per_campaign,s_per_turn}, strategies, interrupt_strategies, ruleset, campaigns, baseline, turns_per_campaign, outcomes, _snapshots` | queries.py:4822-4859 | `TrialRow` |
| `session_*.json` | `campaigns[].{started, retrain{fit.model.{val_rmse,val_r2,val_rows,best_iteration}, rows, campaigns, n_decisions, seconds, mae_in_sample}, retrain_interrupt{rows,screens}, retrain_gnn{rows, fit{val_listwise_nll,val_value_mse,epochs_run,device,stopped_by}, seconds}, retrain_greedy_gnn{rows, fit{val_mse,val_r2,…}, seconds}}` | queries.py:4652-4709 | `TrainingEvent` |
| model `meta.json` | `rows, campaigns, fit{val_mse,val_r2,val_rows,val_listwise_nll,val_value_mse,epochs_run,stopped_by,device}, y_sd, schema_hash, schema_version, fit.model{val_rmse,val_r2,val_rows}, screens, cfg, short_horizon, short_weight, pred_lo, pred_hi, target, num, cat` | queries.py:3886-3943, 3982-3995 | `ModelCard.rows`, `FitConfigRow` |
| `rules/reward_weights.json` | `weights.{settlements,lord_levels,allies,vassals}` | queries.py:1085-1090 | `RewardWeightsPage.weights`, `_FACT_CAMPS` reward |
| `rules/ucb_adjust.json` | `adjustments{faction: delta}` | ucb_stats.py:24-31 | `StartRow.adjust`, `ucb_context["adjust"]` |
| `launcher/startable_factions.json` | `cultures[]`, `maps{k:{label}}` | ident.py:35-59 | `Ident.culture`, `Ident.label` for maps |

---

## C. Analytics layer

### C.0 Driver (analytics/store.py, runner.py)

- Schema `analytics`; spine table `analytics_state(tenant, formula_version, input_version, watermark, rows, source_rows, source_floor, built_ts, last_run_ts, last_run_seconds, last_folded, last_error, last_error_ts)` (store.py:13-30). Writer connection is `autocommit=True` with explicit `BEGIN`/`COMMIT`/`ROLLBACK` strings (store.py:41, 143-153, 169-193); `CREATE SCHEMA` + spine DDL on every non-readonly connect (44-46).
- `ensure()` (131-154): runs tenant `DDL`; `InvalidTableDefinition` → rebuild; otherwise `rebuild_reason()` (96-128) triggers a rebuild when: stored `formula_version` ≠ `tenant.FORMULA_VERSION`; `input_version` ≠ sum of dependencies' formula versions (`_dep_version` 89-93); `COUNT(*)` of `tenant.NAME` table fails (-1, "table missing") or ≠ `state.rows`; `watermark > safe_hi` ("went backwards"); `source_stats(src, watermark)` count ≠ `source_rows` or floor ≠ `source_floor`. Rebuild = `DROP TABLE` each of `tenant.TABLES` (default `(NAME,)`), re-run DDL, `BEGIN; DELETE FROM NAME; _set(watermark=0, rows=0, …); COMMIT` (140-153).
- `run_tenant()` (157-195): `lo = watermark` (0 if `REBUILD_EVERY_PASS`), `hi = safe_hi()`; if `hi <= lo` only `last_run_ts/last_folded=0` are written; else `BEGIN`, `tenant.step(src, an, lo, hi) → (watermark, written)`, `rows = COUNT(*)`, `stats = source_stats(src, watermark)`; **source_rows check**: if `source_rows is not None and source_rows != rows` → `RuntimeError("… rows were dropped")`; any exception → `ROLLBACK`, state gets `last_error` (truncated 500) and `last_run_ts`, then re-raised (188-193). On success `COMMIT` and state update incl. `last_run_seconds`. Note `_table_rows` issues its own `ROLLBACK` on a failed COUNT inside the open transaction (81-86).
- Dependency order via `order_tenants` (198-218) from `DEPENDS_ON`. Runner (runner.py): `POLL_S = 5.0`, `HEARTBEAT_S = 300`, `BUSY_BACKOFF = (1,2,5,15,30)` (17-19); `one_pass` runs every tenant, `psycopg.OperationalError` → BUSY, other exceptions logged and the pass continues (31-48); `--rebuild` deletes each tenant table's rows and its state row (51-62); `--once` prints state and exits (89-97). Started by `runctl.start_analytics` (runctl.py:133-136, called from `runctl.up` when `with_ui`, 208-210) and by `POST /api/analytics/rebuild`; `proc.services` looks for `analytics.runner` in the process list (proc.py:78). Measured `analytics_state.last_run_ts` for every tenant = 09-03 20:43:47-59 (the runner has not run since; no `last_error`).
- Registration: `tenants.py:7-12` — FACTS = `model_agreement, campaign_growth, model_generations, decision_features, acquisitions, campaign_endings, item_events, game_turn`; ROLLUPS = `agreement_summary, agreement_series, agreement_breakdown, growth_summary`.

### C.1 Tenants

**model_agreement** (analytics/model_agreement.py) — FORMULA_VERSION 3 (18); DDL 29-67 (table `model_agreement`, PK `(decision_id, pair)`, 4 indexes). `safe_hi = MAX(decision_id) − 1` (95-97); `source_stats = (COUNT(decisions ≤ hi) × 3 pairs, MIN(decision_id))` (100-103) → the rows check enforces exactly 3 rows per decision. Source SQL (72-79): `SELECT d.decision_id, d.ts, d.turn, d.campaign_id, d.n_offers, t.offer_seq taken_seq, COALESCE(t.policy,d.policy) policy, a.action_type, a.context_kind FROM decisions d LEFT JOIN taken t … LEFT JOIN actions a … WHERE d.decision_id > lo AND ≤ hi ORDER BY d.decision_id` in 2,000-id chunks (199-220), plus `_score_vectors` (112-125): `SELECT decision_id, offer_seq, rank, gnn_rank FROM offer_scores WHERE …` and `SELECT decision_id, offer_seq, model, rank FROM offer_model_scores WHERE … AND model = ANY(['greedy_gnn'])`. **No blobs.** Grain: one row per (decision, pair) with Spearman/Kendall/RBO/top-k from `analytics/metrics.py` (`MIN_N = 3`, metrics.py:6). Consumers: queries.py:4516 (`decision_agreement`, `/api/decisions/{id}`), queries.py:4554 (`rho_for` — **no caller**), rollups below. Measured 620,712 rows / 243 MB, watermark 206,942.

**campaign_growth** (analytics/campaign_growth.py) — FORMULA_VERSION 3, `REBUILD_EVERY_PASS = True` (14-17); DDL 19-40. `safe_hi = MAX(campaign_id)`; `source_stats = (COUNT(campaigns ≤ hi), MIN(campaign_id))` → rows must equal campaigns. Source: `G.trajectories(src)` = `campaign_growth.TRAJECTORY_SQL` (root campaign_growth.py:8-32: whole `decisions` GROUP BY campaign_id + joins on first/last decision), `SELECT campaign_id, MAX(turn) FROM decisions GROUP BY campaign_id`, `SELECT campaign_id, campaign_key, faction, outcome FROM campaigns WHERE campaign_id <= hi`. No blobs. Grain: one row per campaign; upsert of all rows every 5 s pass (measured `last_run_seconds` 0.27 s, 4,827 rows). Consumers: queries.py:491 (`campaign_rows` via `CG.enrich`), growth_rollup.py:44.

**model_generations** (analytics/generations.py) — FORMULA_VERSION 1, `REBUILD_EVERY_PASS` (14-17); DDL 19-36. Source is **not the corpus**: `metrics_db.trials()` (`metrics.trials` payload JSON, keys `run_dirs, started, ts, trial, generation, session, feature_version, corpus_at_train.n_decisions, campaigns`, 50-86) filtered to rows whose `run_dirs` contain `common.RUN_DIR`; `safe_hi = len(trials)`; `source_stats = None`; step deletes and reinserts all (100-114). Grain: one row per trial with clipped `[seg_from_ts, seg_to_ts)` windows. Consumers: queries.py:4003 (`model_versions` → diplomacy/forcing/correlations version pickers), agreement_rollup.py:145, 191; `generations.generation_gains` (126-158, reads `model_generations` + `campaign_gains`) has **no consumer except its own `__main__`** (177-179). Measured 152 rows, 33 retrained.

**decision_features** (analytics/state_facts.py `_DecisionFeatures`) — FORMULA_VERSION 1 (83); TABLES `decision_features, decision_resources, decision_heroes`; DDL 15-40. `safe_hi = MAX(decision_id)`; `source_stats = (COUNT(decisions ≤ hi), MIN)` → rows must equal decisions. Source (100-105): `SELECT d.decision_id, d.campaign_id, d.ts, d.turn, d.settlements, d.income, d.power_rank, d.lord_level, d.allies, d.vassals, b.z FROM decisions d LEFT JOIN blobs b ON b.blob_id = d.campaign_blob WHERE … ORDER BY d.decision_id` in 25,000-id steps (`DF_STEP`, 11). **Parses the campaign blob**: `hero_type_counts`, `treasury`, `armies`, `is_researching`, `ll_wounded`, `resources` (107-126) — the same keys `_positions_data` re-decodes in the API. Grain: one `decision_features` row per decision; one `decision_resources` row per (decision, resource key); one `decision_heroes` row per (decision, hero type). Consumers: `decision_features` → queries.py:2300-2315 (`_fact_turn_states`, `/api/choices/*` only); **`decision_resources` (156 MB) and `decision_heroes` (23 MB): no consumer found** in the tree incl. ui/src.

**acquisitions** (state_facts.py `_Acquisitions`) — FORMULA_VERSION 3 (252); DDL 42-53 (PK `(campaign_id, family, key, ctx)`). `safe_hi = MAX(decision_id)`; `source_stats = (None, MIN(decision_id))` (no rows check). Source in 5,000-id steps (`ACQ_STEP`): `SELECT decision_id, campaign_id, turn FROM decisions WHERE …`; **`SELECT e.decision_id, e.context_kind, e.context_id, e.features_blob, b.z FROM entities e JOIN blobs b … WHERE … AND e.context_kind = ANY(['campaign','lord','hero','province']) ORDER BY e.decision_id`** (296-303); **`SELECT d.decision_id, d.world_blob, b.z FROM decisions d JOIN blobs b ON b.blob_id = d.world_blob WHERE …`** (311-315). Blob keys parsed (147-227): campaign `tech[].key/.researched/.can_research`, `anc_pool[].key`, `equipped_all[].key`; lord/hero `subtype|agent_type`, `is_agent`, `skills[].key/.level/.status`, `equipped[].key`, `traits[].key/.level`, `trait_progress{key: pts}`; province `region`, `built{}` (value or `.key`), `building_now{}`, `buildable[].key/.active`; world `settlements[].region`. Blob parse memo `_FACT_MEMO` keyed `(blob_id, kind)` cleared above 200k entries (230-246). Grain: one row per (campaign, family, key, ctx) with first-seen / acquired turn+decision and max ranks; families `research, items, skills, traits, building, settlement`. Consumers: queries.py:241-245, 1538, 1603-1606, 1755-1758, 1774-1777, 1817-1819, 1854-1860, 1977-1983, 2049-2056, 2185-2189, 2249-2252, 2269-2274, 2408-2413, 2461-2467, 2599-2602, 2639-2641, 2745-2755, 2774-2790, 2923-2927 (the catalog, start, items, positions-history and campaign-state views). Measured 255,267 rows / 61 MB.

**campaign_endings** (state_facts.py `_CampaignEndings`) — FORMULA_VERSION 2 (365); DDL 55-60. `safe_hi = MAX(postmortem_id)`; `source_stats = (None, MIN(postmortem_id))`. Source: `SELECT postmortem_id, campaign_key, ts, payload FROM postmortems WHERE … ORDER BY postmortem_id` in 50,000 steps; **parses `postmortems.payload`**: `campaign_key, ts, faction, outcome, when, error, plausibility.verdict, growth.{reason, turn, metrics{label,then,now,window}, min_gain}` (`_because` 339-359, 381-395). Grain: one row per campaign_key (upsert; measured 1 campaign_key has >1 postmortem). Consumers: queries.py:180-182 (`outcome_join` → campaigns list/detail, starts, picks). Measured 4,392 rows.

**item_events** (analytics/item_events.py) — FORMULA_VERSION 2 (50); TABLES `item_events, item_event_state`; DDL 13-26. `safe_hi = MAX(decision_id)`; `source_stats = (None, MIN)`. Source in 5,000 steps: decisions ids, **`SELECT e.decision_id, e.context_kind, e.context_id, b.z FROM entities e JOIN blobs b … WHERE … AND e.context_kind = ANY(['lord','hero']) ORDER BY e.decision_id`** (80-86); parses `equipped[].key`, `subtype|agent_type` (39-44, 95-96); diffs against `item_event_state.keys` (JSON list) per (campaign, ctx) (74-78, 112-120). Grain: one `on`/`off` event per (campaign, character ctx, item key, decision). Consumers: queries.py:1575-1579, 1652-1654, 1798-1806. Measured 19,250 events.

**game_turn** (analytics/gamestate.py) — FORMULA_VERSION 1 (10); TABLES = 14 tables (16-20); DDL 22-123. `safe_hi = MAX(decision_id)`; `source_stats = None`. `_pending` (184-199): **`SELECT campaign_id, turn, MAX(decision_id) FROM decisions WHERE decision_id <= hi AND turn IS NOT NULL GROUP BY campaign_id, turn ORDER BY MAX(decision_id)` — whole-corpus GROUP BY every pass**, compared against `game_turn_built`, up to `TURNS_PER_PASS = 400` per pass. Per turn `_load` (153-181) reads the decision's campaign blob, world blob and **every entity blob of that decision** (`SELECT e.context_kind, e.context_id, b.z FROM entities e JOIN blobs b … WHERE e.decision_id=%s`), then `_clear` (DELETE from 13 tables) and `_emit` (225-390). Keys parsed: campaign blob `faction, campaign_map, leader, difficulty, treasury, income, settlements, allies, vassals, lord_level, armies, power_rank, defeated, resources{}`; entity campaign `tech[].{key,researched,can_research,cost}`, `current_research`, `missions[].{mission,status,turns_remaining,is_quest,is_victory,pending,issuer}`, `equipped_all`, `anc_pool[].key`, `research_points`; lord/hero `subtype, agent_type, rank, xp, skill_points, region, stance, units, hp, upkeep, wounded, is_leader, garrisoned, x, y, authority, loyalty, skills[].{key,level,status,tier,total_levels}, trait_progress{}, traits[], equipped[].key, armory[], unit_cards[].{key,category,strength_pct,xp}`; province `province, public_order, corruption{}, growth_per_turn, income, gross_income, settlement_level, buildings, free_slots, max_slots, has_port, has_walls, slot_states[].{index,key,queued_key,damaged,ruined}`; world `relations[].{faction,standing,at_war,allied,trade,nap,mil_access,mil_ally,def_ally,their_vassal,our_master}`, `regions[].{region,owner,province,capital,abandoned,x,y}`, `armies[].{cqi,subtype,units,hp,stance,region,x,y}`, `hostiles[].{cqi,kind,faction,…}`. Grain: one `game_turn` row per (campaign, turn) at the turn's last decision, plus child rows per tech/character/skill/trait/item/region/building slot/relation/army/unit/mission/resource. Side effect: `workspace.sync_starts()` in a bare `try/except: pass` whenever `todo` is non-empty (398-402) — see §E. **Consumers: none found** for any of the 14 `game_turn*` tables (grep over the whole tree incl. ui/src returns only gamestate.py). Measured ≈870 MB across `game_turn*` in `analytics` (largest `game_turn_skill` 404 MB, `game_turn_research` 155 MB, `game_turn_region` 101 MB). DB also holds `game_turn_recruit` (87 MB), `game_turn_war` (14 MB), `game_turn_slot` — **not in `gamestate.TABLES`, created by no code in the tree** (leftovers; names/sizes only).

**agreement_summary / agreement_series / agreement_breakdown** (analytics/agreement_rollup.py) — FORMULA_VERSION 4 (14); `DEPENDS_ON = ("model_agreement", "model_generations")` for summary/series (91, 185), `("model_agreement",)` for breakdown (57); `safe_hi = model_agreement watermark` (59-60); `source_stats = None`. Each step `DELETE`s and rebuilds from `model_agreement` (`WHERE pair=%s AND status='ok' ORDER BY decision_id`, whole table per pair: 98-104, 193-195, 271-275) — measured 2.5-6.2 s per rollup per pass when folding. DDL 66-82 (`agreement_summary`, `agreement_hist`), 153-163, 248-255. Grain: summary one row per pair (`scope='all'`), hist 20 bins per pair, series one row per (pair, axis window|generation, seq) with `bucket_size = max(50, ⌈comparable/40⌉)` (46-51), breakdown one row per (pair, dim ∈ arm|action_type|context_kind, key). Consumers: queries.py:3467, 4223-4229, 4306, 4363-4368, 4394-4397, 4431-4432, 4468-4469 (`/api/models/agreement*`). Note `agreement_breakdown` `dim='arm'` is read by `/api/models/agreement` (4363); the other dims are served only by the uncalled `/breakdown` route.

**growth_summary** (analytics/growth_rollup.py) — FORMULA_VERSION 1; `DEPENDS_ON = ("campaign_growth",)`; DDL 15-26; rebuilt from `SELECT * FROM campaign_growth` each pass (43-44). Grain: one row (`scope='all'`). **No consumer found** (grep: only growth_rollup.py).

---

## D. Utility scripts

| Script | Reads | Writes | When it runs |
|---|---|---|---|
| **retention.py** | Filesystem only: sweep classes under `<TWDATA>` (23-41), `CURRENT_SESSION_LOG` pointer (70-76), sizes of `ROLL_FILES` (47-53), `DEAD` sqlite leftovers (55-67) | Deletes aged files/empty dirs (`sweep`, 90-153, 6 h gate via `LOGS_SERVICES/retention_last_sweep.txt`, 13-15, 92-98), renames >512 MB streams to `.rolled` or truncates (156-185), `purge_dead` (188-231). No DB. | `advisor/session.py:498` `boundary_pass` at every campaign boundary; `runctl.harness_tick` → `sweep(apply=True)` every `HARNESS_EVERY_S = 300` s (runctl.py:275, 374-378); manual `python retention.py [--apply] [--purge-dead]` (239-246, `force=True`). |
| **metrics_db.py** | `metrics.trials` (`trials()` 78-98: `SELECT payload,snapshots,ts FROM trials ORDER BY ts, trial`; `trial_ids()` 101-111) | DDL `metrics.trials`, `metrics.trials_archive` executed on every non-readonly `connect()` (11-28); `write_trial` upsert (31-45); `prune_unmatched` moves rows to `trials_archive` and deletes (48-76) | Writers: `advisor/session.py:1013` (`write_trial`), `:1004` (`prune_unmatched`), `:971` (`trial_ids`). Readers: queries.py:4807, 4821; analytics/generations.py:51, 53, 91 (every 5 s pass). Measured 152 trials / 149 archived. |
| **ucb_stats.py** | `rules/ucb_adjust.json` (16-31); `window_rewards` (44-51): `SELECT campaign_map, faction, settlements_gained + levels_gained FROM (SELECT … FROM campaign_gains ORDER BY first_ts DESC LIMIT %s)` | nothing | `window_rewards` from `advisor/session.py:125` (selector); the API uses only the pure functions (`start_stats`, `window_blend`, `score`, `blend`, `gini`, `load_adjustments`, queries.py:704-719, 920, 5164). `WINDOW = 1000`, `MIN_PLAYS = 2` (9-10). |
| **campaign_growth.py** (root) | `TRAJECTORY_SQL` (8-34): `decisions` GROUP BY campaign_id + two self-joins + `campaigns`; `TRAJECTORY_SQL_ONE` (35-36) | nothing | `trajectories()` from analytics/campaign_growth.py:67 (every 5 s pass) and queries.py:4745 (per `/api/models/training`); `TRAJECTORY_SQL_ONE` from advisor/session.py:786; `enrich` from queries.py:490, growth_rollup.py:44. |
| **doctor.py** | `common` paths, `config.toml`, tool presence (27-67) | stdout only | Manual (`config.example.toml:8`). |
| **debugging/timeline.py** | Files: `session_*.log/.err`, `manager_*/ui_*/analytics_*/harness.log`, run-dir jsonl streams, dev streams, `unhandled_screens.jsonl`, screen dumps (352-398); DB `from_store` (223-315): `decisions` (+`timings` JSON: `t_request, t_received, roundtrip_ms, pickup_lag_ms, collect_ms, score_ms, store_ms, housekeep_ms, housekeep_parts`), `taken` (+`timing` JSON `execute_ms, confirm_ms, snapshot_ms`; `diagnostics.stderr` lines), `interrupts`, `rpc_requests` (`payload.decision_id, pick{action_type,key,policy,score,rank}, scores[]`), `rpc_responses`, `postmortems`, `diplomacy_events`, `ucb_picks` — all `WHERE ts BETWEEN t0 AND t1` (`ix_dec_ts`, `ix_taken_ts` exist; `interrupts/rpc_*/postmortems/diplomacy_events.ts` have no index in pg_schema.py:145-160) | `<TWDATA>/debug/timeline_<stamp>.txt` (434-463) | Manual (debugging/README.md:11); output swept by retention class `debug_timelines` after 3 days (retention.py:32). |
| **decisions/cco_audit.py** | `common.CCO_TSV` catalogue (33-43), Python sources of `decisions.collect` and `launcher.cco_actions` via `inspect.getsource` (361-373, 401-412), `bus/mod/twcontrol.lua`, `twstate.lua` (29-31, 

433-442) | stdout only | Manual: `python decisions/cco_audit.py [--verbose] [--tsv PATH] [--selftest]` (504-514). No DB. |
| **decisions/coverage.py** | `decision_points.campaign` (120), `entity_snapshots.features` per `context_kind` (121-124), `action_offers.params` per `action_type` (125-129), `decision_points.world` (132-140) — all blob-backed views, every key of every JSON scanned; sampling only by `MOD(decision_id, step)` when `--sample` is given (76-88); `survivor_report` (110-113): `SELECT action_type, COUNT(*) FROM action_offers GROUP BY action_type` (24.8 M-row view join); imports `advisor/features.py` and `advisor.mapgraph.schema` (91-107) | stdout only; `--selftest` writes through `decisions.pgtest.fresh()/drop()` and `DecisionStore` (187-224) | Manual (`--sample`, `--min-rows`, `--selftest`, 227-241). |
| **decisions/cycle_audit.py** | `taken ⋈ decisions`: `OVERLAP` (21-34, casts `taken.timing` to jsonb for `total_ms`, `LEAD()` window), `ACTED_PAST` (36-42), `STALE_AWAITING` (44-51), `to_regclass('public.taken')` (61) | stdout only | Manual (86-88); skips when PG unreachable. |
| **decisions/dilemma_audit.py** | `DecisionStore(run_dir).interrupt_rows()` (decisions/store.py:739-766): every `interrupts` row **with campaign, world and panel blobs decoded** (43,392 rows); reads `screen`, `options{}.dilemma_id/option_id/text`, `chosen` (26-50); run list from `common.run_dbs` (common.py:198-206, returns one synthetic path if `public.decisions` exists) | stdout only | Manual (82-83). |
| **ui_docshots.py** | HTTP: `/api/health`, `/api/dash/frame`, `/api/dash/campaign/{key}/turns`, `/api/dash/campaign/{key}/position`, `/api/dash/records` (28-59) | `docs/ui/*.png` (12, 121-123) | Manual; **cannot run**: no `/api/dash/*` route exists in app.py (the SPA catch-all at app.py:910-922 returns `index.html`, so `json.load` at line 37 fails). |
| **analytics/health_extract.py** | `decisions ⋈ collector_versions` (24-30), `campaigns ⋈ decisions ⋈ collector_versions` (33-44), `taken ⋈ decisions ⋈ collector_versions ⋈ actions` with `percentile_cont` (47-61, 64-73), `interrupts` + `LATERAL` last decision per campaign (76-89), `postmortems` (+`payload` keys `code_version, outcome, plausibility.verdict, seconds, turns_played, error, ended_by, wh3_running`, 96-143), file `common.UNHANDLED_LOG` (146-170); all `WHERE ts >= now − days·86400` | JSON to `--out` or stdout (200-212) | `analytics/health_report.ps1` runs it with `--days 7` into `%TEMP%`, then an `agent.exe -p` call writes `<TWDATA>/reports/health/health_<stamp>.md` from `health_report_prompt.md`. No scheduler entry found in the tree (grep `schtasks`/`Register-ScheduledTask`): trigger = UNKNOWN. |

---

## E. `app.*` schema readers and writers

DDL for `app.starts, experiments, segments, campaign_tags, boards, views, settings` lives in decisions/workspace.py:13-58, applied by `workspace.ensure()` on every call of every helper (98-115), which also seeds one board and two views (69-95, 104-113). `app.game_event` and `app.game_event_state` DDL lives in logs/events_stream.py:19-35, applied on every `_db()` (38-42).

| Object | Writer file:line | Reader file:line | Purpose |
|---|---|---|---|
| `app.starts` | `workspace.start_id` (149-165, `INSERT … ON CONFLICT DO NOTHING` — no caller found in tree); `workspace.sync_starts` (168-177: `INSERT INTO app.starts SELECT DISTINCT COALESCE(campaign_map,''), COALESCE(faction,''), COALESCE(leader,''), difficulty FROM campaigns WHERE faction IS NOT NULL ON CONFLICT DO NOTHING`) called from analytics/gamestate.py:398-402 every pass that has pending turns | `sync_starts` itself (`COUNT(*)`, 186-187) — no API reader | One row per playable start. **Measured: 951,908 rows / 154 MB, only 326 distinct `(campaign_map, faction, leader)`, 951,770 rows with `difficulty IS NULL`, `MAX(start_id)` = 1,639,424.** Cause in code: `UNIQUE(campaign_map, faction, leader, difficulty)` (workspace.py:20) does not treat two NULL `difficulty` values as equal, so every `sync_starts` re-inserts the NULL-difficulty starts (320 of 4,827 `campaigns` rows have NULL difficulty). |
| `app.campaign_tags` | `sync_starts` (178-185: `INSERT … SELECT c.campaign_key, s.start_id FROM campaigns c JOIN app.starts s ON … AND s.difficulty IS NOT DISTINCT FROM c.difficulty ON CONFLICT(campaign_key) DO UPDATE SET start_id=excluded.start_id`); `workspace.tag_campaign` (229-240) from decisions/decisions_stream.py:30-33 when env `TW_EXPERIMENT_ID`/`TW_SEGMENT_ID` are set | none in tree | Links campaign → start / experiment / segment. **Measured: 4,821 rows, 4,409 with `start_id`, 412 with `experiment_id`; 418 campaigns have no `start_id`; the highest tagged `campaign_id` is 4,473 while `campaigns` reaches 4,897.** With duplicated NULL-difficulty starts the `INSERT … SELECT` yields several rows per NULL-difficulty `campaign_key`; the statement's failure is swallowed by gamestate.py:401-402 (`except Exception: pass`). Whether Postgres rejects it with "ON CONFLICT DO UPDATE command cannot affect row a second time" was not executed (read-only session) — UNKNOWN by execution, consistent by measurement. |
| `app.experiments`, `app.segments` | `workspace.experiment` (193-205), `open_segment` (208-226) from runctl.py:249-258 (`_record_launch → open_segment`), also reached by `POST /api/infra/launch|coldstart` via proc.py:145 | none in tree | Launch ledger. Measured: 1 experiment (`status='live'`), 2 segments (seq 2 and 3, `code_version` `0.3.0+gbc47d673ea7e`, `0.3.3+gadfaf3e973d9`). |
| `app.boards`, `app.views` | seeded by `ensure()` (104-113) | none in tree | Measured: 1 board "Exclusive choices", 2 views (specs reference records `research`, `regions` that no working-tree code serves). |
| `app.settings` | `workspace.save_settings` (135-146) — no caller found | `workspace.settings` (118-132) — no caller found | Measured 4 keys (`score`, `home_map_frame`, `home_characters`, `include_bot_campaigns`), i.e. the `DEFAULT_SETTINGS` from workspace.py:62-67. |
| `app.game_event`, `app.game_event_state` | logs/events_stream.py:139-148 (batched `INSERT INTO app.game_event(ts, campaign_key, turn, faction, event, in_player_turn, payload)`), `_save_offset` (52-56); stream started by manager/manager.py:279, 326-327 | `_offsets` (45-49) reads `game_event_state` only; **no reader of `app.game_event` anywhere in the tree** | Persists the game's script-log event stream. Measured: ≈5.0 M rows / **1,534 MB** (largest table in the DB after the corpus), last row 09-03 20:44:01; 409 source offsets. |

---

## F. Dead or duplicated (facts only)

**Analytics tables with no consumer** (grep over the tree incl. ui/src): all 14 `game_turn*` tables (analytics/gamestate.py; ≈870 MB), `decision_resources` (156 MB) and `decision_heroes` (23 MB) (state_facts.py:29-39), `growth_summary` (growth_rollup.py), `item_event_state` (internal state of item_events only). `model_agreement` columns `top5_overlap, top10_overlap, taken_*` are read only by the rollups. `generations.generation_gains` (generations.py:126-158) has no caller but its `__main__`.

**DB objects not created by any working-tree code** (cleanup inventory, names/sizes only): `analytics.game_turn_recruit` (≈668k rows, 87 MB), `analytics.game_turn_war` (≈138k rows, 14 MB), `analytics.game_turn_slot` (0 MB); schema `game` (52 tables, all 0 MB); schemas `test_fixture` (≈30 MB; created by `decisions.pgtest`, referenced only from `coverage.selftest`) and `test_fixture_analytics` (≈2 MB).

**API endpoints with no UI caller**: `GET /api/campaigns/matrix` (app.py:505), `GET /api/campaigns/{key}/decisions` (591), `GET /api/campaigns/starts/{m}/{f}/actions` (213), `GET /api/models/agreement/breakdown` (762), `GET /api/analytics` (769), `POST /api/analytics/rebuild` (774). `ui_docshots.py` targets `/api/dash/*` routes that do not exist.

**Dead code in the API package**: `queries.join_outcomes` (193), `ucb_pick_counts` (938), `_start_means` (1519), `rho_for` (4547), `ALIGNMENT_CAVEAT` (4374), `REWARD_CAMPAIGNS` (49), `campaign_rows(outcomes=, produced=)` parameters (487; every caller passes neither), `db.write` (69), `db.db_path` (53), `db.columns` (96), `SeriesPoint` model (models.py:43). `workspace.start_id`, `settings`, `save_settings` have no caller.

**Re-derivations of something another table already stores**:
1. `_positions_data` (queries.py:2843-2877) decodes every campaign blob for `treasury, armies, heroes, is_researching, ll_wounded, resources` — exactly the columns `analytics.decision_features/decision_resources/decision_heroes` hold (state_facts.py:100-139); the API reads `decision_features` only in `_fact_turn_states` (2300-2315) and never the other two.
2. `queries._ended_because` (63-85) duplicates `state_facts._because` (339-359); the API receives the precomputed `because` (181, 188) and its own copy runs only when that is NULL; the `growth` branch (66-80) can never fire because `outcome_join` sets `"growth": {}` (188).
3. `campaign_rows` computes reward in Python (`_weighted_reward`, 1117-1123, 549-550) while `_FACT_CAMPS` recomputes it in SQL (1126-1134) and `campaign_reward_series` uses a third formula from `base_model.TARGET_WEIGHTS` (4755-4783); `ProducedCampaign.reward` and `WindowEdgeRow.reward` use a fourth (`settlements_gained + levels_gained` unweighted, 5143-5145, 5191-5192).
4. `_campaign_settlement_growth` (4743-4749) reruns `TRAJECTORY_SQL` on the corpus per request; `analytics.campaign_growth` already holds every field `CG.enrich` needs (campaign_growth.py:66-91) and `campaign_rows` reads it (491).
5. `campaign_state` (249-285), `_campaign_chars` (3250-3270) and `_start_snapshots` (1899-1909) decode entity blobs for research/equipped/pool/rank/hp/wounded/region/skill_points/skills — all materialised per turn in `game_turn`, `game_turn_character`, `game_turn_skill`, `game_turn_item` (gamestate.py:27-64), which nothing reads.
6. `ucb_context`/`starts_rows` recompute UCB stats from `campaign_gains` in Python (696-740, 752-866) while `ucb_picks`/`ucb_pick_rows` store the selector's own `n, mean, blend, entropy, std, explore, score, adjust` per pick (pg_schema.py:126-140); `starts_rows` takes `n` from `start_counts` **and** from campaign rows (769-770, 827).
7. `campaign_growth` (analytics) and the `campaign_gains` view both aggregate `decisions` per campaign for first/peak settlements and lord level (campaign_growth.py:8-32 vs pg_schema.py:259-275); `campaign_rows` joins both (491, 501).
8. `pick_campaigns` (671-692) joins picks to campaigns by a 120 s timestamp window; `campaigns.selector` (pg_schema.py:48) and `campaigns.picked_ts` exist but no stored pick link is read — UNKNOWN whether `selector` carries the pick id.
9. `ucb_pick_rows` (5244-5245) builds the entire `ucb_pick_series` to locate one head row.
10. `start_head` (953-955) recomputes `gains_all` + `ucb_context` although `get_starts` already did; `starts_rows` (753-754) rebuilds `campaign_rows` uncached and `campaign_rows` calls `gains_all` a second time (501).
11. `_stamped("campaign_rows")` vs `_stamped_slow("campaign_rows")` key collision (§0) makes both caches miss alternately.
12. Views `decision_points` and `interrupt_decisions` join campaign/world/panel blobs (pg_schema.py:169-177, 238-251) that `collect_timing`, `decision_detail`, `timeline`, `menus` never read.
13. `_fact_item_rows(mkey,fkey)` (1537-1545) aggregates the whole items family in its `per` CTE before filtering to the start.
14. `diplomacy_tail` reads payload keys (`deal_score, success_chance, outcome, result, standing, target, kind, turn`) that the measured payloads do not carry (1/10 sample of 74,145 rows: key set `channel, faction, gift, ok, pair, panel, req_id, rpc_id, terms, treaty_before, ended_by, tracked, turns_played` + 66-row `answer, attitude, chosen, speech, …`); `campaign_buildings` reads `cost` for `horde_building` whose params carry no `cost`.
15. `_interrupt_coverage` (3746-3762) decodes all 43,392 `options_json` on every `/api/decisions/menus` request; `agreement_breakdown`/`model_agreement` already summarise per-arm coverage for actions but nothing precomputes it for interrupts.
16. `metrics_db.connect()` re-executes the `metrics` DDL on every write (metrics_db.py:24-28); `workspace.ensure()` re-executes the `app` DDL on every helper call; `events_stream._db()` likewise.

**Fallbacks noted (CLAUDE.md asks for them to be flagged)**: `labels._have()`-gated empty returns for every reference table (labels.py:132-137 and each `if not _have(...)` branch); `labels._one/_rows` swallowing all exceptions (31-38, 119-126); `adb.rows/one` returning `[]/None` on any `psycopg.Error` (analytics_db.py:48-60); `_pool()` bare `except Exception → {}` (queries.py:660-667); `pair_options`, `model_versions`, `agreement_matrices`, `decision_agreement`, `rho_for` bare `except Exception` (3466-3469, 4002-4006, 4222-4232, 4515-4519, 4549-4559); `gamestate.step` swallowing `sync_starts` failures (gamestate.py:398-402); `decision_detail` second query when `action_taken` has no row (3601-3607); `ucb_pick_series`/`ucb_pick_rows` recomputing `blend` when the column is NULL (5133-5134, 5256-5257); `_options_of` accepting both dict and list shapes (3432-3442).

---

## G. Measurement appendix (live DB, 2026-09-04, read-only, all queries ≤ 2 s unless stated)

Corpus sizes (`pg_class.reltuples`, `pg_total_relation_size`): `offer_scores` 22.4 M / 2,648 MB; `blobs` 949,090 / 2,388 MB (sample avg `length(z)` 8,802, max 41,367; `entities` 1,130,208 rows over 699,180 distinct `features_blob`; `decisions` use 85,201 distinct `campaign_blob` and 105,177 distinct `world_blob`); `offer_model_scores` 22.1 M / 2,298 MB; `offers` 24.8 M / 2,229 MB; `actions` 5.1 M / 1,619 MB; `taken` 205,533 / 349 MB (`diagnostics` avg 2,428 chars, `timing` 112); `decisions` 206,907 / 136 MB (`timings` avg 315); `entities` 117 MB (province 368,446; lord 314,467; hero 240,388; campaign 206,907); `ucb_pick_rows` 497,037 / 87 MB; `diplomacy_events` 74,145 / 38 MB (`payload` avg 305); `interrupts` 43,392 / 18 MB (`options_json` avg 627); `postmortems` 5,020 / 15 MB (`payload` avg 6,959); `campaigns` 4,827 (outcome: stagnant 3,985, NULL 435, stuck 248, defeated 158, completed 1; `taken.policy` never NULL, values `forced_end_turn, greedy_catboost, greedy_gnn, marwil_gnn, random`).

`analytics_state` (watermark / rows / last_run 09-03): model_agreement 206,942 / 620,712; acquisitions 206,943 / 255,267; decision_features 206,943 / 206,905; item_events 206,943 / 19,250; game_turn 206,943 / 27,772; campaign_growth 4,897 / 4,827; campaign_endings 5,025 / 4,392; model_generations 152 / 152; agreement_summary 3 (last fold 343,818 rows summarised in 2.6 s); agreement_series 222 (2.5 s); agreement_breakdown 104 (6.2 s); growth_summary 1. `agreement_summary` all-scope: `greedy_catboost|marwil_gnn` comparable 85,558 of 206,904 (missing_b 92,557, no_scores 28,155, rho_median 0.041); `greedy_catboost|greedy_gnn` 174,168 (rho 0.051); `marwil_gnn|greedy_gnn` 84,092 (rho 0.070).

Query timings (single run, other lanes active): `COUNT(*) FROM offers` 0.33 s; `campaign_gains` 0.07 s; `_decs_all` 0.04 s; `_acts_all` 0.03 s; `_positions_data` takes 0.58 s (167,374 rows); campaign-blob scan extrapolated ≈4 s fetch + ≈1 s decode (1/50 sample); `_start_snapshots` busiest start (283 campaigns) 0.25 s / 835 blobs / 8.15 MB; `_interrupt_coverage` fetch 0.08 s; `decision_facets` 0.37 s; `matrix` 0.16 s (159,274 rows); `actions_summary` typed 0.08 s (68,276 rows → ≈76 chunked `actions` lookups); `ucb_pick_rows rank<=2` 0.13 s; `turn_open` allies/vassals 0.07 s; `_hist_map('building')` 0.08 s (23,059 rows); `db.stamp` 3 × ≤0.01 s. Plans: `catalog_overtime` firsts = parallel seq scan of `taken`; `start_counts` view = correlated `COUNT` subquery per campaign; `_positions_data` = merge join walking the whole `blobs_pkey`.