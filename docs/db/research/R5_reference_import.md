# R5 — Reference data: RON schema, keys, FKs, packs, version, loc, as-is import

Note: report file not written (session is in plan mode: only the plan file may be written); this message is the report.

Sources reused: `research/decode_probe.{txt,json}`, `type_census.txt`, `pack_scan.json`, `schema_wh3_full.json` (from `ron_schema.py`), `loc_probe.py` (re-run). All new numbers come from the inline heredoc scripts summarised per section; all DB access `pg.connect(autocommit=True, readonly=True)` + `SET statement_timeout='600s'`.

## A. RON schema facts

**File.** `D:\twdata\reference\ui3_extraction\schema_wh3.ron`, 10,769,707 B, 253,234 lines, mtime 2026-08-06 03:36 (identical mtime to all 806 entries of that folder). Top level: `( version: 5, definitions: {...}, patches: {...} )` (`schema_wh3.ron:1-3`, `:251943`). `version: 5` is the RPFM schema *format* version; the file carries no date, game build or changelog → schema date/provenance UNKNOWN. Nothing in the repo reads it: `grep -rn "schema_wh3\|\.ron\b\|rpfm" --include=*.py --include=*.md .` → 0 hits (excluding .venv). `patches:` is 340 KB, 190 table entries, content is `lookup_hardcoded` strings (e.g. `_kv_battle_ai_ability_usage_variables_tables.key`).

**Definition record shape** (`schema_wh3.ron:4-42`): per table a list of versions `(version, fields[], localised_fields[], localised_key_order[])`; per field `name, field_type, is_key, default_value, is_filename, filename_relative_path, is_reference: Some((table_without_suffix, column)), lookup: Some([cols]), description, ca_order, is_bitwise, enum_values{}, is_part_of_colour`.

**Counts** (script: load `schema_wh3_full.json`, iterate versions/fields; "used" = the version whose number equals the pack header version per `decode_probe.json`):

| metric | all definitions | versions used by db.pack |
|---|---|---|
| tables / versions | 1,663 / 2,231 | 1,520 / 1,520 |
| versions-per-table histogram | 1:1339 2:215 3:51 4:27 5:13 6:9 7:2 8:1 9:2 10:2 11:2 | 1:1201 2:210 3:51 4:27 5:13 6:9 7:2 8:1 9:2 10:2 11:2 |
| fields | 14,795 | 6,850 |
| is_key | 3,240 (21.9%) | 2,313 (33.8%) |
| is_reference | 5,585 (37.7%) | 2,901 (42.4%) |
| default_value Some | 4,516 (30.5%) | 2,178 (31.8%) |
| description non-empty | 2,720 (18.4%) | 1,103 (16.1%) |
| is_filename | 270 (filename_relative_path Some: 105) | 140 |
| lookup Some | 246 | 165 |
| ca_order != -1 | 12,123 | 6,845 (99.9%; 0 tables all -1) |
| is_bitwise != 0 / enum_values non-empty | 0 / 0 | 0 / 0 |
| is_part_of_colour Some | 99 fields (r/g/b triplets, e.g. `ancillary_uniqueness_groupings_tables` v4 `col_r/g/b`) | — |
| localised_fields | — | 221 tables, 380 fields; 21 tables with multi-column `localised_key_order` |
| versions with **no** key column | 9 (`campaign_bonus_value_ids_province_initiatives_tables` v0, `..._technologies_tables` v0, `effect_bonus_value_technology_category_junctions_tables` v0, `effect_bonus_value_technology_junctions_tables` v0, `effect_bonus_value_unit_class_junction_tables` v0, `loading_screen_texts_tables` v1, `provinces_to_region_groups_junctions_tables` v0, `ritual_payload_spawn_storms_tables` v0, `warscape_rigid_lod_range_tables` v1) — none is the version the pack uses | 0 |

**Pack ↔ definition matching** (`decode_probe.txt:3-7`, `ron_stats2`-style recount): db.pack has 1,521 tables; 1,520 have a RON definition at the pack's header version and decode CLEAN-EOF (865,404 rows, 1.6 s decode); the 1 unmatched is `autoresolver_unit_group_categories_enums_tables` (v1, header row count 0, 91 raw bytes, no definition at all). 143 RON definitions have no table in db.pack. The pack version is the *newest* RON version for 1,461 tables; for 59 tables the pack is on an older version than the RON's newest (e.g. `factions_tables` 6 vs 36, `main_units_tables` 7 vs 36, `agent_subtypes_tables` 3 vs 19, `ancillaries_tables` 0 vs 18, `rituals_tables` 4 vs 15, `building_levels_tables` 2 vs 16, `technology_nodes_tables` 0 vs 3, `effects_tables` 0 vs 3, `regions_tables` 0 vs 6, `dilemmas_tables` 3 vs 5, `missions_tables` 0 vs 6). Whether those newer definitions belong to a later game build or to other content is UNKNOWN. Used-version histogram: v0 983, v1 232, v2 106, v3 62, v4 32, v5 20, v6 18, v7 12, v8 7, v9 5, v10 7, v11 6, rest ≤5 each (max v54 `land_units_tables`).

**Keys in used versions**: single-column 952, composite 568 (2 cols 401, 3:141, 4:13, 5:8, 6:1, 7:1, 8:1, 11:1 `cai_construction_system_building_values_tables`, 12:1 `cai_construction_system_synergies_tables`), no key 0. Key-column types: StringU8 2,057, OptionalStringU8 117, I64 62, I32 59, Boolean 14 (e.g. `character_experience_skill_tiers_tables.for_army/for_navy`), F32 3 (`battle_currency_modifiers_thresholds_tables.threshold`, …), StringU16 1.

**Field types.** Whole RON (`grep -o "field_type: [A-Za-z0-9_]*" schema_wh3.ron | sort | uniq -c`, minus the localised_fields duplicates): 11 types — StringU8 5,070, OptionalStringU8 3,280, F32 2,084, Boolean 2,049, I32 2,033, I64 192, F64 45, ColourRGB 36, StringU16 4, OptionalI32 1, I16 1. **No `SequenceU16/U32`, `OptionalI64`, `OptionalF32`, `ColourRGBA`, bitmask or nested-table type exists anywhere in this schema.** Versions used by db.pack: 10 types (`type_census.txt:4-13`); OptionalI32's only site is `start_pos_characters_tables` v1 `turns_died_before_start` (pack uses another version); StringU16 sites `advisors_tables.advisor_icon_path`, `battle_types_to_victory_conditions_tables.battle_type`; I16 site `start_pos_regions_tables` v10 `capture_prestige`. The decoder `_read_field` (`build_reference.py:130-145`) handles 12 types = all 11 RON types + OptionalStringU16 → **covers 100 % of the schema**; the earlier map's "SequenceU16/U32 unsupported" gap is moot for this schema. Decoder lossiness to note: F32/F64 rounded to 4 dp (`:139-140`; 29,254 of 550,111 F32 values altered = 5.3 %, `type_census.txt:31`); OptionalStringU8 absent → `""` (`:118-120`; 792,260 = 70.1 % of optional values absent, 0 present-but-empty, `type_census.txt:16-17`) so absent/empty collapse; ColourRGB → `"#RRGGBB"` drops the top byte (always 0 in data, `type_census.txt:49`); `parse_pack` asserts PFH5 (`:40`), honours only `HAS_INDEX_WITH_TIMESTAMPS` (`:15,47`), reads the whole pack into memory (`:39`) and `read_file` linearly scans the entry list per lookup (`:59-67`).

**schema_db.json vs RON** (`advisor/reference/schema_db.json`, 81,584 B): 53 tables / 96 versions; types I32 444, StringU8 392, OptionalStringU8 307, Boolean 251, F32 99, I64 7, ColourRGB 7; every JSON version exists in the RON; for the 53 pack-used versions 45 layouts are byte-identical to the RON (names+types), 8 differ in **names only** (placeholders): `campaign_group_member_criteria_{cultures,factions,subcultures}_tables` (`col0/col1/col2` vs `culture|faction|subculture, member, context`), `campaign_group_members_tables` (`campaign_group/campaign_group_member/weight` vs `group/id/priority`), `campaign_post_battle_captive_options_tables` (`record_key/col_i32_a/outcome` vs `id/sort_priority/captive_outcome`), `character_skill_node_set_items_tables` (`set_key/extra` vs `set/mod_disabled`), `character_traits_tables` (`unk_a/unk_b/unk_c` vs `no_going_back_level/hidden/precedence`), `trait_level_effects_tables` (`level_key` vs `trait_level`); plus literal field name `FIXME` in `action_results_additional_outcomes_tables` v4 (unused version). The JSON carries no key/reference/localised metadata at all (shape `{table:{version:[[name,type],…]}}`, `build_reference.py:164-166`).

## B. Key uniqueness in the data

Method: decode all 1,520 tables with the RON layouts through `BR.decode_db_table`, `Counter(tuple(row[k] for k in key_cols))` per table (same as `decode_probe.py:70-76`; recomputed this lane).

- **Duplicate keys: 0 tables** of 1,520 (`decode_probe.txt:123-125`, recount identical). Every table's RPFM key is unique within db.pack, including the 96 OptionalStringU8 key columns that contain empty/absent values and the 14 Boolean / 3 F32 key columns.
- **No-key tables: none** (all 1,520 have ≥1 key column). **Empty tables: 0** (`decode_probe.txt:65`).
- Composite keys: 568 tables; in 143 of them the first key column alone is already unique, in 425 the composite is genuinely needed.
- Widest keys: `cai_construction_system_synergies_tables` 12 cols/450 rows, `cai_construction_system_building_values_tables` 11/2,109, `battle_secondary_currency_sources_links_tables` 8/54, `mission_timed_payloads_tables` 7/1,281, `battle_catchment_override_battle_mappings_tables` 6/2,220, `unit_set_to_unit_junctions_tables` 5/21,476, `character_skill_level_details_tables` 5/2,100, `special_ability_to_special_ability_phase_junctions_tables` 5/2,487.
- Caveat: uniqueness holds *within db.pack*; cross-pack override duplicates cannot occur here because no other pack carries `db/` (section D).

## C. Foreign keys (is_reference)

Method: for every field with `is_reference: Some((tgt, col))` in the pack-used version, `tt = tgt + "_tables"`; non-empty values (`not in (None, "")`) tested for membership in `{row[col] for row in data[tt]}`.

| metric | value |
|---|---|
| reference fields = distinct (src table, col)→(tgt table, col) pairs | 2,901 (all pairs distinct) |
| distinct target tables | 861 (653 present in db.pack, **208 absent**) |
| relations whose target table is missing from db.pack | **323** |
| relations whose target column is missing in the target's used version | 0 |
| relations with ≥1 non-empty value / all-empty | 2,465 / 113 |
| non-empty referencing values / found in target | 1,776,258 / 1,770,164 = **99.66 %** |
| match-fraction histogram (relations with data) | 100 %: 2,450; 99.89-99.92 %: 3; <5 %: 12 |
| self-referencing relations | 17 |
| relations targeting a non-key column | 6 (`loading_screen_quotes_to_campaigns.campaign→campaign_map_playable_areas.mapname`, `shroud_blackout_regions.campaign→campaigns.map_name`, `taxes_effects_jct.tax_name→taxes_keys.tax_lookup`, `time_of_legends_campaigns.campaign_map→campaign_map_playable_areas.campaign_key`, `unit_variants_colours.unit_variant→unit_variants.name`, `units_custom_battle_types_to_factions.type→units_custom_battle_types.id`) |
| (target table, column) pairs / not unique in target | 656 / 2 (`unit_variants_tables.name` 2,015 distinct of 2,507 rows; `campaign_map_playable_areas_tables.campaign_key` 5 of 10) |
| tables with no relation in or out | 66 (mostly `_kv_*`, `audio_*`, `agent_localisations_tables`) |

Missing target tables (top, relations count): `audio_vo_actors` 17, `tristates` 13, `audio_rtpc_assignments` 9, `campaign_anim_set_enums` 8, `audio_battle_environment_types` 6, `campaign_maps` 5, `campaign_battle_type_enums` 5, `pooled_resource_scopes` 5, `audio_voiceover_cultures` 5, `cdir_events_mission_statuses` 5, `autoresolver_optional_bool_enums` 4, `campaign_localised_strings` 3, `campaign_map_tooltip_texts` 3, `genders` 2, `campaign_difficulties` 2, `campaign_types` 2, `name_types` 2 … (208 total; largely enum/audio tables the game hard-codes).

The 15 imperfect relations: 3 near-misses are `cdir_events_mission_{issuer,option,payloads}_junctions_tables.mission_key → missions_tables.key` (2,466/2,468, 8,582/8,590, 3,513/3,517; missing keys are `wh3_dlc25_mis_dwf_malakai_spider_swarm_level_2[_ie]`). 11 of the 12 "<5 %" relations are **type mismatches that resolve 100 % after `str()` cast**: `battle_set_piece_armies_characters_tables.forename/surname`, `campaign_rogue_army_leaders_tables.name/surname`, `effect_bonus_value_name_record_junctions_tables.name_record`, `unique_agents_tables.clan_name/forename/other_name/surname` (StringU8 → `names_tables.id` I64: 1,992/1,992 etc.), `advice_levels_info_text_juncs_tables.advice_level` (I32 → `advice_levels_tables.key` StringU8: 33/33), `campaign_map_playable_area_ownership_content_pack_junctions_tables.campaign_map_playable_area` (I32 → `.index` StringU8: 10/10). The single real miss is `cai_decision_items_tables.culture_occupation_decision → culture_settlement_occupation_options_tables.id` 77/1,574 where `0` is a null sentinel.

30 largest relations (all 100 % of non-empty): `diplomacy_negotiation_string_options.{key→diplomacy_key_groups.group_id, string→diplomacy_strings.key, group→campaign_groups.id}` 64,889 each; `character_skill_node_set_items.{set→character_skill_node_sets.key, item→character_skill_nodes.key}` 29,517; `character_skill_nodes.character_skill_key→character_skills.key` 24,649; `building_effects_junction.{building→building_levels.level_name, effect→effects.effect, effect_scope→campaign_effect_scopes.key}` 21,613; `unit_set_to_unit_junctions.{unit_set→unit_sets.key 21,476, unit_record→main_units.unit 21,104 non-empty}`; `character_skill_node_links.{child_key, parent_key→character_skill_nodes.key, link_type→character_skill_node_link_types.link_type}` 20,590; `campaign_group_members.group→campaign_groups.id` 17,015; `character_skill_level_to_effects_junctions.{character_skill_key→character_skills.key, effect_key→effects.effect, effect_scope→campaign_effect_scopes.key}` 16,844; `effect_bundles_to_effects_junctions.{effect_bundle_key→effect_bundles.key, effect_key→effects.effect, effect_scope→campaign_effect_scopes.key, advancement_stage→effect_bundle_advancement_stages.key}` 16,430; `effects.category→effect_categories.key` 15,064; `campaign_group_member_criteria_factions.{faction→factions.key, member→campaign_group_members.id}` 11,219; `campaign_group_member_criteria_subcultures.{member→campaign_group_members.id, subculture→cultures_subcultures.subculture}` 10,769; `faction_agent_permitted_subtypes.{agent→agents.key, faction→factions.key, subtype→agent_subtypes.key}` 9,187. Most-referenced targets: `factions` 87 relations, `effects` 69, `campaign_groups` 66, `particle_effects` 61, `main_units` 57, `effect_bundles` 44, `resource_costs` 44, `cultures_subcultures` 43, `building_levels` 42, `campaigns` 38, `cultures` 36, `agent_subtypes` 35.

## D. Packs and load order

From `pack_scan.json` (index-only scan of every `*.pack` in `D:/SteamLibrary/steamapps/common/Total War WARHAMMER III/data`, 261 packs, 0 scan errors, all magic `PFH5`):

- **Packs containing `db/`: exactly one — `db.pack`** (1,521 files = 1,521 tables, one file `db/<table>/data__` per table, header flags 0x0, header timestamp 0). 0 tables appear in more than one pack. Packs containing `.loc`: `local_en.pack` 235, every other `local_{br,cn,cz,fr,ge,it,kr,pl,ru,sp,tr,zh}.pack` 1. Header pack-type byte: 259 packs type 1 (Release), `boot.pack` type 0 (Boot), `tw.pack` type 4 (Movie). 10 packs declare a dependency pack in the header (`data_bl.pack→data.pack`, `ui_3.pack→ui.pack`, `anim_3.pack→anim.pack`, `variants*_bl.pack`, `audio_base_m.pack`).

| pack | bytes | mtime | files | header ts |
|---|---|---|---|---|
| db.pack | 7,826,301 | 2026-07-16 10:02:55 | 1,521 | 0 |
| data.pack | 403,268,199 | 2026-07-09 17:10:47 | 13,207 | 2022-02-21 13:26:17 |
| local_en.pack | 31,367,487 | 2026-07-09 17:10:47 | 373 | 2022-02-21 13:14:43 |
| data_maps.pack | 419,005,639 | 2026-04-01 03:22:16 | (read by `build_map_geometry.py:272`) | — |
| boot.pack | 42,975 | — | 30 | — |
| tw.pack (project mod) | 230,722 | 2026-08-31 19:55:39 | 33 (0 db, 0 loc) | — |

Load-order rule: CA packs list in `data/manifest.txt` (16,020 B, mtime 2026-07-16 13:55, 492 lines `path\tsize\tflag`, 260 `.pack` lines — every pack except `tw.pack`; flag is 1 for all but `data_bl.pack`=0, meaning UNKNOWN). The documented CA/RPFM rule (not verifiable from local files, stated as known): packs load by header type Boot(0) → Release(1) → Patch(2) → Mod(3) → Movie(4), alphabetically within a type, and the last-loaded copy of a file path wins; Mod packs must be enabled (`used_mods.txt` / launcher), Movie packs load unconditionally — which is why the project writes `tw.pack` as type `MOVIE = 4` (`bus/pack_multi.py:14`, `:134`) and installs it at `data/tw.pack` (`launcher/bus_launcher.py:20-21`). For db tables the rule is currently irrelevant: with only `db.pack` carrying `db/` there is no cross-pack override to resolve.

Mods enabled: none. `<game>/used_mods.txt` is 0 bytes (mtime 2026-07-17); `%APPDATA%\The Creative Assembly\Launcher\20190104-moddata.dat` is 2 bytes `[]` (mtime 2026-08-29); `twmm/` (third-party mod manager, `_meta/version.txt` = `4224151`) present but no mod list; `lua_mod_log.txt` shows only `script/*/mod/*.lua` from the project's `tw.pack` (`twcontrol`, `twstate`, `battle_logging`, …). The repo never reads `used_mods.txt`/moddata (grep → 0 hits).

## E. Game version

- **`decisions/collect.py:218-240` `_game_version()`**: reads the Win32 version resource of `<GAME_DIR>/Warhammer3.exe` via `ctypes.windll.version.GetFileVersionInfoW` + `VerQueryValueW("\\")`, formats `dwFileVersionMS/LS` as `a.b.c.d`, caches in `_GAME_VERSION`, writes `None` on failure (stderr line `:238`); stored per decision as `campaign.game_version` (`:268`; blob field, `coverage.py:22`; not in MODEL_COLUMNS per R2 `:131`). Nothing writes it into `reference.meta`.
- **Value now: `8.1.1.0`** (FileVersion = ProductVersion; string table under `040904B0` absent). `Warhammer3.exe` 241,712,592 B, mtime 2026-07-16 10:02:55 — identical to `db.pack`'s mtime, i.e. the 07-16 update shipped exe + db.pack (+ `libled.dll`), while `data.pack`/`local_en.pack` date 07-09.
- Steam: `D:/SteamLibrary/steamapps/appmanifest_1142710.acf` `buildid 24237342`, `lastupdated 1784235334` = 2026-07-16 13:55:34 (same minute as `manifest.txt`), `LastPlayed` 2026-09-04 11:09. The repo never reads the appmanifest (grep `appmanifest|buildid` → 0).
- No version string inside packs: index scan of boot/data/db/local_en/data_script for `version`-named entries → none (only `*building*` noise). Pack header timestamps are 0 (db.pack) or 2022-02-21 (data/local_en) — not usable as build ids. Practical fingerprint available today: (exe FileVersion, Steam buildid, db.pack size+mtime).

## F. Localisation

`loc_probe.py` re-run: `local_en.pack` parse 0.01 s; **235 `.loc` files, 241,972 entries, 241,972 distinct keys, 0 duplicate keys**; packed 5,702,613 B, decompressed 63,399,516 B, UTF-16 payload 62.2 MB, UTF-8 payload 31.1 MB; decode 0.40 s; max key 174 chars, max text 3,121; 45,551 empty texts; 14,436 texts are `{{tr:…}}` indirections. Header per file: `FF FE "LOC" 00 01 00 00 00` then u32 count at offset 10, rows from 14 as u16-len UTF-16LE key, u16-len value, 1 pad byte (`build_reference.py:70-82`). 232 of 235 files are `text/db/<table>__.loc`; 3 are `encyclopedia_*__coc_.loc`. Largest: `diplomacy_strings__.loc` 23,809, `effects__.loc` 15,064, `character_skills__.loc` 11,888, `effect_bundles__.loc` 11,710, `uied_component_texts__.loc` 10,214, `missions__.loc` 9,558, `ui_text_replacements__.loc` 9,505, `ancillaries__.loc` 8,013. 14 `.loc` stems have no db table (`building_description_texts`, `building_flavour_texts`, `building_short_description_texts`, `campaign_localised_strings`, `campaign_map_tooltip_texts`, `encyclopedia_*`, `message_event_text`, `ministerial_positions_strings`, …).

**Key convention verified**: `<table stem>_<localised_field>_<key values concatenated in localised_key_order>`. Reconstructing keys from RON `localised_fields` × decoded rows for the 221 tables gives 220,840 candidate keys, **218,059 (98.7 %) present in the loc**; only `ui_colours_tables` (0/163) and `units_custom_battle_permissions_tables` (0/2,570) yield nothing. Reverse: 220,840 of 241,972 loc keys (91.3 %) start with a known `<table>_<field>_` prefix; the rest are `uied_*` 10,214, `building_(description|flavour|short_description)_texts_*` 3,851, `mission*` 3,718, `campaign_*` 2,355, `start_pos*` 827. Multi-column examples: `building_culture_variants_tables` name key = `building+culture+subculture+faction` (`localised_key_order [0,1,4,5]`); `land_units_tables` uses field index 7 (`key`); `technologies_tables` fields `onscreen_name, short_description, long_description` on `key`.

**Population today**: `build()` `build_reference.py:554-567` — `DROP TABLE IF EXISTS loc; CREATE TABLE loc (key TEXT PRIMARY KEY, text TEXT)`, then for every `.loc` entry `decode_loc` and per entry `_rep` = `DELETE … WHERE key=%s` + `INSERT` (`:23-29`) → 241,972 rows, live size 64,348,160 B (table+pkey). No language other than `local_en` is read; the `{{tr:…}}` indirection is left unresolved in storage.

**Consumers**: `advisor/reference/features_db.py:123-125 label(key)` (`SELECT text FROM loc WHERE key=%s`), `:133-140 agent_action_label` (resolves one `{{tr:X}}` hop to `ui_text_replacements_localised_text_X`), `:143-159 agent_action_keys` (`LIKE 'agent_actions_localised_action_name_%<suffix>'`), `:294-303` subtype names via `agent_subtypes_onscreen_name_override_`; callers `launcher/cco_actions.py:903-904,952`. `advisor_api/labels.py:41-53 _loc` (cached, ≤2 `{{tr:}}` hops, tries raw key then `ui_text_replacements_localised_text_`), prefixes `regions_onscreen_` `:77`, `effect_bundles_localised_title_`/`provincial_initiative_records_localised_name_` `:99-100`, `agent_subtypes_onscreen_name_override_`/`names_name_` `:103-104`, `factions_screen_name_` `:107`, `effects_description_` `:183`, `building_culture_variants_name_%` bulk `:257-266`, `character_trait_levels_onscreen_name_` `:336`; sole importer `advisor_api/queries.py` (77 `labels.` uses). `build_reference._verify` `:599,605,611` and `_captive_reference` `:331` read loc during the build. Nothing else reads `loc` (repo-wide grep `FROM loc\b|reference\.loc`).

## G. As-is import verdict facts

Confirmed against working tree + live DB (`SELECT relname, reltuples, pg_total_relation_size … WHERE nspname='reference'` + per-table `COUNT(*)`):

- **44 tables, 118,808,576 B total** (113.3 MiB), owner `tw`. Exact rows: loc 241,972; building_effects 21,613; effects_meta 15,064; skill_effects 16,844; skill_links 23,361; skill_set_members 29,366; agent_permitted_subtypes 9,187; skill_indents 6,059; skills 5,944; buildings 5,259; ancillary_effects 4,386; tech_effects 3,734; dilemma_choice 2,866; ancillaries 2,671; units 2,609; skill_level_ranks 2,100; tech 2,056; building_chains 1,943; tech_links 1,792; trait_effects 1,735; rituals 1,326; trait_levels 979; merc_units 884; skill_actions 775; tech_groups 772; trait_meta 744; skill_node_sets 547; agent_actions 176; captive_options 95; captive_binding 92; action_result_outcomes 75; action_results 74; trait_antitraits 46; skill_categories 42; agent_types 9; agent_abilities 9; meta 2; ref_region 814; ref_tech 5,191; ref_tech_effect 10,456; ref_tech_parent 405; ref_skill 10,467; ref_skill_level 29,367; ref_skill_effect 33,027. (Earlier map's "skill_categories (empty)" / "trait_antitraits (empty)" is wrong: 42 and 46 rows.)
- **DROP+CREATE per build**: 34 `DROP TABLE IF EXISTS` sites in `build_reference.py` (`:227,248,260,269,282,297,325,385,403,413,427,456,473,484,519,555,662,672,687,697,706,721,…,874`). **Per-row DELETE+INSERT** via `_rep` (`:23-29`, keys from `_KEYS` `:17-20`, default `("key",)`); no `executemany`/`COPY` anywhere; `effects_meta` uses `INSERT … ON CONFLICT DO NOTHING` (`:878-879`). Ungated drops (decode failure still empties the table): `building_chains` `:402-403`, `buildings` `:412-413`, `tech` `:424-427`, `units` `:453-456`; gated: skills `:471-473`, rituals `:482-484`, all of `build_extra` (`:660-874`).
- **No version/hash columns, no triggers, no views** (`pg_trigger` non-internal in `reference`: 0; `relkind` other than r/i: 0; `information_schema.columns` matching `version|hash|mtime|built|source|pack|updated`: only `rituals.required_resources/expended_resources`). Constraints are 24 PKs only, no FKs; 17 plain indexes.
- **`reference.meta`** = 2 rows: `built = 1786170352.786` (2026-08-07 23:25:52), `extra_built = 1788319259.686` (2026-09-01 20:20:59) — written `:584-585`, `:883-884`. No pack mtime/size/hash, no game build.
- Builders are manual only: no caller of `build_reference`/`build_map_geometry` outside their own `__main__` (`:899-901`); the strings in `catalogue.py:16,22` and `collect.py:542` are error messages telling the operator to run them.
- **Consumers** (file:line): `advisor/reference/features_db.py` — `_lookup` tables `buildings/building_chains/tech/units/skills/rituals/ancillaries`, `ancillary_effects` `:112`, `loc` `:124,149,303`, `agent_actions/action_results/action_result_outcomes` `:166-175`, `agent_abilities` `:230,254`, `agent_permitted_subtypes` `:261`, `captive_binding/captive_options` `:276-278`, `skill_actions` `:321-322`; importers `advisor/features.py:11`, `advisor/options.py:237,269,282,407`, `advisor/loop.py:168`, `decisions/collect.py:1314`, `launcher/cco_actions.py:314,902,951`. `advisor/mapgraph/catalogue.py` — `_open` `:9-24` (hard-fails unless `reference.buildings` and `reference.ancillary_effects` exist), `_load` `:27-71` reads `buildings :37, building_chains :48, agent_actions :51, units :53, skill_actions :56, tech_links :58, skill_links :61, ancillary_effects :65`; `dense_ids` `:82-98` assigns 1-based ids in sorted key order from `_KIND_TABLE` `:74` (`buildings/building_chains/units/tech/skills/rituals/agent_actions/ancillaries`.key) and `_KIND_COLUMN` `:77-78` (`agent_permitted_subtypes.subtype`, `effects_meta.effect`); `attrs` `:102-113`. `advisor/mapgraph/corpus.py:44-65 fingerprint()` mixes `meta.built` + counts into the cache key. `advisor_api/labels.py` (section F + `ancillaries :141, effects_meta :168, buildings :297,306, skills :315, trait_levels :324, trait_meta :345, trait_antitraits :365, trait_effects :382, tech :410-433, tech_links :398, skill_categories/skill_indents :481-486, tech_groups :513, skill_links/skill_node_sets :531-560`). `decisions/collect.py:531-541 _merc_flavors` → `SELECT DISTINCT unit, flavor FROM merc_units`, raises `CollectError("reference merc_units is empty …")` `:541` (map said 535-546; drift). Analytics: no reference reads.
- **`ref_*` (7 tables, live game, not packs)**: DDL `decisions/gameref.py:14-54` (`CREATE SCHEMA IF NOT EXISTS reference` `:14`, `CREATE TABLE IF NOT EXISTS ref_region :16, ref_tech :22, ref_tech_effect :28, ref_tech_parent :32, ref_skill :35, ref_skill_level :41, ref_skill_effect :46`); `write()` `:80-156` = `ref_region` upsert `ON CONFLICT (region) DO UPDATE` `:90-91`, per-faction `DELETE`+`INSERT` for tech `:101-120`, per-subtype `DELETE`+`INSERT … ON CONFLICT DO NOTHING` for skills `:126-149`. Driven by `decisions/decisions_stream.py:38-73 _sync_reference` on every snapshot (`:125`): `have_faction_tech :53`, `have_subtypes :54`, `region_count()==0 :55` → `collect.game_reference` (`collect.py:1129`, Lua `_LUA_REF_REGIONS :1018` 60 s, `_LUA_REF_TECH :1051`, `_LUA_REF_SKILLS :1068`) → `gameref.write :68`. Live content: 814 regions (all 814 have `outline/cx/cy` filled by `build_map_geometry.py:36-42,345`), ref_tech for many factions (top `wh3_dlc27_hef_aislinn` 84 techs), 240 subtypes in ref_skill. **Readers: only `gameref.py:164,177,189` (its own existence probes) and `build_map_geometry.py:337,399` (`SELECT region[, adjacent] FROM reference.ref_region`).** Repo-wide grep `ref_(region|tech|skill)` hits 3 files only (`gameref.py` 34, `collect.py` 6 parsers, `build_map_geometry.py` 7). No API/model/analytics consumer.
- **Orphans**: `reference.skill_actions(skill TEXT, agent_action TEXT)` PK, 775 rows, 145 distinct skills / 163 actions — read at `catalogue.py:56` and `features_db.py:322`, created by nothing in the tree (grep `skill_actions|agent_action_unlocks` → only those two readers). It is re-derivable from the pack: `character_skill_level_to_effects_junctions_tables` has 1,583 rows whose `effect_key` contains `agent_action` (378 distinct skills, e.g. `wh2_dlc09_skill_all_dummy_agent_actions_tmb_liche_priest → wh2_main_effect_agent_action_enable_wizard_assist_army_replenish_troops`), and all 145 live skills are among them. `reference.dilemma_choice(dilemma, choice, title, label, ancillaries, effect_bundles, treasury float8)` 2,866 rows, no PK, no producer, **no reader** (the only `dilemma_choice` hits are unrelated: `advisor/loop.py:91` string, `bus/mod/twcontrol.lua:1508` event name).

**Earlier map: claims found wrong or unverifiable.** (1) `skill_categories`/`trait_antitraits` are not empty (42 / 46 rows). (2) `_merc_flavors` is at `collect.py:531-541`. (3) "SequenceU16/U32… unsupported" is a decoder property but not a gap for this game: the RON has none of those types; the decoder covers every type in the schema. (4) schema_db.json is a strict subset of the RON (45/53 identical layouts, 8 with placeholder names) rather than independent reverse-engineering — and a full RPFM schema (`schema_wh3.ron`) has been on disk since 2026-08-06 unread by any code. (5) Commit provenance claims (`89eb100`, `ce738bf`, `babfb23`, `1e7a685`) not verified (git history before `9a90568` is off-limits). (6) The map's "12 JSON entries decoded by nothing" not re-verified. Everything else reused here (PFH5 assert, 12-type dispatcher, 373/235 loc files, 1,521 single-file table folders, 44 tables, meta content, DROP+CREATE + `_rep`, no scheduler, consumer lists) re-checked and holds.